# Розділ 52: Фінальний проєкт — Інтелектуальний автономний рій БПЛА

---

## 📋 Анотація

Ви пройшли неймовірний шлях: від першого `println!("Hello, Agent!")` до макросів, від ownership до async/await, від одного простого агента на сітці до розподіленої системи з мережевою комунікацією. Тепер час **зібрати все разом** у цілісну систему.

Фінальний проєкт — це не просто ще одна вправа чи лабораторна робота. Це **інтеграційний досвід**, де кожен розділ підручника знаходить своє місце в реальній системі. Ви побачите, як ownership захищає від data races між агентами, як traits забезпечують гнучкість архітектури, як async масштабує систему до сотень агентів, як BDI робить їх по-справжньому інтелектуальними.

Цей розділ — не покроковий туторіал "напишіть цей код". Це **архітектурний путівник**: як думати про складну систему, як організувати код для підтримуваності, як тестувати, документувати, готувати до production. Це те, що відрізняє навчальний проєкт від реального продукту.

---

## 🎯 Цілі навчання

Після завершення цього розділу ви зможете:

1. **Спроектувати** архітектуру складної мультиагентної системи
2. **Інтегрувати** концепції з усіх попередніх розділів
3. **Організувати** код у модульну, підтримувану структуру
4. **Протестувати** систему на різних рівнях (unit, integration, e2e)
5. **Документувати** проєкт на професійному рівні
6. **Налаштувати** CI/CD pipeline для автоматизації

---

## 📚 Ключові терміни

| Термін | Визначення |
|--------|------------|
| **Системна архітектура** | Високорівнева структура системи та взаємодія компонентів |
| **Модульність** | Розділення системи на незалежні, замінювані компоненти |
| **Separation of Concerns** | Принцип: кожен модуль відповідає за одну річ |
| **Інтеграційне тестування** | Перевірка взаємодії між компонентами |
| **CI/CD** | Continuous Integration / Continuous Deployment |
| **Technical debt** | Компроміси в коді, що потребують майбутнього рефакторингу |
| **Production-ready** | Код, готовий до реального використання |

---

## 💡 Мотиваційний кейс: Від прототипу до продукту

Уявімо реальну ситуацію: ви розробили прототип системи координації БПЛА для пошуково-рятувальних операцій. Демонстрація пройшла успішно — рій з 5 дронів знайшов "потерпілого" (манекен) на тестовому полігоні. Інвестор зацікавлений. Тепер критичне питання:

**"Чи можемо ми використати це в реальній рятувальній операції через 6 місяців?"**

Відповідь залежить від того, чи у вас **прототип** чи **продукт**:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ПРОТОТИП VS PRODUCTION                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ПРОТОТИП:                        PRODUCTION:                       │
│                                                                     │
│  ✓ Працює на демо                 ✓ Працює стабільно 24/7          │
│  ✗ Падає при помилках             ✓ Graceful degradation           │
│  ✗ Немає тестів                   ✓ 80%+ test coverage             │
│  ✗ Один розробник розуміє код     ✓ Документація для команди       │
│  ✗ Важко додати нову функцію      ✓ Розширюваність в архітектурі   │
│  ✗ Hardcoded конфігурація         ✓ Конфігурація ззовні            │
│  ✗ Console.log дебаг              ✓ Структуроване логування        │
│  ✗ Ручний deploy                  ✓ Автоматизований CI/CD          │
│                                                                     │
│  Час до production: 6+ місяців    Готовий до production             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

Цей розділ показує, як **одразу будувати production-ready систему**, а не переробляти прототип.

---

## 52.1 ОГЛЯД ФІНАЛЬНОЇ СИСТЕМИ

### 52.1.1 Що ми будуємо

**Інтелектуальний автономний рій БПЛА** — система для:
- **Розвідки** території (пошук об'єктів, картографування)
- **Пошуково-рятувальних** операцій (знаходження людей)
- **Моніторингу** (спостереження за зоною)
- **Доставки** (координована транспортування)

### 52.1.2 Архітектура системи

```
┌─────────────────────────────────────────────────────────────────────┐
│                    АРХІТЕКТУРА СИСТЕМИ                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │              GROUND CONTROL STATION (GCS)                     │ │
│  │                                                               │ │
│  │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │ │
│  │   │  Mission    │  │  Real-time  │  │  Operator   │         │ │
│  │   │  Planning   │  │  Monitoring │  │  Interface  │         │ │
│  │   │             │  │             │  │  (WebAssembly)│        │ │
│  │   └─────────────┘  └─────────────┘  └─────────────┘         │ │
│  │                                                               │ │
│  └─────────────────────────────┬─────────────────────────────────┘ │
│                                │                                    │
│                                │ TCP (commands) / UDP (telemetry)   │
│                                ▼                                    │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │              SWARM COORDINATOR                                │ │
│  │                                                               │ │
│  │   • Task allocation (розподіл завдань)                       │ │
│  │   • Conflict resolution (вирішення конфліктів)               │ │
│  │   • Formation management (керування формацією)               │ │
│  │   • Emergency handling (обробка надзвичайних ситуацій)       │ │
│  │                                                               │ │
│  └─────────────────────────────┬─────────────────────────────────┘ │
│                                │                                    │
│            ┌───────────────────┼───────────────────┐               │
│            │                   │                   │               │
│            ▼                   ▼                   ▼               │
│  ┌─────────────────┐ ┌─────────────────┐ ┌─────────────────┐      │
│  │   UAV AGENT 1   │ │   UAV AGENT 2   │ │   UAV AGENT N   │      │
│  │                 │ │                 │ │                 │      │
│  │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │      │
│  │  │   BDI     │  │ │  │   BDI     │  │ │  │   BDI     │  │      │
│  │  │  Engine   │  │ │  │  Engine   │  │ │  │  Engine   │  │      │
│  │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │      │
│  │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │      │
│  │  │  Sensors  │  │ │  │  Sensors  │  │ │  │  Sensors  │  │      │
│  │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │      │
│  │  ┌───────────┐  │ │  ┌───────────┐  │ │  ┌───────────┐  │      │
│  │  │  Comms    │  │ │  │  Comms    │  │ │  │  Comms    │  │      │
│  │  └───────────┘  │ │  └───────────┘  │ │  └───────────┘  │      │
│  └─────────────────┘ └─────────────────┘ └─────────────────┘      │
│            │                   │                   │               │
│            └───────────────────┼───────────────────┘               │
│                                │ UDP (peer-to-peer telemetry)      │
│                                ▼                                    │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │              ENVIRONMENT (симуляція)                          │ │
│  │                                                               │ │
│  │   • Terrain, obstacles                                        │ │
│  │   • Weather conditions                                        │ │
│  │   • Targets, threats                                          │ │
│  │   • Physics simulation                                        │ │
│  │                                                               │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.1.3 Ієрархія рою

Відповідно до документу проєкту, рій має таку структуру:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ІЄРАРХІЯ РОЮ                                      │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                      ┌─────────────────┐                           │
│                      │  КОМАНДИР РОЮ   │                           │
│                      │  (Swarm Leader) │                           │
│                      └────────┬────────┘                           │
│                               │                                     │
│         ┌─────────────────────┼─────────────────────┐              │
│         │                     │                     │              │
│         ▼                     ▼                     ▼              │
│  ┌─────────────┐      ┌─────────────┐      ┌─────────────┐        │
│  │ КОМАНДИР    │      │ КОМАНДИР    │      │ КОМАНДИР    │        │
│  │ ГРУПИ А     │      │ ГРУПИ Б     │      │ ГРУПИ В     │        │
│  │(Розвідники) │      │(Штурмани)   │      │(Транспорт)  │        │
│  └──────┬──────┘      └──────┬──────┘      └──────┬──────┘        │
│         │                    │                    │                │
│    ┌────┼────┐          ┌────┼────┐          ┌────┼────┐          │
│    ▼    ▼    ▼          ▼    ▼    ▼          ▼    ▼    ▼          │
│   🛸   🛸   🛸        🛸   🛸   🛸        🛸   🛸   🛸          │
│  UAV  UAV  UAV       UAV  UAV  UAV       UAV  UAV  UAV           │
│                                                                     │
│  ТИПИ ГРУП:                                                        │
│  • Розвідники (Scouts) — пошук цілей                              │
│  • Штурманська (Navigation) — планування маршрутів                │
│  • Бойова (Combat) — ударні функції                               │
│  • РЕБ (Electronic Warfare) — глушіння, захист                    │
│  • Зв'язківці (Communications) — ретрансляція                     │
│  • Транспорт (Transport) — доставка, евакуація                    │
│  • Розпізнавачі (Recognizers) — класифікація об'єктів             │
│  • Радники (Advisors) — аналітика, моделювання                    │
│  • Центр рішень (Decision Center) — колективний аналіз            │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 52.2 ПРИНЦИПИ АРХІТЕКТУРИ

### 52.2.1 Separation of Concerns

Кожен модуль системи відповідає за **одну** чітко визначену задачу:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    SEPARATION OF CONCERNS                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  МОДУЛЬ              ВІДПОВІДАЛЬНІСТЬ                              │
│  ─────────────────────────────────────────────────────────────     │
│                                                                     │
│  agent::bdi          Beliefs, Desires, Intentions — логіка рішень  │
│  agent::state        Машина станів агента (Idle, Moving, etc.)     │
│  agent::sensors      Абстракція сенсорів (GPS, Camera, Radar)      │
│                                                                     │
│  comms::protocol     Формат повідомлень, серіалізація              │
│  comms::tcp          TCP з'єднання для команд                       │
│  comms::udp          UDP датаграми для телеметрії                   │
│  comms::discovery    Виявлення інших агентів                       │
│                                                                     │
│  world::environment  Симуляція середовища                          │
│  world::physics      Фізичні розрахунки                            │
│  world::entities     Об'єкти світу (targets, obstacles)            │
│                                                                     │
│  coordinator         Розподіл завдань, координація рою             │
│                                                                     │
│  visualization       WebAssembly візуалізація                      │
│                                                                     │
│  Кожен модуль НЕ ЗНАЄ про внутрішню реалізацію інших.             │
│  Взаємодія через публічні інтерфейси (traits).                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.2.2 Layered Architecture

Система організована в **шари** з чіткими залежностями:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ШАРИ АРХІТЕКТУРИ                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  PRESENTATION LAYER (UI/Visualization)                        │ │
│  │                                                               │ │
│  │  • WebAssembly frontend                                       │ │
│  │  • REST API endpoints                                         │ │
│  │  • CLI interface                                              │ │
│  │                                                               │ │
│  │  Залежить від: Application Layer                              │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                              │                                      │
│                              ▼                                      │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  APPLICATION LAYER (Use Cases)                                │ │
│  │                                                               │ │
│  │  • StartMission, AbortMission                                 │ │
│  │  • AssignTask, ReportStatus                                   │ │
│  │  • AddAgent, RemoveAgent                                      │ │
│  │                                                               │ │
│  │  Залежить від: Domain Layer                                   │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                              │                                      │
│                              ▼                                      │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  DOMAIN LAYER (Business Logic)                                │ │
│  │                                                               │ │
│  │  • Agent, Swarm, Mission                                      │ │
│  │  • BDI Engine, State Machine                                  │ │
│  │  • Path Planning algorithms                                   │ │
│  │                                                               │ │
│  │  Залежить від: Нічого (чисте ядро)                            │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                              │                                      │
│                              ▼                                      │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  INFRASTRUCTURE LAYER (Technical Details)                     │ │
│  │                                                               │ │
│  │  • TCP/UDP networking                                         │ │
│  │  • Serialization (serde, bincode)                             │ │
│  │  • Persistence (if needed)                                    │ │
│  │  • External APIs                                              │ │
│  │                                                               │ │
│  │  Імплементує інтерфейси з Domain Layer                        │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                     │
│  ПРАВИЛО: Залежності йдуть ТІЛЬКИ ВНИЗ, ніколи вгору.              │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.2.3 Dependency Inversion через Traits

Domain Layer не залежить від Infrastructure — це досягається через **traits**:

```rust
// Domain Layer — визначає ЩО потрібно
pub trait Communicator: Send + Sync {
    fn send(&self, agent_id: AgentId, message: &Message) -> Result<(), CommError>;
    fn broadcast(&self, message: &Message) -> Result<(), CommError>;
    fn receive(&self) -> Option<Message>;
}

pub trait SensorReader: Send + Sync {
    fn read_position(&self) -> Option<Position>;
    fn read_battery(&self) -> u8;
    fn scan_area(&self, radius: f64) -> Vec<Detection>;
}

// Infrastructure Layer — визначає ЯК
pub struct TcpCommunicator { /* ... */ }
pub struct UdpCommunicator { /* ... */ }
pub struct SimulatedSensor { /* ... */ }
pub struct RealGpsSensor { /* ... */ }

impl Communicator for TcpCommunicator { /* ... */ }
impl Communicator for UdpCommunicator { /* ... */ }
impl SensorReader for SimulatedSensor { /* ... */ }
impl SensorReader for RealGpsSensor { /* ... */ }

// Agent використовує traits, не конкретні імплементації
pub struct Agent<C: Communicator, S: SensorReader> {
    communicator: C,
    sensors: S,
    // ...
}
```

**Перевага:** Можемо легко підміняти імплементації для тестування або різних середовищ.

---

## 52.3 ЕВОЛЮЦІЯ АГЕНТА ЧЕРЕЗ ПІДРУЧНИК

### 52.3.1 Шлях агента

Пригадаймо, як еволюціонував наш агент протягом підручника:

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ЕВОЛЮЦІЯ АГЕНТА                                   │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ЧАСТИНА 0: Агент як алгоритм                                      │
│  ────────────────────────────                                      │
│  let x = 0;                                                        │
│  let y = 0;                                                        │
│  // Імперативний код без структур                                  │
│                                                                     │
│  ЧАСТИНА I: Агент як структура                                     │
│  ─────────────────────────────                                     │
│  struct Agent { x: i32, y: i32, battery: u8 }                     │
│  impl Agent { fn move_to(&mut self, ...) }                        │
│  // Ownership, borrowing, struct, enum                            │
│                                                                     │
│  ЧАСТИНА II: Агент з обробкою помилок                             │
│  ────────────────────────────────────                              │
│  fn move_to(&mut self) -> Result<(), AgentError>                  │
│  // Option, Result, власні типи помилок, serde                    │
│                                                                     │
│  ЧАСТИНА III: Агент як процес                                      │
│  ────────────────────────────                                      │
│  std::thread::spawn(|| { agent.run() });                          │
│  Arc<Mutex<SharedState>>                                           │
│  // Threads, Arc, Mutex, channels                                 │
│                                                                     │
│  ЧАСТИНА IV: Агент як актор                                        │
│  ──────────────────────────                                        │
│  tokio::spawn(async { agent.run().await });                       │
│  mpsc::Sender<Command>, mpsc::Receiver<Event>                     │
│  // Async/await, Tokio, Actor Model                               │
│                                                                     │
│  ЧАСТИНА V: Агент як BDI-система                                   │
│  ───────────────────────────────                                   │
│  struct BdiAgent { beliefs, desires, intentions }                 │
│  // BDI, ECS, мережева комунікація, WebAssembly                   │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.3.2 Фінальна структура агента

**Постановка задачі:** Об'єднаємо всі концепції в структуру фінального агента.

```rust
use tokio::sync::mpsc;
use std::sync::Arc;

/// Повноцінний агент БПЛА
pub struct UavAgent<C: Communicator, S: SensorReader> {
    // Ідентифікація
    pub id: AgentId,
    pub group: GroupType,
    pub role: AgentRole,
    
    // BDI компоненти (Розділ 49)
    beliefs: Beliefs,
    desires: Desires,
    current_intention: Option<Intention>,
    plan_library: PlanLibrary,
    
    // Машина станів (Розділ 12)
    state: AgentState,
    
    // Комунікація (Розділ 50)
    communicator: C,
    command_rx: mpsc::Receiver<Command>,
    event_tx: mpsc::Sender<AgentEvent>,
    
    // Сенсори (абстракція)
    sensors: S,
    
    // Конфігурація
    config: AgentConfig,
}

/// Стан агента (Розділ 12: enum + pattern matching)
#[derive(Debug, Clone, PartialEq)]
pub enum AgentState {
    Idle,
    Moving { target: Position, speed: f64 },
    Scanning { sector: SectorId },
    Returning { reason: ReturnReason },
    Emergency { details: String },
    Offline,
}

/// Тип групи агента
#[derive(Debug, Clone, Copy, PartialEq)]
pub enum GroupType {
    Scouts,
    Navigation,
    Combat,
    ElectronicWarfare,
    Communications,
    Transport,
    Recognizers,
    Advisors,
    DecisionCenter,
}

/// Роль в групі
#[derive(Debug, Clone, Copy, PartialEq)]
pub enum AgentRole {
    Leader,
    Member,
}
```

### 52.3.3 Головний цикл агента

**Постановка задачі:** Реалізуємо async run loop агента.

```rust
impl<C: Communicator, S: SensorReader> UavAgent<C, S> {
    /// Головний цикл агента (Actor pattern з Розділу 42)
    pub async fn run(&mut self) {
        loop {
            // 1. Сприйняття (Perception)
            self.perceive();
            
            // 2. Оновлення beliefs
            self.update_beliefs();
            
            // 3. Обробка вхідних повідомлень
            self.process_messages().await;
            
            // 4. BDI цикл (deliberation)
            self.deliberate();
            
            // 5. Виконання поточного intention
            self.execute().await;
            
            // 6. Надсилання телеметрії
            self.send_telemetry();
            
            // 7. Перевірка умов виходу
            if self.should_shutdown() {
                break;
            }
            
            // Затримка для наступної ітерації
            tokio::time::sleep(self.config.tick_interval).await;
        }
    }
    
    /// Сприйняття через сенсори
    fn perceive(&mut self) {
        // Читаємо дані з сенсорів (Розділ 23: traits)
        if let Some(pos) = self.sensors.read_position() {
            self.beliefs.my_position = pos;
        }
        self.beliefs.my_battery = self.sensors.read_battery();
        
        // Сканування області
        let detections = self.sensors.scan_area(self.config.scan_radius);
        for detection in detections {
            self.beliefs.add_detection(detection);
        }
    }
    
    /// Обробка вхідних команд
    async fn process_messages(&mut self) {
        // Неблокуюче отримання команд (Розділ 39: async channels)
        while let Ok(command) = self.command_rx.try_recv() {
            self.handle_command(command).await;
        }
    }
    
    /// BDI deliberation (Розділ 49)
    fn deliberate(&mut self) {
        // Генеруємо опції на основі beliefs
        self.generate_options();
        
        // Перевіряємо, чи потрібно переглянути intention
        if self.should_reconsider() {
            // Обираємо найпріоритетніший desire
            if let Some(desire) = self.select_desire() {
                // Знаходимо план
                if let Some(plan) = self.plan_library.find_plan(&desire, &self.beliefs) {
                    self.current_intention = Some(Intention::new(desire, plan));
                }
            }
        }
    }
}
```

**Розглянемо цей цикл:**

1. **`perceive()`** — читає дані з сенсорів, оновлює beliefs про позицію, батарею, виявлені об'єкти
2. **`update_beliefs()`** — логічні висновки, очищення застарілих даних
3. **`process_messages()`** — обробка команд від координатора та інших агентів
4. **`deliberate()`** — BDI цикл: генерація desires, вибір intention, планування
5. **`execute()`** — виконання поточного кроку плану
6. **`send_telemetry()`** — надсилання статусу координатору

---

## 52.4 ІНТЕГРАЦІЯ КОМПОНЕНТІВ

### 52.4.1 Як частини підручника з'єднуються

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ІНТЕГРАЦІЯ КОНЦЕПЦІЙ                              │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  КОНЦЕПЦІЯ             ДЕ В СИСТЕМІ                                │
│  ─────────────────────────────────────────────────────────────     │
│                                                                     │
│  Ownership (7-10)      Agent володіє своїми beliefs, desires       │
│                        Coordinator володіє списком agents          │
│                                                                     │
│  Structs (11)          Agent, Position, Beliefs, Plan              │
│                                                                     │
│  Enums (12)            AgentState, MessageType, GroupType          │
│                                                                     │
│  Traits (22-23)        Communicator, SensorReader, Agent trait     │
│                                                                     │
│  Generics (24-25)      Agent<C: Communicator, S: SensorReader>     │
│                                                                     │
│  Error Handling (18-20) AgentError, NetworkError, thiserror        │
│                                                                     │
│  Serde (26)            Message серіалізація для мережі             │
│                                                                     │
│  Threads (33-35)       Паралельні агенти, channels                 │
│                                                                     │
│  Async (37-42)         Tokio runtime, actor pattern                │
│                                                                     │
│  ECS (48)              Симуляція середовища                        │
│                                                                     │
│  BDI (49)              Логіка прийняття рішень                     │
│                                                                     │
│  Networking (50)       TCP/UDP комунікація                         │
│                                                                     │
│  WebAssembly (51)      Візуалізація в браузері                     │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.4.2 Граф залежностей крейтів

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ГРАФ ЗАЛЕЖНОСТЕЙ                                  │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                        swarm-visualization                          │
│                        (wasm-bindgen)                               │
│                              │                                      │
│                              ▼                                      │
│                        swarm-coordinator                           │
│                              │                                      │
│              ┌───────────────┼───────────────┐                     │
│              │               │               │                     │
│              ▼               ▼               ▼                     │
│      swarm-agent     swarm-comms     swarm-simulation              │
│              │               │               │                     │
│              └───────────────┼───────────────┘                     │
│                              │                                      │
│                              ▼                                      │
│                        swarm-core                                  │
│                    (types, traits, errors)                         │
│                                                                     │
│  swarm-core:        Спільні типи, traits, базові структури         │
│  swarm-agent:       Логіка агента, BDI                             │
│  swarm-comms:       TCP/UDP комунікація                            │
│  swarm-simulation:  Симуляція середовища                           │
│  swarm-coordinator: Координація рою                                │
│  swarm-visualization: WebAssembly frontend                         │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.4.3 Workspace структура

```toml
# Cargo.toml (workspace root)
[workspace]
members = [
    "crates/swarm-core",
    "crates/swarm-agent",
    "crates/swarm-comms",
    "crates/swarm-simulation",
    "crates/swarm-coordinator",
    "crates/swarm-visualization",
]
resolver = "2"

[workspace.dependencies]
tokio = { version = "1.35", features = ["full"] }
serde = { version = "1.0", features = ["derive"] }
bincode = "1.3"
tracing = "0.1"
thiserror = "1.0"
```

---

## 52.5 ТЕСТУВАННЯ

### 52.5.1 Піраміда тестування

```
┌─────────────────────────────────────────────────────────────────────┐
│                    ПІРАМІДА ТЕСТУВАННЯ                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│                          ▲                                          │
│                         ╱ ╲                                         │
│                        ╱   ╲                                        │
│                       ╱ E2E ╲        10% — повільні, крихкі        │
│                      ╱  tests╲       Повна система                  │
│                     ╱─────────╲                                     │
│                    ╱           ╲                                    │
│                   ╱ Integration ╲    20% — середні                  │
│                  ╱    tests      ╲   Взаємодія компонентів          │
│                 ╱─────────────────╲                                 │
│                ╱                   ╲                                │
│               ╱    Unit tests       ╲  70% — швидкі, надійні       │
│              ╱                       ╲ Ізольовані функції           │
│             ╱─────────────────────────╲                             │
│                                                                     │
│  ШВИДКІСТЬ:    Секунди → Хвилини → Десятки хвилин                  │
│  СТАБІЛЬНІСТЬ: Висока → Середня → Низька                           │
│  COVERAGE:     Деталі → Інтеграція → User scenarios                │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.5.2 Unit тести

**Постановка задачі:** Тестуємо ізольовані компоненти.

```rust
#[cfg(test)]
mod tests {
    use super::*;
    
    // Тест BDI deliberation
    #[test]
    fn test_low_battery_triggers_return_desire() {
        let mut agent = create_test_agent();
        
        // Встановлюємо низький заряд
        agent.beliefs.my_battery = 15;
        
        // Генеруємо опції
        agent.generate_options();
        
        // Перевіряємо, що з'явився desire на повернення
        let has_return_desire = agent.desires.iter()
            .any(|d| matches!(d.desire_type, DesireType::ReturnToBase));
        
        assert!(has_return_desire, "Low battery should trigger return desire");
    }
    
    // Тест машини станів
    #[test]
    fn test_state_transition_moving_to_idle() {
        let mut state = AgentState::Moving { 
            target: Position { x: 100.0, y: 100.0 },
            speed: 10.0,
        };
        
        // Симулюємо досягнення цілі
        let current_pos = Position { x: 100.0, y: 100.0 };
        
        if reached_target(&state, &current_pos) {
            state = AgentState::Idle;
        }
        
        assert_eq!(state, AgentState::Idle);
    }
    
    // Тест серіалізації повідомлень
    #[test]
    fn test_message_serialization_roundtrip() {
        let original = Message::PositionUpdate {
            agent_id: 42,
            position: Position { x: 1.5, y: 2.5 },
            timestamp: 12345,
        };
        
        // Серіалізуємо
        let bytes = bincode::serialize(&original).unwrap();
        
        // Десеріалізуємо
        let decoded: Message = bincode::deserialize(&bytes).unwrap();
        
        assert_eq!(original, decoded);
    }
}
```

### 52.5.3 Інтеграційні тести

**Постановка задачі:** Тестуємо взаємодію компонентів.

```rust
// tests/integration/agent_communication.rs

#[tokio::test]
async fn test_agent_receives_command_from_coordinator() {
    // Створюємо канали
    let (cmd_tx, cmd_rx) = mpsc::channel(100);
    let (event_tx, mut event_rx) = mpsc::channel(100);
    
    // Створюємо агента з mock сенсорами
    let mut agent = UavAgent::new(
        AgentId(1),
        MockCommunicator::new(),
        MockSensor::new(),
        cmd_rx,
        event_tx,
    );
    
    // Запускаємо агента в окремій задачі
    let agent_handle = tokio::spawn(async move {
        agent.run().await;
    });
    
    // Надсилаємо команду
    cmd_tx.send(Command::MoveTo { 
        target: Position { x: 100.0, y: 100.0 }
    }).await.unwrap();
    
    // Очікуємо подію про початок руху
    let event = tokio::time::timeout(
        Duration::from_secs(1),
        event_rx.recv()
    ).await.unwrap().unwrap();
    
    assert!(matches!(event, AgentEvent::StateChanged { 
        new_state: AgentState::Moving { .. }
    }));
    
    // Завершуємо
    cmd_tx.send(Command::Shutdown).await.unwrap();
    agent_handle.await.unwrap();
}
```

### 52.5.4 Property-based тести

```rust
use proptest::prelude::*;

proptest! {
    /// Перевіряємо, що серіалізація завжди reversible
    #[test]
    fn position_serialization_roundtrip(x in -1000.0f64..1000.0, y in -1000.0f64..1000.0) {
        let pos = Position { x, y };
        let bytes = bincode::serialize(&pos).unwrap();
        let decoded: Position = bincode::deserialize(&bytes).unwrap();
        
        prop_assert!((pos.x - decoded.x).abs() < f64::EPSILON);
        prop_assert!((pos.y - decoded.y).abs() < f64::EPSILON);
    }
    
    /// Інваріант: батарея ніколи не стає від'ємною
    #[test]
    fn battery_never_negative(drain_amounts in prop::collection::vec(0u8..100, 1..20)) {
        let mut battery = 100u8;
        
        for drain in drain_amounts {
            battery = battery.saturating_sub(drain);
        }
        
        prop_assert!(battery <= 100);  // Завжди в межах 0-100
    }
}
```

---

## 52.6 ДОКУМЕНТАЦІЯ

### 52.6.1 Рівні документації

```
┌─────────────────────────────────────────────────────────────────────┐
│                    РІВНІ ДОКУМЕНТАЦІЇ                                │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  1. API DOCUMENTATION (rustdoc)                                    │
│     • Документація кожної публічної функції/структури              │
│     • Приклади використання                                        │
│     • cargo doc --open                                             │
│                                                                     │
│  2. MODULE DOCUMENTATION                                           │
│     • //! коментарі на початку модуля                              │
│     • Огляд призначення модуля                                     │
│     • Приклади типового використання                               │
│                                                                     │
│  3. CRATE DOCUMENTATION                                            │
│     • README.md                                                    │
│     • Огляд архітектури                                            │
│     • Quick start guide                                            │
│                                                                     │
│  4. PROJECT DOCUMENTATION                                          │
│     • Architecture Decision Records (ADRs)                         │
│     • API specification                                            │
│     • Deployment guide                                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.6.2 Приклад rustdoc

```rust
//! # Swarm Agent
//! 
//! Цей модуль містить реалізацію автономного агента БПЛА.
//! 
//! ## Архітектура
//! 
//! ```text
//! ┌─────────────────────────────────────────┐
//! │              UavAgent                    │
//! │  ┌───────────┐  ┌───────────────────┐  │
//! │  │    BDI    │  │   State Machine   │  │
//! │  │  Engine   │  │                   │  │
//! │  └───────────┘  └───────────────────┘  │
//! │  ┌───────────┐  ┌───────────────────┐  │
//! │  │  Sensors  │  │   Communicator    │  │
//! │  └───────────┘  └───────────────────┘  │
//! └─────────────────────────────────────────┘
//! ```
//! 
//! ## Приклад
//! 
//! ```rust
//! use swarm_agent::{UavAgent, AgentConfig};
//! 
//! let config = AgentConfig::default();
//! let agent = UavAgent::new(AgentId(1), config);
//! ```

/// Автономний агент БПЛА з BDI архітектурою.
///
/// # Приклад
///
/// ```rust
/// use swarm_agent::UavAgent;
///
/// let agent = UavAgent::builder()
///     .id(AgentId(1))
///     .group(GroupType::Scouts)
///     .build();
/// ```
///
/// # Errors
///
/// Повертає `AgentError` якщо конфігурація невалідна.
pub struct UavAgent<C, S> {
    // ...
}
```

---

## 52.7 CI/CD PIPELINE

### 52.7.1 GitHub Actions

```yaml
# .github/workflows/ci.yml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      
      - name: cargo fmt
        run: cargo fmt --all -- --check
      
      - name: cargo clippy
        run: cargo clippy --all-targets --all-features -- -D warnings
      
      - name: cargo test
        run: cargo test --all-features
      
      - name: cargo doc
        run: cargo doc --no-deps --all-features

  wasm:
    name: WebAssembly
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      
      - name: Install wasm-pack
        run: cargo install wasm-pack
      
      - name: Build WASM
        run: wasm-pack build --target web crates/swarm-visualization

  coverage:
    name: Code Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      
      - name: Install cargo-tarpaulin
        run: cargo install cargo-tarpaulin
      
      - name: Generate coverage
        run: cargo tarpaulin --out xml
      
      - name: Upload to Codecov
        uses: codecov/codecov-action@v3
```

---

## 52.8 СЦЕНАРІЙ: РОЗВІДУВАЛЬНА МІСІЯ

### 52.8.1 Опис місії

```
┌─────────────────────────────────────────────────────────────────────┐
│                    СЦЕНАРІЙ: ПОШУКОВА МІСІЯ                          │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ЗАДАЧА: Знайти зниклого туриста в лісовій зоні 5x5 км             │
│                                                                     │
│  РЕСУРСИ:                                                          │
│  • 8 БПЛА-розвідників                                              │
│  • 2 БПЛА-ретранслятори                                            │
│  • 1 координатор                                                   │
│                                                                     │
│  ФАЗИ МІСІЇ:                                                       │
│                                                                     │
│  1. DEPLOYMENT (5 хв)                                              │
│     • Запуск дронів                                                │
│     • Формування зв'язку                                           │
│     • Розподіл секторів                                            │
│                                                                     │
│  2. SEARCH (60 хв)                                                 │
│     • Систематичне сканування                                      │
│     • Обмін інформацією                                            │
│     • Адаптація при виявленні                                      │
│                                                                     │
│  3. INVESTIGATION (15 хв)                                          │
│     • Детальне обстеження точки інтересу                          │
│     • Підтвердження знахідки                                       │
│     • Маркування позиції                                           │
│                                                                     │
│  4. RETURN (10 хв)                                                 │
│     • Повернення на базу                                           │
│     • Завантаження даних                                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 52.8.2 Розподіл зони пошуку

```
┌─────────────────────────────────────────────────────────────────────┐
│                    КАРТА ЗОНИ ПОШУКУ                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│    0km      1km      2km      3km      4km      5km                │
│     │────────│────────│────────│────────│────────│                 │
│  5km┌────────┬────────┬────────┬────────┐                          │
│     │   A    │   B    │   C    │   D    │                          │
│     │  🛸1   │  🛸2   │  🛸3   │  🛸4   │                          │
│  4km├────────┼────────┼────────┼────────┤                          │
│     │   E    │   F    │   G    │   H    │                          │
│     │  🛸5   │  🛸6   │  🛸7   │  🛸8   │                          │
│  3km├────────┴────────┴────────┴────────┤                          │
│     │                                    │                          │
│     │        🛸9 (Ретранслятор)         │                          │
│  2km│                                    │                          │
│     │        🛸10 (Ретранслятор)        │                          │
│     │                                    │                          │
│  1km│                                    │                          │
│     │                                    │                          │
│  0km└────────────────────────────────────┘                          │
│                     ▲                                               │
│                     │                                               │
│                 🏠 БАЗА                                             │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

---

## 52.9 ЛАБОРАТОРНА РОБОТА

### Мета роботи

Інтегрувати компоненти в працюючу систему.

### Завдання 1: Core types (3 бали)

Створіть `swarm-core` крейт з:
- Position, Velocity, AgentId
- Message enum з серіалізацією
- AgentError з thiserror

### Завдання 2: Agent implementation (4 бали)

Реалізуйте базового агента:
- Машина станів (Idle, Moving, Scanning)
- Обробка команд через channel
- Тести для переходів станів

### Завдання 3: Coordinator (3 бали)

Створіть координатора:
- Розподіл агентів по секторах
- Broadcast команд
- Збір статусів

---

## 52.10 РЕЗЮМЕ

### Ключові принципи

**Separation of Concerns** — кожен модуль робить одну річ добре.

**Layered Architecture** — чіткі залежності між шарами.

**Dependency Inversion** — залежність від абстракцій (traits), не конкретних типів.

**Test Pyramid** — багато unit, менше integration, ще менше e2e.

### Checklist готовності до production

- [ ] Код компілюється без warnings
- [ ] Clippy не має скарг
- [ ] Test coverage > 70%
- [ ] Документація для всіх публічних API
- [ ] CI pipeline налаштований
- [ ] Логування структуроване (tracing)
- [ ] Помилки обробляються gracefully
- [ ] Конфігурація винесена назовні

---

## 🔗 ЗВ'ЯЗОК З НАСТУПНИМ РОЗДІЛОМ

Ви побудували повноцінну систему. Але екосистема Rust величезна, і є багато напрямків для подальшого розвитку.

У **Розділі 53: Екосистема та подальші напрямки** ви:
- Дізнаєтесь про крейти для робототехніки та AI
- Побачите тренди розвитку Rust
- Отримаєте ресурси для подальшого навчання

---

> **Наступний розділ:** [Розділ 53: Екосистема та подальші напрямки](./53_Ecosystem.md)
