# Практичне заняття 4: Умовні оператори

## Мета заняття

Після цього заняття ви зможете:
- Використовувати конструкції `if`, `else`, `else if`
- Застосовувати `if` як вираз для присвоєння значень
- Комбінувати умови за допомогою логічних операторів
- Порівнювати значення різними способами
- Використовувати базовий `match` для вибору з варіантів

---

## Теоретичний вступ

### Навіщо потрібні умовні оператори?

Програми рідко виконуються лінійно від початку до кінця. Зазвичай потрібно приймати рішення: якщо батарея низька — повертатися на базу, якщо виявлено перешкоду — змінити курс, якщо отримано команду — виконати дію.

Умовні оператори дозволяють програмі "розгалужуватися" — виконувати різний код залежно від умов.

### Базовий if

Найпростіша форма умовного оператора:

```rust
fn main() {
    let battery = 15;
    
    if battery < 20 {
        println!("Увага! Низький заряд батареї!");
    }
}
```

**Синтаксис:**
- `if` — ключове слово
- `battery < 20` — умова (має бути типу `bool`)
- `{ }` — блок коду, що виконується, якщо умова `true`

**Важливо:** У Rust умова **обов'язково** має бути булевого типу. На відміну від деяких мов, числа не конвертуються в `bool` автоматично:

```rust
let x = 5;
// if x { }  // ПОМИЛКА! x — це i32, не bool
if x != 0 { }  // Правильно: явне порівняння
```

### if-else

Додаємо альтернативну гілку:

```rust
fn main() {
    let battery = 75;
    
    if battery < 20 {
        println!("Низький заряд!");
    } else {
        println!("Заряд достатній");
    }
}
```

`else` виконується, коли умова `if` — `false`.

### if-else if-else

Для кількох умов використовуємо ланцюжок:

```rust
fn main() {
    let battery = 55;
    
    if battery < 20 {
        println!("Критичний рівень!");
    } else if battery < 50 {
        println!("Низький рівень");
    } else if battery < 80 {
        println!("Середній рівень");
    } else {
        println!("Високий рівень");
    }
}
```

**Порядок важливий!** Умови перевіряються зверху вниз. Виконується перший блок, умова якого `true`. Решта ігноруються.

### Оператори порівняння

| Оператор | Значення | Приклад |
|----------|----------|---------|
| `==` | Дорівнює | `x == 5` |
| `!=` | Не дорівнює | `x != 0` |
| `<` | Менше | `x < 10` |
| `>` | Більше | `x > 0` |
| `<=` | Менше або дорівнює | `x <= 100` |
| `>=` | Більше або дорівнює | `x >= 1` |

```rust
fn main() {
    let a = 5;
    let b = 10;
    
    println!("a == b: {}", a == b);  // false
    println!("a != b: {}", a != b);  // true
    println!("a < b:  {}", a < b);   // true
    println!("a > b:  {}", a > b);   // false
    println!("a <= 5: {}", a <= 5);  // true
    println!("b >= 10:{}", b >= 10); // true
}
```

### Логічні оператори

| Оператор | Назва | Опис |
|----------|-------|------|
| `&&` | AND (І) | `true`, якщо обидві умови `true` |
| `\|\|` | OR (АБО) | `true`, якщо хоча б одна умова `true` |
| `!` | NOT (НЕ) | Інвертує значення |

```rust
fn main() {
    let battery = 50;
    let has_gps = true;
    let has_connection = false;
    
    // AND: обидві умови мають бути true
    if battery >= 20 && has_gps {
        println!("Можна летіти (батарея ОК і GPS є)");
    }
    
    // OR: достатньо однієї true
    if !has_connection || battery < 10 {
        println!("Потрібно повернутися на базу");
    }
    
    // NOT: інверсія
    if !has_connection {
        println!("Немає зв'язку!");
    }
}
```

### Таблиці істинності

**AND (&&):**
| A | B | A && B |
|---|---|--------|
| true | true | true |
| true | false | false |
| false | true | false |
| false | false | false |

**OR (||):**
| A | B | A \|\| B |
|---|---|----------|
| true | true | true |
| true | false | true |
| false | true | true |
| false | false | false |

**NOT (!):**
| A | !A |
|---|----|
| true | false |
| false | true |

### Коротке замикання (Short-circuit evaluation)

Логічні оператори в Rust "ліниві":
- `&&` — якщо перша умова `false`, друга не обчислюється
- `||` — якщо перша умова `true`, друга не обчислюється

```rust
fn check_something() -> bool {
    println!("Функція викликана!");
    true
}

fn main() {
    let x = false;
    
    // check_something() НЕ буде викликана, бо x вже false
    if x && check_something() {
        println!("Обидві true");
    }
    
    let y = true;
    
    // check_something() НЕ буде викликана, бо y вже true
    if y || check_something() {
        println!("Хоча б одна true");
    }
}
```

### if як вираз

У Rust `if` — це вираз, що повертає значення! Це дуже потужна особливість:

```rust
fn main() {
    let battery = 75;
    
    let status = if battery >= 50 {
        "OK"
    } else {
        "Low"
    };
    
    println!("Статус: {}", status);
}
```

**Важливо:**
1. Обидві гілки мають повертати значення **одного типу**
2. Немає `;` після значень всередині гілок
3. `;` ставиться після всього виразу `if-else`

**Приклад з числами:**

```rust
fn main() {
    let speed = 25;
    
    let adjusted_speed = if speed > 30 {
        30  // Обмежуємо максимум
    } else if speed < 5 {
        5   // Обмежуємо мінімум
    } else {
        speed  // Залишаємо як є
    };
    
    println!("Швидкість: {} → {}", speed, adjusted_speed);
}
```

### Порівняння з тернарним оператором

У мовах як C/Java є тернарний оператор: `condition ? value1 : value2`

У Rust замість нього використовують `if-else`:

```rust
// C/Java: int max = a > b ? a : b;
// Rust:
let max = if a > b { a } else { b };
```

### Вступ до match

`match` — потужна конструкція для порівняння значення з патернами:

```rust
fn main() {
    let command = 'W';
    
    match command {
        'W' => println!("Рух вперед"),
        'S' => println!("Рух назад"),
        'A' => println!("Поворот ліворуч"),
        'D' => println!("Поворот праворуч"),
        _ => println!("Невідома команда"),
    }
}
```

**Синтаксис:**
- `match значення { }` — початок
- `патерн => вираз,` — гілка (arm)
- `_` — "будь-що інше" (wildcard)

**Важливо:** `match` має бути **вичерпним** — покривати всі можливі значення. `_` зазвичай використовується як "решта".

### match з діапазонами

```rust
fn main() {
    let battery = 45;
    
    let status = match battery {
        0..=19 => "Критичний",
        20..=49 => "Низький",
        50..=79 => "Середній",
        80..=100 => "Високий",
        _ => "Невалідне значення",
    };
    
    println!("Батарея {}%: {}", battery, status);
}
```

`0..=19` означає діапазон від 0 до 19 включно.

### match як вираз

`match` теж повертає значення:

```rust
fn main() {
    let code = 2;
    
    let message = match code {
        0 => "Успіх",
        1 => "Попередження",
        2 => "Помилка",
        _ => "Невідомий код",
    };
    
    println!("Код {}: {}", code, message);
}
```

---

## Практичні задачі

### Задача 1: Рівень батареї

**Умова:** Напишіть функцію `get_battery_level`, яка приймає відсоток заряду батареї та повертає текстовий опис рівня:
- 0-19%: "CRITICAL"
- 20-49%: "LOW"  
- 50-79%: "NORMAL"
- 80-100%: "FULL"

Протестуйте функцію на різних значеннях.

**Розв'язання:**

```rust
fn get_battery_level(percent: i32) -> &'static str {
    if percent < 20 {
        "CRITICAL"
    } else if percent < 50 {
        "LOW"
    } else if percent < 80 {
        "NORMAL"
    } else {
        "FULL"
    }
}

fn main() {
    println!("=== Тестування рівня батареї ===\n");
    
    let test_values = [0, 10, 19, 20, 49, 50, 79, 80, 99, 100];
    
    for percent in test_values {
        let level = get_battery_level(percent);
        println!("{}% → {}", percent, level);
    }
}
```

**Пояснення:**

1. Функція повертає `&'static str` — рядковий літерал
2. Умови перевіряються по порядку: спочатку `< 20`, потім `< 50` і т.д.
3. Якщо `percent >= 80`, виконується `else` — це "FULL"
4. Кожна гілка — вираз без `;`, тому значення повертається автоматично

**Чому порядок важливий?**

Якби ми написали `if percent < 80` першим, то значення 15 потрапило б у цю гілку, хоча воно "CRITICAL".

**Результат:**
```
=== Тестування рівня батареї ===

0% → CRITICAL
10% → CRITICAL
19% → CRITICAL
20% → LOW
49% → LOW
50% → NORMAL
79% → NORMAL
80% → FULL
99% → FULL
100% → FULL
```

---

### Задача 2: Напрямок руху

**Умова:** Дрон знаходиться в точці (x, y) і має досягти цілі (target_x, target_y). Напишіть функцію `get_direction`, яка визначає напрямок руху:
- "NORTH" — якщо потрібно рухатись вгору (y збільшується)
- "SOUTH" — якщо потрібно рухатись вниз (y зменшується)
- "EAST" — якщо потрібно рухатись вправо (x збільшується)
- "WEST" — якщо потрібно рухатись вліво (x зменшується)
- "ARRIVED" — якщо вже на місці

Спростіть: визначайте лише основний напрямок (більша різниця).

**Розв'язання:**

```rust
fn get_direction(x: i32, y: i32, target_x: i32, target_y: i32) -> &'static str {
    let dx = target_x - x;  // Різниця по X
    let dy = target_y - y;  // Різниця по Y
    
    // Якщо вже на місці
    if dx == 0 && dy == 0 {
        return "ARRIVED";
    }
    
    // Визначаємо основний напрямок (де більша різниця)
    if dx.abs() >= dy.abs() {
        // Рух по горизонталі
        if dx > 0 {
            "EAST"
        } else {
            "WEST"
        }
    } else {
        // Рух по вертикалі
        if dy > 0 {
            "NORTH"
        } else {
            "SOUTH"
        }
    }
}

fn main() {
    println!("=== Визначення напрямку руху ===\n");
    
    // Дрон у центрі (0, 0)
    let drone_x = 0;
    let drone_y = 0;
    
    // Різні цілі
    let targets = [
        (10, 3, "На схід"),
        (-5, 2, "На захід"),
        (2, 15, "На північ"),
        (1, -8, "На південь"),
        (0, 0, "На місці"),
    ];
    
    println!("Дрон знаходиться в ({}, {})\n", drone_x, drone_y);
    
    for (tx, ty, description) in targets {
        let direction = get_direction(drone_x, drone_y, tx, ty);
        println!("Ціль ({:>3}, {:>3}) {}: {}", tx, ty, description, direction);
    }
}
```

**Пояснення:**

1. `dx` і `dy` — різниці координат (вектор до цілі)
2. `.abs()` — модуль (абсолютне значення) числа
3. `dx.abs() >= dy.abs()` — перевіряємо, яка різниця більша
4. `return "ARRIVED"` — ранній вихід, якщо вже на місці
5. Вкладені `if` визначають конкретний напрямок

**Результат:**
```
=== Визначення напрямку руху ===

Дрон знаходиться в (0, 0)

Ціль ( 10,   3) На схід: EAST
Ціль ( -5,   2) На захід: WEST
Ціль (  2,  15) На північ: NORTH
Ціль (  1,  -8) На південь: SOUTH
Ціль (  0,   0) На місці: ARRIVED
```

---

### Задача 3: if як вираз

**Умова:** Напишіть функцію `get_flight_mode`, яка приймає швидкість вітру (м/с) та видимість (метри) і повертає режим польоту:
- "NORMAL" — вітер < 10 м/с І видимість > 1000 м
- "CAREFUL" — вітер < 15 м/с І видимість > 500 м
- "GROUNDED" — інакше

Використайте `if` як вираз для присвоєння результату.

**Розв'язання:**

```rust
fn get_flight_mode(wind_speed: f64, visibility: f64) -> &'static str {
    let mode = if wind_speed < 10.0 && visibility > 1000.0 {
        "NORMAL"
    } else if wind_speed < 15.0 && visibility > 500.0 {
        "CAREFUL"
    } else {
        "GROUNDED"
    };
    
    mode  // Повертаємо значення
}

fn main() {
    println!("=== Визначення режиму польоту ===\n");
    println!("{:<12} {:<15} {:<10}", "Вітер(м/с)", "Видимість(м)", "Режим");
    println!("{}", "-".repeat(40));
    
    // Тестові сценарії
    let scenarios = [
        (5.0, 2000.0),   // Ідеальні умови
        (8.0, 1500.0),   // Хороші умови
        (12.0, 800.0),   // Обережний режим
        (9.0, 600.0),    // Обережний (видимість на межі)
        (16.0, 2000.0),  // Сильний вітер
        (5.0, 400.0),    // Погана видимість
        (20.0, 200.0),   // Погані умови
    ];
    
    for (wind, vis) in scenarios {
        let mode = get_flight_mode(wind, vis);
        println!("{:<12} {:<15} {:<10}", wind, vis, mode);
    }
}
```

**Пояснення:**

1. `if wind_speed < 10.0 && visibility > 1000.0` — комбінація двох умов через AND
2. Весь `if-else if-else` — це вираз, результат присвоюється змінній `mode`
3. `mode` в кінці функції — це повернення значення
4. `{:<12}` — форматування: вирівнювання ліворуч, ширина 12 символів
5. `"-".repeat(40)` — створює рядок з 40 дефісів

**Результат:**
```
=== Визначення режиму польоту ===

Вітер(м/с)   Видимість(м)    Режим     
----------------------------------------
5            2000            NORMAL    
8            1500            NORMAL    
12           800             CAREFUL   
9            600             CAREFUL   
16           2000            GROUNDED  
5            400             GROUNDED  
20           200             GROUNDED  
```

---

### Задача 4: Перевірка дозволу на зліт

**Умова:** Напишіть функцію `can_take_off`, яка перевіряє, чи може дрон злетіти. Умови для зльоту (ВСІ мають виконуватись):
- Батарея >= 30%
- GPS активний (true)
- Немає критичних помилок (false)
- Вітер < 12 м/с

Функція повертає `true`, якщо всі умови виконані.

**Розв'язання:**

```rust
fn can_take_off(battery: f64, has_gps: bool, has_errors: bool, wind_speed: f64) -> bool {
    // Всі умови мають виконуватись
    battery >= 30.0 && has_gps && !has_errors && wind_speed < 12.0
}

fn print_check_result(name: &str, battery: f64, gps: bool, errors: bool, wind: f64) {
    let result = can_take_off(battery, gps, errors, wind);
    let status = if result { "✓ ДОЗВОЛЕНО" } else { "✗ ЗАБОРОНЕНО" };
    
    println!("\n{}", name);
    println!("  Батарея: {}%", battery);
    println!("  GPS: {}", if gps { "активний" } else { "неактивний" });
    println!("  Помилки: {}", if errors { "є" } else { "немає" });
    println!("  Вітер: {} м/с", wind);
    println!("  Результат: {}", status);
}

fn main() {
    println!("=== Перевірка дозволу на зліт ===");
    
    // Ідеальні умови
    print_check_result("Дрон Alpha", 85.0, true, false, 5.0);
    
    // Низька батарея
    print_check_result("Дрон Beta", 25.0, true, false, 5.0);
    
    // Немає GPS
    print_check_result("Дрон Gamma", 90.0, false, false, 3.0);
    
    // Є помилки
    print_check_result("Дрон Delta", 100.0, true, true, 2.0);
    
    // Сильний вітер
    print_check_result("Дрон Epsilon", 75.0, true, false, 15.0);
    
    // Кілька проблем
    print_check_result("Дрон Zeta", 20.0, false, true, 20.0);
}
```

**Пояснення:**

1. `battery >= 30.0 && has_gps && !has_errors && wind_speed < 12.0` — всі умови через AND
2. `!has_errors` — інверсія: `true` коли помилок немає
3. `if result { "✓ ДОЗВОЛЕНО" } else { "✗ ЗАБОРОНЕНО" }` — if як вираз для вибору рядка
4. Допоміжна функція `print_check_result` робить код чистішим

**Результат:**
```
=== Перевірка дозволу на зліт ===

Дрон Alpha
  Батарея: 85%
  GPS: активний
  Помилки: немає
  Вітер: 5 м/с
  Результат: ✓ ДОЗВОЛЕНО

Дрон Beta
  Батарея: 25%
  GPS: активний
  Помилки: немає
  Вітер: 5 м/с
  Результат: ✗ ЗАБОРОНЕНО

Дрон Gamma
  Батарея: 90%
  GPS: неактивний
  Помилки: немає
  Вітер: 3 м/с
  Результат: ✗ ЗАБОРОНЕНО

Дрон Delta
  Батарея: 100%
  GPS: активний
  Помилки: є
  Вітер: 2 м/с
  Результат: ✗ ЗАБОРОНЕНО

Дрон Epsilon
  Батарея: 75%
  GPS: активний
  Помилки: немає
  Вітер: 15 м/с
  Результат: ✗ ЗАБОРОНЕНО

Дрон Zeta
  Батарея: 20%
  GPS: неактивний
  Помилки: є
  Вітер: 20 м/с
  Результат: ✗ ЗАБОРОНЕНО
```

---

## Додаткові приклади

### Вкладені if

```rust
fn classify_altitude(altitude: i32, is_indoor: bool) -> &'static str {
    if is_indoor {
        if altitude > 10 {
            "Занадто високо для приміщення!"
        } else if altitude > 5 {
            "Обережно: близько до стелі"
        } else {
            "Безпечна висота"
        }
    } else {
        if altitude > 500 {
            "Занадто високо!"
        } else if altitude > 120 {
            "Потрібен дозвіл"
        } else {
            "Нормальна висота"
        }
    }
}
```

### match з кількома значеннями

```rust
fn is_weekend(day: &str) -> bool {
    match day {
        "Saturday" | "Sunday" => true,  // АБО
        _ => false,
    }
}

fn main() {
    println!("Monday: {}", is_weekend("Monday"));     // false
    println!("Saturday: {}", is_weekend("Saturday")); // true
}
```

### match з guard (додатковою умовою)

```rust
fn describe_number(n: i32) -> &'static str {
    match n {
        0 => "нуль",
        n if n < 0 => "від'ємне",
        n if n > 100 => "велике додатне",
        _ => "звичайне додатне",
    }
}
```

---

## Типові помилки та їх виправлення

### Помилка 1: Умова не булевого типу

```rust
fn main() {
    let x = 5;
    if x {  // ПОМИЛКА! x — це i32
        println!("True");
    }
}
```

**Повідомлення:**
```
error[E0308]: mismatched types
  expected `bool`, found `i32`
```

**Виправлення:** Використайте порівняння: `if x != 0 { }`

### Помилка 2: Різні типи в гілках if

```rust
fn main() {
    let x = 5;
    let result = if x > 0 {
        "positive"  // &str
    } else {
        -1          // i32 — ПОМИЛКА!
    };
}
```

**Повідомлення:**
```
error[E0308]: `if` and `else` have incompatible types
```

**Виправлення:** Обидві гілки мають повертати однаковий тип.

### Помилка 3: Відсутній else при присвоєнні

```rust
fn main() {
    let x = 5;
    let result = if x > 0 {
        "positive"
    };  // ПОМИЛКА! Немає else
}
```

**Повідомлення:**
```
error[E0317]: `if` may be missing an `else` clause
```

**Виправлення:** Додайте `else` з значенням того ж типу.

### Помилка 4: Присвоєння замість порівняння

```rust
fn main() {
    let x = 5;
    if x = 10 {  // ПОМИЛКА! = замість ==
        println!("Ten");
    }
}
```

**Виправлення:** Використайте `==` для порівняння: `if x == 10 { }`

### Помилка 5: Невичерпний match

```rust
fn main() {
    let n: i32 = 5;
    let result = match n {
        1 => "one",
        2 => "two",
        // ПОМИЛКА! Не покриті всі можливі i32
    };
}
```

**Виправлення:** Додайте `_ => "other"` для покриття решти випадків.

---

## Домашнє завдання

### Завдання 1: Категоризація швидкості

**Умова:** Напишіть функцію `categorize_speed`, яка приймає швидкість в м/с та повертає категорію:
- 0: "STOPPED"
- 1-5: "SLOW"
- 6-15: "NORMAL"
- 16-25: "FAST"
- >25: "MAXIMUM"

Використайте `match` з діапазонами.

**Розв'язання:**

```rust
fn categorize_speed(speed: i32) -> &'static str {
    match speed {
        0 => "STOPPED",
        1..=5 => "SLOW",
        6..=15 => "NORMAL",
        16..=25 => "FAST",
        _ => "MAXIMUM",
    }
}

fn main() {
    println!("=== Категоризація швидкості ===\n");
    
    let speeds = [0, 1, 5, 6, 10, 15, 16, 20, 25, 26, 30, 50];
    
    println!("{:>10} | {:<10}", "Швидкість", "Категорія");
    println!("{}", "-".repeat(25));
    
    for speed in speeds {
        let category = categorize_speed(speed);
        println!("{:>7} м/с | {:<10}", speed, category);
    }
}
```

**Пояснення:**

1. `match speed { }` — порівнюємо швидкість з патернами
2. `0 => "STOPPED"` — точне значення
3. `1..=5` — діапазон від 1 до 5 включно
4. `_ => "MAXIMUM"` — все, що більше 25

**Результат:**
```
=== Категоризація швидкості ===

 Швидкість | Категорія 
-------------------------
      0 м/с | STOPPED   
      1 м/с | SLOW      
      5 м/с | SLOW      
      6 м/с | NORMAL    
     10 м/с | NORMAL    
     15 м/с | NORMAL    
     16 м/с | FAST      
     20 м/с | FAST      
     25 м/с | FAST      
     26 м/с | MAXIMUM   
     30 м/с | MAXIMUM   
     50 м/с | MAXIMUM   
```

---

### Завдання 2: Валідація координат

**Умова:** Зона польоту обмежена прямокутником:
- X: від -100 до 100
- Y: від -100 до 100

Напишіть функцію `is_valid_position`, яка перевіряє, чи координати в межах зони. Напишіть також функцію `validate_position`, яка повертає текстовий опис проблеми або "OK".

**Розв'язання:**

```rust
const MIN_X: i32 = -100;
const MAX_X: i32 = 100;
const MIN_Y: i32 = -100;
const MAX_Y: i32 = 100;

fn is_valid_position(x: i32, y: i32) -> bool {
    x >= MIN_X && x <= MAX_X && y >= MIN_Y && y <= MAX_Y
}

fn validate_position(x: i32, y: i32) -> String {
    let mut problems = Vec::new();
    
    if x < MIN_X {
        problems.push(format!("X занадто мале ({} < {})", x, MIN_X));
    }
    if x > MAX_X {
        problems.push(format!("X занадто велике ({} > {})", x, MAX_X));
    }
    if y < MIN_Y {
        problems.push(format!("Y занадто мале ({} < {})", y, MIN_Y));
    }
    if y > MAX_Y {
        problems.push(format!("Y занадто велике ({} > {})", y, MAX_Y));
    }
    
    if problems.is_empty() {
        String::from("OK")
    } else {
        problems.join(", ")
    }
}

fn main() {
    println!("=== Валідація координат ===");
    println!("Зона польоту: X[{}, {}], Y[{}, {}]\n", MIN_X, MAX_X, MIN_Y, MAX_Y);
    
    let positions = [
        (0, 0),
        (50, 50),
        (100, 100),
        (-100, -100),
        (150, 50),
        (50, -150),
        (200, 200),
    ];
    
    for (x, y) in positions {
        let valid = is_valid_position(x, y);
        let validation = validate_position(x, y);
        let symbol = if valid { "✓" } else { "✗" };
        
        println!("({:>4}, {:>4}): {} {}", x, y, symbol, validation);
    }
}
```

**Пояснення:**

1. Константи визначають межі зони
2. `is_valid_position` — проста перевірка через AND
3. `validate_position` — детальна діагностика кожної проблеми
4. `Vec::new()` — динамічний масив для збору проблем (вивчимо детальніше пізніше)
5. `problems.join(", ")` — об'єднує елементи через кому

**Результат:**
```
=== Валідація координат ===
Зона польоту: X[-100, 100], Y[-100, 100]

(   0,    0): ✓ OK
(  50,   50): ✓ OK
( 100,  100): ✓ OK
(-100, -100): ✓ OK
( 150,   50): ✗ X занадто велике (150 > 100)
(  50, -150): ✗ Y занадто мале (-150 < -100)
( 200,  200): ✗ X занадто велике (200 > 100), Y занадто велике (200 > 100)
```

---

### Завдання 3: Пора доби

**Умова:** Напишіть функцію `get_time_of_day`, яка приймає годину (0-23) та повертає пору доби:
- 0-5: "NIGHT"
- 6-11: "MORNING"
- 12-17: "AFTERNOON"
- 18-23: "EVENING"

Також обробіть невалідні значення (< 0 або > 23).

**Розв'язання:**

```rust
fn get_time_of_day(hour: i32) -> &'static str {
    match hour {
        0..=5 => "NIGHT",
        6..=11 => "MORNING",
        12..=17 => "AFTERNOON",
        18..=23 => "EVENING",
        _ => "INVALID",
    }
}

fn get_emoji(time_of_day: &str) -> &'static str {
    match time_of_day {
        "NIGHT" => "🌙",
        "MORNING" => "🌅",
        "AFTERNOON" => "☀️",
        "EVENING" => "🌆",
        _ => "❓",
    }
}

fn main() {
    println!("=== Визначення пори доби ===\n");
    
    // Вибірка годин
    println!("Година | Пора доби");
    println!("{}", "-".repeat(25));
    
    for hour in [0, 5, 6, 11, 12, 17, 18, 23] {
        let time_of_day = get_time_of_day(hour);
        let emoji = get_emoji(time_of_day);
        println!("{:>6} | {} {}", hour, emoji, time_of_day);
    }
    
    // Невалідні значення
    println!("\n=== Невалідні значення ===");
    println!("-1: {}", get_time_of_day(-1));
    println!("24: {}", get_time_of_day(24));
    println!("100: {}", get_time_of_day(100));
}
```

**Пояснення:**

1. `match hour { }` — елегантне рішення для діапазонів
2. `0..=5` — від 0 до 5 включно
3. `_` покриває всі значення поза 0-23
4. Додаткова функція `get_emoji` демонструє `match` з рядками

**Результат:**
```
=== Визначення пори доби ===

Година | Пора доби
-------------------------
     0 | 🌙 NIGHT
     5 | 🌙 NIGHT
     6 | 🌅 MORNING
    11 | 🌅 MORNING
    12 | ☀️ AFTERNOON
    17 | ☀️ AFTERNOON
    18 | 🌆 EVENING
    23 | 🌆 EVENING

=== Невалідні значення ===
-1: INVALID
24: INVALID
100: INVALID
```

---

### Завдання 4: Система попереджень

**Умова:** Напишіть функцію `get_warning_level`, яка аналізує стан дрона та повертає рівень попередження:
- "CRITICAL" — батарея < 10% АБО втрата зв'язку
- "WARNING" — батарея < 25% АБО сильний вітер (> 15 м/с) АБО низька видимість (< 500м)
- "CAUTION" — батарея < 50% АБО помірний вітер (> 10 м/с)
- "NORMAL" — все в нормі

Пріоритет: CRITICAL > WARNING > CAUTION > NORMAL

**Розв'язання:**

```rust
fn get_warning_level(
    battery: f64,
    has_connection: bool,
    wind_speed: f64,
    visibility: f64,
) -> &'static str {
    // Перевіряємо умови від найсерйознішої до найменшої
    
    // CRITICAL: найвищий пріоритет
    if battery < 10.0 || !has_connection {
        return "CRITICAL";
    }
    
    // WARNING
    if battery < 25.0 || wind_speed > 15.0 || visibility < 500.0 {
        return "WARNING";
    }
    
    // CAUTION
    if battery < 50.0 || wind_speed > 10.0 {
        return "CAUTION";
    }
    
    // NORMAL: все добре
    "NORMAL"
}

fn get_warning_symbol(level: &str) -> &'static str {
    match level {
        "CRITICAL" => "🔴",
        "WARNING" => "🟡",
        "CAUTION" => "🟠",
        "NORMAL" => "🟢",
        _ => "⚪",
    }
}

fn main() {
    println!("=== Система попереджень дрона ===\n");
    
    // Тестові сценарії: (battery, connection, wind, visibility, description)
    let scenarios = [
        (85.0, true, 5.0, 2000.0, "Ідеальні умови"),
        (45.0, true, 8.0, 1500.0, "Батарея нижче 50%"),
        (60.0, true, 12.0, 1000.0, "Помірний вітер"),
        (20.0, true, 5.0, 800.0, "Низька батарея"),
        (80.0, true, 18.0, 1000.0, "Сильний вітер"),
        (70.0, true, 5.0, 300.0, "Погана видимість"),
        (5.0, true, 5.0, 1000.0, "Критична батарея"),
        (80.0, false, 5.0, 1000.0, "Втрата зв'язку"),
        (8.0, false, 20.0, 100.0, "Кілька проблем"),
    ];
    
    for (battery, conn, wind, vis, desc) in scenarios {
        let level = get_warning_level(battery, conn, wind, vis);
        let symbol = get_warning_symbol(level);
        
        println!("{} {:>10} | {}", symbol, level, desc);
        println!("    Батарея: {}%, Зв'язок: {}, Вітер: {} м/с, Видимість: {} м\n",
                 battery, conn, wind, vis);
    }
}
```

**Пояснення:**

1. Перевіряємо умови в порядку пріоритету (CRITICAL першим)
2. `||` (OR) — достатньо однієї умови для спрацювання
3. `return` забезпечує ранній вихід при знаходженні проблеми
4. Якщо жодна умова не спрацювала — повертаємо "NORMAL"

**Результат:**
```
=== Система попереджень дрона ===

🟢     NORMAL | Ідеальні умови
    Батарея: 85%, Зв'язок: true, Вітер: 5 м/с, Видимість: 2000 м

🟠    CAUTION | Батарея нижче 50%
    Батарея: 45%, Зв'язок: true, Вітер: 8 м/с, Видимість: 1500 м

🟠    CAUTION | Помірний вітер
    Батарея: 60%, Зв'язок: true, Вітер: 12 м/с, Видимість: 1000 м

🟡    WARNING | Низька батарея
    Батарея: 20%, Зв'язок: true, Вітер: 5 м/с, Видимість: 800 м

🟡    WARNING | Сильний вітер
    Батарея: 80%, Зв'язок: true, Вітер: 18 м/с, Видимість: 1000 м

🟡    WARNING | Погана видимість
    Батарея: 70%, Зв'язок: true, Вітер: 5 м/с, Видимість: 300 м

🔴   CRITICAL | Критична батарея
    Батарея: 5%, Зв'язок: true, Вітер: 5 м/с, Видимість: 1000 м

🔴   CRITICAL | Втрата зв'язку
    Батарея: 80%, Зв'язок: false, Вітер: 5 м/с, Видимість: 1000 м

🔴   CRITICAL | Кілька проблем
    Батарея: 8%, Зв'язок: false, Вітер: 20 м/с, Видимість: 100 м
```

---

## Підсумок заняття

На цьому занятті ви навчились:

1. **Використовувати if/else/else if** для розгалуження коду
2. **Застосовувати оператори порівняння**: `==`, `!=`, `<`, `>`, `<=`, `>=`
3. **Комбінувати умови** через `&&` (AND), `||` (OR), `!` (NOT)
4. **Використовувати if як вираз** для присвоєння значень
5. **Писати match** для елегантного вибору з варіантів
6. **Застосовувати діапазони в match**: `1..=10`
7. **Розуміти пріоритет умов** та порядок їх перевірки

---

## Перевірте себе

1. Яка різниця між `=` та `==`?
2. Що поверне `true && false`?
3. Що поверне `true || false`?
4. Чому важливий порядок в ланцюжку `if-else if-else`?
5. Як використати `if` для присвоєння значення змінній?
6. Що означає `_` в `match`?

**Відповіді:**
1. `=` — присвоєння, `==` — порівняння на рівність
2. `false` (AND вимагає обидва true)
3. `true` (OR вимагає хоча б один true)
4. Виконується перша гілка з true умовою, решта ігноруються
5. `let x = if condition { value1 } else { value2 };`
6. Wildcard — "будь-яке інше значення"

---

## Наступне заняття

На наступному занятті ми вивчимо **цикли**: `loop`, `while`, `for`, а також оператори `break` і `continue`. Ви навчитесь повторювати дії та ітерувати по колекціях даних.
