# Підступні задачі з операторами циклу

---

## 📋 Анотація

Цей розділ досліджує неочевидні аспекти роботи циклів, які можуть перетворити просту ітерацію на джерело важковиявлюваних помилок. Ви дізнаєтесь, чому модифікація колекції під час ітерації — це гра з вогнем, як пропустити елемент, навіть не помітивши цього, та чому цикл може ніколи не завершитися, хоча на перший погляд все правильно. У контексті рою БПЛА ці знання критичні: цикл обробки команд, що "з'їдає" повідомлення, або нескінченна ітерація по списку цілей може паралізувати всю систему.

---

## 🎯 Цілі навчання

Після завершення цього розділу ви зможете:

1. **Пояснити** чому модифікація колекції під час ітерації небезпечна
2. **Розпізнавати** ситуації пропуску елементів при видаленні
3. **Уникати** нескінченних циклів через помилки з лічильниками
4. **Розуміти** різницю між `loop`, `while` та `for` у Rust
5. **Правильно використовувати** `break` та `continue` у вкладених циклах
6. **Застосовувати** ітератори замість індексних циклів
7. **Писати** безпечний код обходу колекцій

---

## 📚 Ключові терміни

| Термін | Визначення |
|--------|------------|
| **ітератор** | Абстракція, що дозволяє послідовно обходити елементи колекції |
| **borrowing під час ітерації** | Позичання колекції ітератором, що блокує її модифікацію |
| **інвалідація ітератора** | Ситуація, коли ітератор стає недійсним через зміну колекції |
| **off-by-one error** | Помилка на одиницю в межах циклу |
| **нескінченний цикл** | Цикл, умова завершення якого ніколи не стає істинною |
| **мітка циклу (label)** | Іменований цикл для керування `break`/`continue` у вкладених структурах |

---

## 💡 Мотиваційний кейс: Втрачені команди рою

Уявімо систему керування роєм БПЛА. Центральний диспетчер отримує команди від оператора і розподіляє їх між дронами. Команди зберігаються в черзі, і система обробляє їх у циклі:

```rust
// Спрощений псевдокод проблемного підходу
for (index, command) in commands.iter().enumerate() {
    if command.is_urgent() {
        execute_immediately(command);
        commands.remove(index);  // Проблема!
    }
}
```

Програміст хотів видаляти термінові команди одразу після виконання. Але при видаленні елемента всі наступні зсуваються, а індекс продовжує зростати. Результат: деякі команди пропускаються і ніколи не обробляються.

У реальному бою це може означати, що команда "Повернутися на базу" або "Припинити атаку" просто зникне. Дрон продовжить виконувати застарілу місію, не отримавши критичного оновлення.

Ця проблема настільки поширена, що має власну назву: "модифікація колекції під час ітерації". Rust, на щастя, захищає від найгірших варіантів цієї помилки через систему borrowing, але розуміння механізму все одно критичне.

---

## ТЕОРІЯ: ЧОМУ НЕ МОЖНА МОДИФІКУВАТИ КОЛЕКЦІЮ ПІД ЧАС ІТЕРАЦІЇ

### Як працює ітерація

Коли ви пишете цикл `for item in collection`, Rust створює ітератор — спеціальний об'єкт, що відстежує поточну позицію в колекції. Ітератор "знає", де він зараз знаходиться, і вміє переходити до наступного елемента.

Ключовий момент: ітератор покладається на те, що структура колекції не змінюється. Він зберігає внутрішній стан (наприклад, індекс або вказівник), який має сенс лише для конкретної конфігурації колекції.

### Що відбувається при модифікації

Уявімо список з п'яти елементів: `[A, B, C, D, E]`. Ітератор знаходиться на позиції 2 (елемент C). Тепер ми видаляємо елемент B (позиція 1).

Після видалення список стає `[A, C, D, E]`. Але ітератор все ще "думає", що він на позиції 2. Раніше позиція 2 містила C, тепер там D. Ітератор "перестрибне" через C і перейде одразу до D. Елемент C буде пропущено.

Якщо ми додаємо елемент замість видалення, проблема дзеркальна: деякі елементи можуть оброблятися двічі.

### Захист Rust через borrowing

У багатьох мовах (C#, Java, Python) модифікація колекції під час ітерації призводить до виключення під час виконання або, що гірше, до тихої некоректної поведінки. Rust застосовує радикальніший підхід: він забороняє таку модифікацію на етапі компіляції.

Коли ви ітеруєте по колекції через `for item in &collection` або `for item in &mut collection`, ви позичаєте колекцію. Immutable borrow (`&`) забороняє будь-які зміни. Mutable borrow (`&mut`) дозволяє змінювати елементи, але не структуру колекції (додавання/видалення).

Спроба викликати `collection.push()` або `collection.remove()` всередині такого циклу призведе до помилки компіляції: "cannot borrow `collection` as mutable because it is also borrowed as immutable".

Це може здаватися обмеженням, але насправді це захист. Компілятор не дозволяє вам написати код, який би працював некоректно.

### Коли Rust не захистить

Захист працює лише коли ви використовуєте ітератори безпосередньо. Якщо ви ітеруєте по індексах і модифікуєте колекцію через індексний доступ, компілятор може не помітити проблему:

```rust
let mut numbers = vec![1, 2, 3, 4, 5];
let mut i = 0;
while i < numbers.len() {
    if numbers[i] == 3 {
        numbers.remove(i);
    }
    i += 1;
}
```

Цей код скомпілюється, але працюватиме неправильно. Після видалення елемента з позиції `i` наступний елемент зсувається на цю позицію, але `i` все одно збільшується. Елемент, що зсунувся, буде пропущено.

---

## ТЕОРІЯ: ПРОПУСК ЕЛЕМЕНТІВ ПРИ ВИДАЛЕННІ

### Детальний розбір механізму

Розглянемо список `[1, 2, 2, 3, 2, 4]` і завдання видалити всі двійки. Наївний підхід — ітерувати по індексах і видаляти:

Початок: індекси 0,1,2,3,4,5 містять значення 1,2,2,3,2,4.

Ітерація i=0: numbers[0]=1, не двійка, пропускаємо.
Ітерація i=1: numbers[1]=2, видаляємо! Список стає [1,2,3,2,4].
Ітерація i=2: numbers[2]=3, не двійка, пропускаємо. Але зачекайте — ми пропустили двійку, яка була на позиції 2 і зсунулася на позицію 1!
Ітерація i=3: numbers[3]=2, видаляємо! Список стає [1,2,3,4].
Ітерація i=4: i >= numbers.len() (4 >= 4), цикл завершується.

Результат: [1, 2, 3, 4]. Одна двійка залишилася!

Проблема в тому, що після видалення елемента всі наступні елементи зсуваються вліво, займаючи менші індекси. Але наш лічильник i продовжує зростати, не враховуючи цей зсув.

### Рішення 1: Ітерація з кінця

Якщо ітерувати від останнього елемента до першого, зсув елементів не впливає на ще не оброблені позиції:

```rust
let mut numbers = vec![1, 2, 2, 3, 2, 4];
for i in (0..numbers.len()).rev() {
    if numbers[i] == 2 {
        numbers.remove(i);
    }
}
```

При видаленні елемента з позиції i всі елементи з більшими індексами зсуваються. Але ми вже обробили ці позиції! Елементи з меншими індексами (які ще попереду) залишаються на своїх місцях.

### Рішення 2: Корекція індексу після видалення

Якщо після видалення не збільшувати індекс, наступна ітерація обробить елемент, що зсунувся на поточну позицію:

```rust
let mut numbers = vec![1, 2, 2, 3, 2, 4];
let mut i = 0;
while i < numbers.len() {
    if numbers[i] == 2 {
        numbers.remove(i);
        // Не збільшуємо i — наступний елемент сам прийшов на цю позицію
    } else {
        i += 1;
    }
}
```

Цей підхід працює, але вимагає уважності. Легко забути про корекцію або зробити її неправильно.

### Рішення 3: Метод retain (ідіоматичний Rust)

Найкращий підхід у Rust — використовувати метод `retain`, який залишає лише елементи, що задовольняють умову:

```rust
let mut numbers = vec![1, 2, 2, 3, 2, 4];
numbers.retain(|&x| x != 2);
```

Метод `retain` приймає замикання (closure), що повертає `true` для елементів, які треба залишити. Він ефективно обробляє всі елементи за один прохід, не створюючи проблем з індексами.

Цей підхід не лише безпечніший, але й ефективніший: він не зсуває елементи багаторазово, а робить один прохід з компактифікацією.

### Рішення 4: Створення нової колекції через filter

Якщо вам не потрібно модифікувати оригінальну колекцію, створіть нову:

```rust
let numbers = vec![1, 2, 2, 3, 2, 4];
let filtered: Vec<_> = numbers.into_iter().filter(|&x| x != 2).collect();
```

Цей підхід функціональний і декларативний: ви описуєте, що хочете отримати, а не як це зробити. Ітератор з `filter` обробляє кожен елемент рівно один раз, без ризику пропуску.

---

## ТЕОРІЯ: НЕСКІНЧЕННІ ЦИКЛИ ЧЕРЕЗ ПОМИЛКИ З ЛІЧИЛЬНИКАМИ

### Класична помилка: неправильний напрямок

Одна з найпоширеніших помилок — написати інкремент замість декременту (або навпаки) у циклі:

```rust
// Задум: вивести числа від 10 до 0
for i in (0..=10).rev() {
    println!("{}", i);
}

// Помилка: нескінченний цикл
let mut i = 10;
while i >= 0 {
    println!("{}", i);
    i += 1;  // Мало бути i -= 1!
}
```

У першому випадку Rust сам керує ітерацією — помилка неможлива. У другому випадку програміст написав `i += 1` замість `i -= 1`. Змінна i буде зростати: 10, 11, 12... Умова `i >= 0` завжди істинна для додатних чисел.

Насправді цей цикл не буде нескінченним — він завершиться, коли i переповниться. У debug-режимі Rust викличе паніку при переповненні. У release-режимі значення "обернеться" і стане від'ємним, тоді умова стане хибною. Але це не та поведінка, яку програміст хотів.

### Цикл з дійсними числами: поглинання кроку

Про цю проблему ми детально говорили в розділі про дійсні числа, але вона настільки важлива для циклів, що варто повторити.

```rust
let mut x = 1.0_f32;
while x > 0.0 {
    x -= 0.00000001;
}
```

Цей цикл виглядає простим: зменшуємо x, поки воно не стане нулем або від'ємним. Але крок 0.00000001 надто малий порівняно з 1.0. Через обмежену точність f32, віднімання такого малого числа від значення близько 1.0 не змінює результат — мале число "поглинається".

Цикл буде виконуватися, поки x не наблизиться до значень порядку 10^-7, де крок стане відчутним. Це мільйони ітерацій замість очікуваних ста мільйонів. А якщо крок ще менший, цикл може фактично зависнути.

### Цикл з перетворенням типів

```rust
let mut i = 0;
while i != 10 {
    i = (i as f64 + 0.3) as i32;
    println!("{}", i);
}
```

Тут програміст хотів збільшувати i на 0.3 кожну ітерацію. Але перетворення `as i32` відкидає дробову частину. На першій ітерації: 0 + 0.3 = 0.3, (int)0.3 = 0. Змінна i залишається 0! Цикл нескінченний.

Навіть якщо початкове значення інше, наприклад i = 5, послідовність буде: 5.3 → 5, 5.3 → 5, 5.3 → 5... Крок 0.3 недостатній, щоб після округлення збільшити ціле число.

---

## ТЕОРІЯ: ОСОБЛИВОСТІ РІЗНИХ ВИДІВ ЦИКЛІВ У RUST

### Цикл for: безпечний за замовчуванням

Цикл `for` у Rust працює з ітераторами, а не з індексами напряму. Це робить його значно безпечнішим за аналоги в інших мовах.

```rust
for item in collection {
    // item — це елемент, не індекс
}
```

Ви не можете випадково вийти за межі колекції, бо ітератор сам знає, коли зупинитися. Ви не можете забути збільшити лічильник, бо немає лічильника. Ви не можете використати неправильний крок, бо ітератор сам визначає порядок обходу.

Якщо вам потрібні індекси, використовуйте `enumerate`:

```rust
for (index, item) in collection.iter().enumerate() {
    println!("Елемент {} на позиції {}", item, index);
}
```

### Цикл while: контроль у ваших руках

Цикл `while` дає більше контролю, але й більше відповідальності:

```rust
while condition {
    // тіло циклу
}
```

Умова перевіряється перед кожною ітерацією. Якщо умова хибна з самого початку, тіло не виконається жодного разу. Це важливо пам'ятати, коли ви розраховуєте на хоча б одне виконання.

Всередині циклу ви відповідаєте за те, щоб умова колись стала хибною. Якщо ви забудете змінити змінні, від яких залежить умова, цикл буде нескінченним.

### Цикл loop: явна нескінченність

Rust має окремий синтаксис для нескінченних циклів:

```rust
loop {
    // тіло
    if exit_condition {
        break;
    }
}
```

Використання `loop` замість `while true` має кілька переваг. По-перше, воно явно сигналізує наміри: цей цикл задуманий як нескінченний. По-друге, компілятор краще оптимізує такий код. По-третє, `loop` може повертати значення через `break`:

```rust
let result = loop {
    let attempt = try_something();
    if attempt.is_ok() {
        break attempt.unwrap();  // Повертаємо значення з циклу
    }
};
```

### do-while у Rust: loop з break в кінці

Rust не має окремого `do-while`, але його можна емулювати:

```rust
loop {
    // тіло виконується хоча б раз
    
    if !condition {
        break;
    }
}
```

Або через `while` з початковою ініціалізацією:

```rust
let mut first_iteration = true;
while first_iteration || condition {
    first_iteration = false;
    // тіло
}
```

Семантика do-while: тіло виконується, потім перевіряється умова. Якщо умова істинна, цикл повторюється. Важливо: тіло завжди виконується хоча б один раз, навіть якщо умова хибна з самого початку.

Це корисно, коли перша ітерація має відбутися безумовно — наприклад, показати меню користувачу і чекати валідного введення.

---

## ТЕОРІЯ: BREAK ТА CONTINUE У ВКЛАДЕНИХ ЦИКЛАХ

### Проблема: break виходить лише з одного циклу

Розглянемо типову ситуацію пошуку в двовимірній структурі:

```rust
let matrix = vec![
    vec![1, 2, 3],
    vec![4, 5, 6],
    vec![7, 8, 9],
];

for row in &matrix {
    for &value in row {
        if value == 5 {
            println!("Знайдено!");
            break;  // Виходить лише з внутрішнього циклу!
        }
    }
}
```

Програміст очікує, що після знаходження 5 пошук завершиться. Насправді `break` виходить лише з внутрішнього циклу (по елементах рядка). Зовнішній цикл (по рядках) продовжується. Програма перевірить рядки [7,8,9], хоча елемент уже знайдено.

Це не просто неефективність — це може бути логічною помилкою, якщо код після циклу очікує певного стану.

### Рішення: мітки циклів (loop labels)

Rust дозволяє іменувати цикли і вказувати, з якого саме циклу виходити:

```rust
'outer: for row in &matrix {
    for &value in row {
        if value == 5 {
            println!("Знайдено!");
            break 'outer;  // Виходить із зовнішнього циклу
        }
    }
}
```

Мітка `'outer:` іменує зовнішній цикл. Інструкція `break 'outer` виходить саме з нього, а не з найближчого внутрішнього.

Мітки можна використовувати з будь-якою глибиною вкладеності:

```rust
'level1: loop {
    'level2: loop {
        'level3: loop {
            break 'level1;  // Виходить одразу з усіх трьох циклів
        }
    }
}
```

### Continue з мітками

Аналогічно працює `continue` — можна вказати, який саме цикл продовжити:

```rust
'outer: for i in 0..5 {
    for j in 0..5 {
        if some_condition(i, j) {
            continue 'outer;  // Переходить до наступної ітерації зовнішнього циклу
        }
        process(i, j);
    }
}
```

Звичайний `continue` пропустив би решту внутрішнього циклу і перейшов до j+1. `continue 'outer` пропускає решту обох циклів і переходить до i+1.

### Коли мітки виправдані, а коли — code smell

Мітки — потужний інструмент, але їх надмірне використання може зробити код заплутаним. Рекомендації:

Мітки виправдані для пошуку в багатовимірних структурах, де потрібно вийти одразу після знаходження. Це чітка, зрозуміла семантика.

Мітки підозрілі, якщо їх більше двох рівнів. Це може сигналізувати про надто складну логіку, яку варто розбити на функції.

Мітки зайві, якщо можна переписати код через ітератори з `find`, `any`, `position` тощо:

```rust
let found = matrix.iter()
    .flat_map(|row| row.iter())
    .any(|&x| x == 5);
```

Цей код не лише коротший, але й чіткіше виражає намір: "перевірити, чи є 5 у матриці".

---

## ТЕОРІЯ: CONTINUE ТА ПРОПУСК ВАЖЛИВОГО КОДУ

### Як працює continue

Інструкція `continue` негайно переходить до наступної ітерації циклу, пропускаючи весь код, що залишився в тілі циклу. Це зручно для фільтрації:

```rust
for item in items {
    if !is_valid(&item) {
        continue;  // Пропускаємо невалідні елементи
    }
    
    process(&item);  // Обробляємо лише валідні
}
```

### Небезпека: пропуск критичного коду

Проблема виникає, коли `continue` знаходиться перед важливим кодом, який має виконуватися для кожного елемента:

```rust
let mut total = 0;
let mut count = 0;

for value in values {
    if value < 0 {
        println!("Пропускаємо від'ємне: {}", value);
        continue;
    }
    
    total += value;
    count += 1;  // Цей код не виконується для від'ємних!
}

let average = total / count;  // count може бути 0!
```

Якщо всі значення від'ємні, count залишиться 0, і ділення викличе паніку.

Менш очевидний приклад — коли `continue` додається пізніше при модифікації коду:

```rust
// Початковий код
for item in items {
    prepare(&item);
    process(&item);
    cleanup(&item);
}

// Після "оптимізації" — додали ранній вихід
for item in items {
    prepare(&item);
    
    if item.is_empty() {
        continue;  // Пропускаємо порожні
    }
    
    process(&item);
    cleanup(&item);  // БАГ: cleanup не викликається для порожніх!
}
```

Якщо `cleanup` звільняє ресурси, виділені в `prepare`, ми отримали витік ресурсів.

### Рекомендації

Розміщуйте continue якомога раніше в тілі циклу — до будь-якого коду, що виділяє ресурси або змінює стан.

Віддавайте перевагу позитивним умовам замість continue:

```rust
// Замість
for item in items {
    if condition {
        continue;
    }
    process(&item);
}

// Краще
for item in items {
    if !condition {
        process(&item);
    }
}
```

Використовуйте filter для ітераторів:

```rust
for item in items.iter().filter(|i| !i.is_empty()) {
    process(item);
}
```

---

## ТЕОРІЯ: МОДИФІКАЦІЯ ЕЛЕМЕНТІВ ЧЕРЕЗ ІТЕРАТОР

### Проблема: foreach не змінює оригінал (у багатьох мовах)

У деяких мовах ітераційна змінна є копією елемента. Зміна цієї копії не впливає на оригінальну колекцію.

У Rust поведінка залежить від типу ітерації:

```rust
let mut numbers = vec![1, 2, 3];

// Ітерація по значеннях (споживає колекцію)
for n in numbers {
    // n — це власне значення, numbers більше не існує
}

// Ітерація по посиланнях
let numbers = vec![1, 2, 3];
for n in &numbers {
    // n — це &i32, змінити не можна
}

// Ітерація по мутабельних посиланнях
let mut numbers = vec![1, 2, 3];
for n in &mut numbers {
    *n += 10;  // Змінюємо оригінал!
}
// numbers тепер [11, 12, 13]
```

### Чому потрібна зірочка (dereference)

Коли ви ітеруєте через `&mut`, змінна циклу є мутабельним посиланням. Щоб змінити значення, на яке вона вказує, потрібно розіменувати посилання оператором `*`:

```rust
for n in &mut numbers {
    *n += 10;  // *n — це саме значення, не посилання
}
```

Без зірочки ви б спробували змінити саме посилання (на що воно вказує), а не значення за ним. Rust не дозволить це, бо час життя посилання контролюється ітератором.

### Альтернатива: iter_mut з індексами

Якщо потрібен доступ і до індексу, і до мутабельного елемента:

```rust
for (i, n) in numbers.iter_mut().enumerate() {
    *n = i as i32 * 10;  // Присвоюємо нове значення на основі індексу
}
```

### map для трансформації

Якщо вам потрібна нова колекція з трансформованими значеннями, використовуйте `map`:

```rust
let doubled: Vec<i32> = numbers.iter().map(|n| n * 2).collect();
```

Цей підхід не модифікує оригінал, а створює нову колекцію. Це часто безпечніше і виразніше.

---

## ТЕОРІЯ: СКЛАДНІ УМОВИ ЦИКЛУ

### Пріоритет операторів у умові while

Умова `while` підпорядковується тим самим правилам пріоритету, що й звичайні вирази. Про це ми детально говорили в розділі про умовні оператори, але в контексті циклів це набуває нового значення.

Розглянемо:

```rust
let mut i = 0;
while i < 10 || i % 3 != 0 {
    i += 1;
}
```

Програміст, можливо, хотів: "крутити цикл, поки i менше 10 І не кратне 3". Тобто зупинитися, коли i стане 10 або більше АБО коли i стане кратним 3.

Але умова читається як: "крутити, поки (i < 10) АБО (i % 3 != 0)". Цикл зупиниться лише коли ОБИДВІ частини хибні: i >= 10 І i % 3 == 0.

Прослідкуємо: при i=10 маємо (false || true) = true, цикл продовжується. При i=11: (false || true) = true. При i=12: (false || false) = false, цикл зупиняється.

Цикл виконався 12 разів замість очікуваних 3 (до першого кратного 3).

### Рішення: явні дужки та розбиття на змінні

```rust
// Явні дужки
while i < 10 && i % 3 != 0 {
    i += 1;
}

// Або змінні з зрозумілими іменами
while !reached_limit && !found_multiple_of_three {
    let reached_limit = i >= 10;
    let found_multiple_of_three = i % 3 == 0;
    i += 1;
}
```

---

## ПРАКТИЧНІ РЕКОМЕНДАЦІЇ

### Правило 1: Використовуйте for замість while для обходу колекцій

Цикл `for` з ітератором безпечніший за `while` з індексом:

```rust
// Погано: легко помилитися з індексом
let mut i = 0;
while i < vec.len() {
    process(&vec[i]);
    i += 1;  // Можна забути
}

// Добре: ітератор керує всім
for item in &vec {
    process(item);
}
```

### Правило 2: Для видалення елементів використовуйте retain або filter

```rust
// Погано: ручне керування індексами
let mut i = 0;
while i < vec.len() {
    if should_remove(&vec[i]) {
        vec.remove(i);
    } else {
        i += 1;
    }
}

// Добре: декларативний підхід
vec.retain(|item| !should_remove(item));
```

### Правило 3: Для вкладених циклів з виходом використовуйте мітки або функції

```rust
// Варіант з міткою
'search: for row in &matrix {
    for &cell in row {
        if cell == target {
            break 'search;
        }
    }
}

// Варіант з функцією
fn find_in_matrix(matrix: &[Vec<i32>], target: i32) -> Option<(usize, usize)> {
    for (i, row) in matrix.iter().enumerate() {
        for (j, &cell) in row.iter().enumerate() {
            if cell == target {
                return Some((i, j));
            }
        }
    }
    None
}
```

### Правило 4: Уникайте дійсних чисел у умовах циклу

```rust
// Погано: може не завершитися або працювати неочікувано
while x != 1.0 {
    x += 0.1;
}

// Добре: цілочисельний лічильник
for i in 0..10 {
    let x = i as f64 * 0.1;
}
```

### Правило 5: Розміщуйте continue на початку тіла циклу

```rust
// Погано: continue в середині
for item in items {
    let resource = acquire_resource();
    
    if item.is_invalid() {
        continue;  // resource не звільнено!
    }
    
    process(&resource);
    release_resource(resource);
}

// Добре: continue до виділення ресурсів
for item in items {
    if item.is_invalid() {
        continue;
    }
    
    let resource = acquire_resource();
    process(&resource);
    release_resource(resource);
}
```

---

## Застосування до рою БПЛА

### Безпечна обробка черги команд

```rust
struct CommandQueue {
    commands: Vec<Command>,
}

impl CommandQueue {
    // Небезпечно: може пропустити команди
    fn process_naive(&mut self) {
        let mut i = 0;
        while i < self.commands.len() {
            if self.commands[i].is_urgent() {
                let cmd = self.commands.remove(i);
                execute(cmd);
                // Забули: не збільшувати i!
            }
            i += 1;  // БАГ: пропуск команди
        }
    }
    
    // Безпечно: через retain
    fn process_safe(&mut self) {
        // Спочатку виконуємо термінові
        for cmd in self.commands.iter().filter(|c| c.is_urgent()) {
            execute(cmd.clone());
        }
        // Потім видаляємо виконані
        self.commands.retain(|c| !c.is_urgent());
    }
    
    // Ще краще: через drain_filter (nightly) або partition
    fn process_best(&mut self) {
        let (urgent, regular): (Vec<_>, Vec<_>) = 
            std::mem::take(&mut self.commands)
                .into_iter()
                .partition(|c| c.is_urgent());
        
        for cmd in urgent {
            execute(cmd);
        }
        
        self.commands = regular;
    }
}
```

### Пошук цілі в секторі

```rust
fn find_target_in_sector<'a>(
    drones: &'a [Drone],
    sector: &Sector,
) -> Option<&'a Drone> {
    // Ітератор замість вкладених циклів з break
    drones.iter()
        .filter(|d| sector.contains(d.position()))
        .filter(|d| d.is_hostile())
        .min_by(|a, b| {
            let dist_a = sector.center().distance_to(a.position());
            let dist_b = sector.center().distance_to(b.position());
            dist_a.partial_cmp(&dist_b).unwrap()
        })
}
```

---

## Резюме

У цьому розділі ми розглянули підступні аспекти роботи з циклами.

**Модифікація колекції під час ітерації** призводить до пропуску елементів або подвійної обробки. Rust захищає від цього через borrowing, але при використанні індексів проблема залишається. Рішення: `retain`, `filter`, ітерація з кінця.

**Пропуск елементів при видаленні** відбувається через зсув індексів. Після видалення елемента наступний займає його місце, але лічильник продовжує зростати. Рішення: ітерація з кінця або корекція індексу.

**Нескінченні цикли** виникають через помилки з лічильниками (неправильний напрямок), дійсними числами (поглинання кроку) або перетворенням типів. Рішення: ретельна перевірка умов, уникнення float у лічильниках.

**break у вкладених циклах** виходить лише з найближчого. Для виходу з зовнішніх циклів використовуйте мітки (`'label: loop`) та `break 'label`.

**continue пропускає весь код** до кінця ітерації. Розміщуйте його на початку тіла циклу, до виділення ресурсів.

**Ітератори Rust** — безпечніша альтернатива індексним циклам. Вони автоматично керують межами та надають багатий набір методів для обробки колекцій.

---

## 🔗 Зв'язок з наступним матеріалом

Опанувавши безпечну роботу з циклами, ви готові перейти до вивчення функцій та модульної організації коду. Там ви побачите, як розбивати складну логіку на окремі функції, як Rust забезпечує безпеку при передачі даних між функціями, та як організувати код агента у чітку модульну структуру.
