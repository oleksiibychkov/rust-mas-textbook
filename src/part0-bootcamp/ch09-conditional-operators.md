# Підступне виконання умовних операторів

---

## 📋 Анотація

Цей розділ розкриває неочевидні аспекти роботи умовних операторів та логічних виразів. Ви дізнаєтесь, чому порядок операндів у логічних виразах може вплинути на виконання програми, як коротке замикання "з'їдає" виклики функцій, та чому неправильне групування умов призводить до логічних помилок. У Rust немає класичного тернарного оператора `?:`, натомість `if-else` є виразом, що повертає значення — це створює свої унікальні можливості та пастки. У контексті рою БПЛА ці знання критичні: неправильна логіка перевірки умов може призвести до хибних рішень агента — наприклад, атаки союзника або ігнорування загрози.

---

## 🎯 Цілі навчання

Після завершення цього розділу ви зможете:

1. **Пояснити** пріоритет операторів `&&` та `||` і правильно групувати умови
2. **Передбачити** поведінку короткого замикання у складних виразах
3. **Використовувати** `if-else` як вираз для ініціалізації змінних
4. **Уникати** типових помилок з вкладеними умовними конструкціями
5. **Застосовувати** `match` для складної логіки з кількома умовами
6. **Розуміти** різницю між виразами та операторами в Rust
7. **Писати** читабельний код з явним групуванням умов

---

## 📚 Ключові терміни

| Термін | Визначення |
|--------|------------|
| **коротке замикання (short-circuit)** | Припинення обчислення логічного виразу, коли результат уже відомий |
| **пріоритет операторів** | Порядок, у якому обчислюються оператори без явних дужок |
| **асоціативність** | Напрямок групування операторів однакового пріоритету |
| **вираз (expression)** | Конструкція, що обчислюється до значення |
| **оператор (statement)** | Конструкція, що виконує дію, але не повертає значення |
| **pattern matching** | Механізм порівняння значення зі зразками та деструктуризації |
| **exhaustive matching** | Вимога обробити всі можливі варіанти у `match` |

---

## 💡 Мотиваційний кейс: Система "свій-чужий"

Уявімо гіпотетичну ситуацію. Рій БПЛА патрулює територію. Кожен дрон має систему ідентифікації "свій-чужий" (IFF — Identification Friend or Foe). Програміст написав логіку прийняття рішення про атаку:

```rust
if target_detected && is_hostile || direct_order {
    attack();
}
```

Задум програміста був таким: атакувати, якщо виявлено ворожу ціль, АБО якщо отримано прямий наказ командування. Тобто він мав на увазі таке групування: `(target_detected && is_hostile) || direct_order`.

Однак компілятор бачить цей вираз інакше. Через правила пріоритету операторів (про які ми детально поговоримо далі), оператор `&&` має вищий пріоритет за `||`. Тому фактичне групування виглядає так: `target_detected && (is_hostile || direct_order)`.

Розглянемо сценарій катастрофи. Нехай `target_detected = true` (виявлено об'єкт), `is_hostile = false` (це союзник!), `direct_order = true` (є наказ атакувати іншу ціль десь в іншому місці).

За задумом програміста союзника атакувати не треба — адже він не ворожий, а прямий наказ стосується іншої цілі. Але за фактичним кодом обчислення відбувається так: спочатку `is_hostile || direct_order` дає `false || true`, що дорівнює `true`. Потім `target_detected && true` дає `true && true`, що дорівнює `true`. Результат — атака союзника.

Правильний код вимагає явних дужок:

```rust
if (target_detected && is_hostile) || direct_order {
    attack();
}
```

Цей приклад демонструє, наскільки критичним є розуміння пріоритету операторів. Помилка в одному символі (відсутність дужок) може призвести до катастрофічних наслідків у реальній системі.

---

## ТЕОРІЯ: ПРІОРИТЕТ ЛОГІЧНИХ ОПЕРАТОРІВ

### Чому існує пріоритет?

Коли ми пишемо математичний вираз `2 + 3 × 4`, ми очікуємо результат 14, а не 20. Це тому, що множення має вищий пріоритет за додавання — спочатку обчислюється `3 × 4 = 12`, а потім `2 + 12 = 14`. Якби ми хотіли спочатку додати, потрібні були б дужки: `(2 + 3) × 4 = 20`.

Така сама логіка застосовується до логічних операторів. У мовах програмування, включаючи Rust, оператор логічного "І" (`&&`) має вищий пріоритет за оператор логічного "АБО" (`||`). Це не випадковість — існує глибока аналогія між арифметикою та логікою, яку називають булевою алгеброю.

В булевій алгебрі оператор `&&` поводиться подібно до множення: якщо хоча б один операнд дорівнює `false` (аналог нуля), весь результат буде `false`. Оператор `||` поводиться подібно до додавання: достатньо одного `true`, щоб результат був `true`. Тому `&&` отримав вищий пріоритет за `||`, так само як множення має вищий пріоритет за додавання.

### Таблиця пріоритетів логічних операторів

| Пріоритет | Оператор | Назва | Опис |
|-----------|----------|-------|------|
| 1 (найвищий) | `!` | Логічне НЕ | Інвертує значення: `!true` дає `false` |
| 2 | `&&` | Логічне І | Повертає `true` лише якщо обидва операнди `true` |
| 3 (найнижчий) | `\|\|` | Логічне АБО | Повертає `true` якщо хоча б один операнд `true` |

### Як компілятор "бачить" ваш код

Коли ви пишете вираз без дужок, компілятор автоматично групує операції згідно з пріоритетом. Розглянемо вираз `a || b && c`. Оскільки `&&` має вищий пріоритет, компілятор інтерпретує його як `a || (b && c)`.

Це означає, що спочатку обчислюється `b && c`, а потім результат цього обчислення використовується як правий операнд для `||`. Якщо ви хотіли іншого порядку — спочатку `a || b`, а потім результат `&& c` — вам потрібно явно написати `(a || b) && c`.

### Практичний приклад: коли порядок має значення

Розглянемо конкретні значення: `a = true`, `b = false`, `c = true`.

При групуванні `a || (b && c)` обчислення відбувається так: спочатку `b && c` дає `false && true`, що дорівнює `false`. Потім `a || false` дає `true || false`, що дорівнює `true`. Фінальний результат: `true`.

При групуванні `(a || b) && c` обчислення інше: спочатку `a || b` дає `true || false`, що дорівнює `true`. Потім `true && c` дає `true && true`, що дорівнює `true`. Фінальний результат теж `true`.

У цьому випадку результати співпали. Але це не завжди так! Розглянемо інші значення: `a = true`, `b = false`, `c = false`.

При групуванні `a || (b && c)`: спочатку `b && c` дає `false && false = false`. Потім `a || false` дає `true || false = true`. Результат: `true`.

При групуванні `(a || b) && c`: спочатку `a || b` дає `true || false = true`. Потім `true && c` дає `true && false = false`. Результат: `false`.

Ось тут результати різні! Якщо ви написали код, очікуючи одне групування, а компілятор застосував інше, ваша програма працюватиме неправильно. Причому помилка може проявлятися лише для деяких комбінацій вхідних даних, що робить її особливо підступною — тести можуть проходити, а в продакшені програма "зламається".

### Рекомендація: завжди використовуйте явні дужки

Навіть якщо ви впевнені в пріоритеті операторів, явні дужки мають кілька переваг. По-перше, вони роблять код самодокументованим — будь-хто, хто читає код, одразу бачить задуманий порядок обчислень. По-друге, вони захищають від помилок при редагуванні — якщо хтось додасть ще один операнд, дужки збережуть правильне групування. По-третє, вони допомагають при code review — ревьюер може зосередитися на логіці, а не на згадуванні таблиці пріоритетів.

---

## ТЕОРІЯ: КОРОТКЕ ЗАМИКАННЯ

### Що таке коротке замикання?

Коротке замикання (англ. short-circuit evaluation) — це оптимізація, при якій обчислення логічного виразу припиняється, щойно результат стає відомим. Це не просто внутрішня оптимізація компілятора — це гарантована поведінка мови, на яку можна і потрібно покладатися.

Розглянемо вираз `true || something`. Оператор `||` повертає `true`, якщо хоча б один операнд істинний. Лівий операнд уже `true`, тому незалежно від значення правого операнда результат буде `true`. Rust гарантує, що в такому випадку `something` взагалі не буде обчислюватися.

Аналогічно для виразу `false && something`. Оператор `&&` повертає `true` лише якщо обидва операнди істинні. Лівий операнд `false`, тому результат гарантовано буде `false` незалежно від правого операнда. Rust не буде обчислювати `something`.

### Правила короткого замикання

Для оператора `||` (логічне АБО):
- Якщо лівий операнд `true`, правий операнд не обчислюється, результат `true`
- Якщо лівий операнд `false`, правий операнд обчислюється, результат залежить від нього

Для оператора `&&` (логічне І):
- Якщо лівий операнд `false`, правий операнд не обчислюється, результат `false`
- Якщо лівий операнд `true`, правий операнд обчислюється, результат залежить від нього

### Чому це важливо?

Коротке замикання має практичні наслідки, коли операнди — це не просто змінні, а виклики функцій або складні вирази з побічними ефектами.

Уявімо, що у вас є функція `перевірити_з'єднання()`, яка виконує мережевий запит і повертає `true`, якщо сервер доступний. Ця операція займає час і має побічний ефект (мережевий трафік, можливі логи на сервері). Якщо ви напишете:

```rust
if кеш_актуальний || перевірити_з'єднання() {
    використати_дані();
}
```

То при `кеш_актуальний = true` функція `перевірити_з'єднання()` взагалі не викличеться. Це може бути як бажаною поведінкою (економія ресурсів), так і помилкою (якщо ви очікували, що перевірка завжди відбувається).

### Небезпека побічних ефектів у логічних виразах

Розглянемо приклад з логуванням. Програміст хоче логувати кожну перевірку авторизації:

```rust
let запис_логу = |повідомлення| {
    записати_в_журнал(повідомлення);
    true
};

if авторизований || запис_логу("Перевірка авторизації") {
    надати_доступ();
}
```

Проблема в тому, що при `авторизований = true` функція `запис_логу` не викличеться через коротке замикання. У журналі не буде запису про цю перевірку. Якщо пізніше виникне потреба проаналізувати всі спроби доступу, частина інформації буде відсутня.

Правильний підхід — виконувати дії з побічними ефектами окремо від логічних виразів:

```rust
записати_в_журнал("Перевірка авторизації");
if авторизований {
    надати_доступ();
}
```

### Коротке замикання як захисний механізм

Водночас коротке замикання часто використовується як корисний патерн для захисту від помилок. Класичний приклад — перевірка на `None` перед доступом до значення:

```rust
if let Some(вказівник) = maybe_pointer {
    if вказівник.is_valid() && вказівник.значення() > 0 {
        обробити(вказівник);
    }
}
```

У цьому випадку якщо `is_valid()` поверне `false`, метод `значення()` не викличеться. Це захищає від потенційної паніки при спробі прочитати невалідні дані.

---

## ТЕОРІЯ: IF-ELSE ЯК ВИРАЗ

### Фундаментальна відмінність Rust

У багатьох мовах програмування (C, Java, Python) конструкція `if-else` є оператором (statement) — вона виконує дію, але не повертає значення. Якщо вам потрібно присвоїти різні значення змінній залежно від умови, ви використовуєте тернарний оператор `умова ? значення1 : значення2`.

Rust обрав інший підхід: `if-else` є виразом (expression), який обчислюється до значення. Це означає, що результат `if-else` можна присвоїти змінній, передати у функцію, повернути з функції тощо.

```rust
let статус = if температура > 30.0 { "спекотно" } else { "нормально" };
```

Цей код повністю еквівалентний тернарному оператору з інших мов, але використовує звичний синтаксис `if-else`. Rust не має окремого тернарного оператора саме тому, що `if-else` і так є виразом.

### Вимога однакових типів у гілках

Оскільки `if-else` повертає значення, обидві гілки мають повертати значення одного типу. Це логічно: змінна не може мати "іноді тип числа, іноді тип рядка". Тип має бути визначений на етапі компіляції.

Спроба написати код з різними типами у гілках призведе до помилки компіляції:

```rust
let результат = if умова { 42 } else { "помилка" };
```

Компілятор видасть повідомлення про несумісні типи: він очікував ціле число (бо перша гілка повертає `42`), але друга гілка повертає рядок. Ця помилка захищає вас від ситуації, коли код працює для одних умов і падає для інших.

### Як працювати з різними типами результатів

Якщо вам справді потрібно повертати значення різної природи, є кілька підходів.

Перший підхід — привести до спільного типу. Якщо обидва значення можна представити як рядки, конвертуйте число в рядок:

```rust
let результат = if умова { 
    значення.to_string() 
} else { 
    "помилка".to_string() 
};
```

Другий підхід — використати enum. Це ідіоматичний спосіб у Rust для представлення значень, які можуть бути різними за природою:

```rust
enum Результат {
    Число(i32),
    Повідомлення(String),
}

let результат = if умова { 
    Результат::Число(42) 
} else { 
    Результат::Повідомлення("помилка".to_string()) 
};
```

Третій підхід — переглянути дизайн. Часто потреба в різних типах сигналізує про те, що логіка потребує рефакторингу. Можливо, замість одного `if-else` краще мати окремі гілки коду для різних сценаріїв.

### Відсутність else та тип unit

Якщо `if` використовується без `else`, Rust вважає, що відсутня гілка повертає `()` — спеціальний тип "unit", що означає "нічого". Тому присвоєння результату `if` без `else` змінній потребує, щоб гілка `if` теж повертала `()`:

```rust
let x = if умова { 
    виконати_дію()  // має повертати ()
};
```

Якщо `виконати_дію()` повертає якесь значення, а не `()`, компілятор повідомить про несумісність типів. Це ще один приклад того, як система типів Rust захищає від логічних помилок.

---

## ТЕОРІЯ: ЗАХИСТ RUST ВІД КЛАСИЧНИХ ПОМИЛОК

### Присвоєння замість порівняння

У мовах C та C++ існує сумнозвісна помилка: написати `=` (присвоєння) замість `==` (порівняння) в умові `if`. Код `if (x = 5)` присвоює 5 змінній x і перевіряє результат присвоєння (який у C дорівнює присвоєному значенню). Оскільки 5 — ненульове значення, умова завжди істинна. Ця помилка призвела до незліченних багів і навіть уразливостей безпеки.

Rust повністю захищений від цієї помилки завдяки системі типів. У Rust операція присвоєння повертає `()` (unit type), а не присвоєне значення. Конструкція `if` вимагає, щоб умова мала тип `bool`. Тип `()` не є `bool`, тому код `if x = 5 { ... }` просто не скомпілюється.

Повідомлення компілятора буде чітким: "очікувався тип `bool`, знайдено `()`". Ви не зможете випадково написати присвоєння замість порівняння — компілятор не дасть.

### Неініціалізовані змінні

У C можна оголосити змінну без початкового значення, а потім забути її ініціалізувати перед використанням. Значення такої змінної буде "сміттям" — тим, що залишилося в пам'яті від попередніх операцій. Це джерело непередбачуваної поведінки та уразливостей.

Rust вимагає, щоб кожна змінна була ініціалізована перед першим використанням. Якщо ви намагаєтесь прочитати змінну, яка могла залишитися неініціалізованою в якійсь гілці коду, компілятор видасть помилку.

```rust
let x: i32;
if умова {
    x = 10;
}
println!("{}", x);  // Помилка: x може бути неініціалізованою
```

Компілятор бачить, що якщо `умова` хибна, змінна `x` не отримає значення. Тому він відмовляється компілювати код, де `x` використовується після `if`.

---

## ТЕОРІЯ: MATCH ЯК БЕЗПЕЧНІША АЛЬТЕРНАТИВА

### Проблема ланцюжків if-else

Коли у вас є багато умов для перевірки, природно писати ланцюжок `if-else if-else`. Однак такий підхід має вади. По-перше, легко забути якийсь випадок — компілятор не перевіряє, чи всі можливі варіанти оброблені. По-друге, при додаванні нового варіанту (наприклад, нового стану агента) потрібно знайти всі місця в коді з відповідними `if-else` і додати нову гілку.

### Exhaustive matching у match

Конструкція `match` у Rust має унікальну властивість: вона вимагає обробки всіх можливих варіантів. Якщо ви працюєте з enum і забули один із варіантів, компілятор видасть помилку.

Уявімо enum для станів дрона:

```rust
enum СтанДрона {
    Очікування,
    Політ,
    Атака,
    Повернення,
}
```

Якщо ви напишете `match`, який обробляє лише три з чотирьох станів:

```rust
match стан {
    СтанДрона::Очікування => ...,
    СтанДрона::Політ => ...,
    СтанДрона::Атака => ...,
    // Забули Повернення!
}
```

Компілятор відмовиться компілювати код з повідомленням: "pattern `СтанДрона::Повернення` not covered". Це неможливо проігнорувати — ви змушені або додати обробку забутого варіанту, або явно вказати `_ => ...` для всіх інших випадків (що є свідомим рішенням, а не випадковим пропуском).

### Коли з'являється новий варіант

Уявімо, що через місяць після написання коду ви додаєте новий стан `Заправка` до enum. Якщо ви використовували `if-else`, компілятор нічого не скаже — старий код просто не буде обробляти новий стан, і це може призвести до помилки в рантаймі.

Якщо ви використовували `match` без `_ => ...`, компілятор видасть помилку в кожному місці, де є `match` по цьому enum. Ви змушені явно вирішити, що робити з новим станом у кожному контексті. Це робить код набагато надійнішим при еволюції системи.

---

## ПРАКТИЧНІ РЕКОМЕНДАЦІЇ

### Правило 1: Явні дужки для складних умов

Навіть якщо ви пам'ятаєте пріоритет операторів, ваші колеги можуть не пам'ятати. Або ви самі через півроку забудете. Явні дужки роблять код самодокументованим:

```rust
// Погано: потрібно згадувати пріоритет
if авторизований && активний || адміністратор { ... }

// Добре: наміри очевидні
if (авторизований && активний) || адміністратор { ... }
```

### Правило 2: Виносьте складні умови у змінні

Якщо умова складається з кількох частин, дайте кожній частині зрозуміле ім'я:

```rust
// Погано: важко зрозуміти, що перевіряється
if (вік >= 18 || має_дозвіл) && не_в_чорному_списку && (баланс > 0 || vip) { ... }

// Добре: кожна підумова має ім'я
let має_право_доступу = вік >= 18 || має_дозвіл;
let не_заблокований = не_в_чорному_списку;
let може_платити = баланс > 0 || vip;

if має_право_доступу && не_заблокований && може_платити { ... }
```

Такий код не лише легше читати — його легше дебажити (можна вивести значення кожної підумови) та тестувати (можна перевірити кожну підумову окремо).

### Правило 3: Уникайте побічних ефектів у логічних виразах

Не покладайтеся на коротке замикання для виконання важливих операцій. Якщо функція має викликатися завжди, викличте її окремо:

```rust
// Погано: логування може не відбутися
if швидка_перевірка() || детальна_перевірка_з_логуванням() { ... }

// Добре: логування відбудеться завжди
let швидкий_результат = швидка_перевірка();
let детальний_результат = детальна_перевірка_з_логуванням();
if швидкий_результат || детальний_результат { ... }
```

### Правило 4: Використовуйте match для enum

Якщо ви працюєте з enum, завжди віддавайте перевагу `match` над ланцюжком `if let`. Це гарантує, що ви не забудете обробити якийсь варіант:

```rust
// Погано: можна забути варіант
if let Команда::Атака(ціль) = команда { ... }
else if let Команда::Рух(позиція) = команда { ... }
// Інші команди ігноруються без попередження

// Добре: компілятор нагадає про пропущені варіанти
match команда {
    Команда::Атака(ціль) => ...,
    Команда::Рух(позиція) => ...,
    Команда::Стоп => ...,
    Команда::Повернення => ...,
}
```

### Правило 5: Ніколи не використовуйте unwrap() у продакшен-коді

Метод `unwrap()` на `Option` або `Result` викликає паніку, якщо значення `None` або `Err`. У тестах це прийнятно, але в робочому коді завжди обробляйте можливість відсутності значення:

```rust
// Погано: паніка при None
let ціль = знайти_ціль().unwrap();

// Добре: явна обробка відсутності
match знайти_ціль() {
    Some(ціль) => атакувати(ціль),
    None => продовжити_патруль(),
}

// Або з значенням за замовчуванням
let ціль = знайти_ціль().unwrap_or(стандартна_ціль);
```

---

## Резюме

У цьому розділі ми розглянули підступні аспекти умовних операторів, які можуть призвести до важковиявлюваних помилок.

**Пріоритет операторів** визначає, як компілятор групує вирази без явних дужок. Оператор `&&` має вищий пріоритет за `||`, що може призвести до неочікуваного групування. Рекомендація: завжди використовуйте явні дужки для складних умов.

**Коротке замикання** гарантує, що частина логічного виразу не обчислюватиметься, якщо результат уже відомий. Це може бути як корисним (оптимізація, захисні перевірки), так і небезпечним (пропущені побічні ефекти). Рекомендація: не покладайтеся на коротке замикання для важливих операцій.

**if-else як вираз** дозволяє присвоювати результат умовної конструкції змінній, але вимагає однакових типів в обох гілках. Rust не має окремого тернарного оператора саме завдяки цій властивості.

**Захист системи типів** робить неможливими класичні помилки інших мов: присвоєння замість порівняння, використання неініціалізованих змінних.

**Конструкція match** з exhaustive matching гарантує обробку всіх варіантів enum і попереджає про пропущені випадки при еволюції коду.

Розуміння цих концепцій критичне для написання надійного коду, особливо в системах, де помилка може мати серйозні наслідки — як у нашому прикладі з системою "свій-чужий" для рою БПЛА.

---

## 🔗 Зв'язок з наступним матеріалом

Тепер, коли ви розумієте підступні особливості умовних операторів, ви готові до вивчення циклів та ітераторів. Там ви побачите, як Rust забезпечує безпечну ітерацію через колекції та як уникати типових помилок з індексами та межами масивів — ще одного джерела критичних помилок у системному програмуванні.
