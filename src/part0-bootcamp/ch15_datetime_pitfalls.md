# Підступні задачі з часом та датами

---

## 📋 Анотація

Цей розділ розкриває неочевидні аспекти роботи з часом і датами, які перетворюють, здавалося б, просту задачу на джерело найбільш підступних помилок у програмуванні. Ви дізнаєтесь, чому година може "зникнути" або "повторитись", чому "31 січня + 1 місяць" не має однозначної відповіді, та чому порівняння двох моментів часу може дати неправильний результат. У контексті рою БПЛА ці знання життєво важливі: синхронізація між агентами, планування місій, логування подій — все це потребує коректної роботи з часом. Помилка в одну годину може означати, що дрон вилетить не тоді, коли заплановано.

---

## 🎯 Цілі навчання

Після завершення цього розділу ви зможете:

1. **Пояснити** різницю між UTC, локальним часом та часовими зонами
2. **Розуміти** проблеми переходу на літній/зимовий час
3. **Уникати** помилок в арифметиці дат
4. **Правильно обирати** між Instant та SystemTime
5. **Обробляти** високосні роки та граничні випадки
6. **Використовувати** монотонний час для вимірювання інтервалів
7. **Серіалізувати** час у форматах, що зберігають часову зону

---

## 📚 Ключові терміни

| Термін | Визначення |
|--------|------------|
| **UTC (Coordinated Universal Time)** | Всесвітній координований час, еталон без часових зон |
| **часова зона (timezone)** | Регіон з єдиним стандартним часом, зазвичай зсув від UTC |
| **DST (Daylight Saving Time)** | Літній час, сезонний зсув годинника на 1 годину |
| **Unix timestamp** | Кількість секунд з 1 січня 1970 року 00:00:00 UTC |
| **Instant** | Монотонний момент часу для вимірювання інтервалів |
| **SystemTime** | Системний час, може "стрибати" при синхронізації |
| **високосна секунда** | Додаткова секунда для синхронізації з астрономічним часом |

---

## 💡 Мотиваційний кейс: Рейс, що зник на годину

У 2007 році авіакомпанія зіткнулась з дивною проблемою. Система бронювання показувала, що рейс о 2:30 ночі не існує. Пасажири не могли придбати квитки на цей час.

Причина: перехід на літній час. У ніч переходу годинники переводяться з 2:00 на 3:00. Час між 2:00 та 3:00 просто не існує в цю дату. Система правильно відмовлялась створювати рейс на неіснуючий час.

Але була й зворотна проблема: при переході на зимовий час годинники переводяться з 3:00 назад на 2:00. Час між 2:00 та 3:00 існує двічі! Якщо рейс заплановано на 2:30, це перші 2:30 (до переведення) чи другі 2:30 (після)?

Ці проблеми не обмежуються авіацією. Будь-яка система, що планує події в локальному часі, стикається з ними: системи автоматизації будівель, медичні нагадування, фінансові транзакції, і, звичайно, координація рою БПЛА.

Один рій дронів мав вилетіти о 2:30 ночі для нічного патрулювання. Через баг з DST система інтерпретувала час як "другі 2:30" замість "перших 2:30". Дрони вилетіли на годину пізніше, коли вже почало світати, і нічне патрулювання втратило сенс.

---

## ТЕОРІЯ: ЧАС — ЦЕ СКЛАДНО

### Ілюзія простоти

На перший погляд, час — це просто число, що постійно збільшується. Секунди, хвилини, години, дні — все здається логічним і передбачуваним. Ця ілюзія руйнується, щойно ви починаєте працювати з реальними даними.

Час у комп'ютерних системах має кілька фундаментальних проблем:

**Час — соціальна конструкція**. Люди вигадали часові зони, літній час, календарі — і всі ці винаходи мають винятки, історичні зміни та політичні рішення. Samoa у 2011 році пропустила цілу п'ятницю, 30 грудня, перестрибнувши через лінію зміни дати. Північна Корея у 2015 році створила власну часову зону UTC+8:30.

**Час не лінійний**. Через DST години можуть зникати або повторюватись. Через високосні секунди хвилина може мати 61 секунду. Через синхронізацію NTP системний час може "стрибнути" назад.

**Час відносний**. Один і той самий момент — це різний "час" у різних частинах світу. Коли в Києві 15:00, у Нью-Йорку 8:00, а в Токіо 22:00. Але це один і той самий момент!

### UTC як єдина точка відліку

UTC (Coordinated Universal Time) — це глобальний стандарт часу без часових зон та літнього часу. Один момент часу має одне UTC-представлення незалежно від того, де ви знаходитесь.

Правило для серверних систем: **зберігайте час у UTC, конвертуйте в локальний лише для відображення**. Це усуває більшість проблем з часовими зонами.

```rust
use chrono::{Utc, Local, TimeZone};

// Зберігаємо в UTC
let event_time = Utc::now();
save_to_database(event_time);

// Відображаємо в локальному часі користувача
let local_time = event_time.with_timezone(&Local);
println!("Подія о {}", local_time.format("%H:%M"));
```

### Чому "просто Unix timestamp" недостатньо

Unix timestamp — кількість секунд з 1 січня 1970 року UTC — здається ідеальним рішенням: одне число, без часових зон, легко порівнювати.

Але є проблеми:

**Точність**: класичний timestamp — це секунди. Для багатьох застосувань (логування, профілювання) потрібні мілі- або наносекунди.

**Діапазон**: 32-бітний signed timestamp переповниться 19 січня 2038 року (проблема Y2K38). Багато систем досі використовують 32-бітні timestamps.

**Високосні секунди**: Unix timestamp ігнорує високосні секунди, що може призвести до розбіжностей з астрономічним часом.

**Читабельність**: число 1705123456 нічого не каже людині. Дебаг ускладнюється.

---

## ТЕОРІЯ: ЧАСОВІ ЗОНИ — ГЛИБША ПРОБЛЕМА

### Часова зона — це не просто число

Спрощене розуміння часової зони — це "зсув від UTC". Київ — UTC+2, Нью-Йорк — UTC-5. Але це неповна картина.

Часова зона — це набір правил, що визначають зсув для кожного моменту часу. Ці правила включають:

- Базовий зсув (стандартний час)
- Правила переходу на літній час (якщо є)
- Історичні зміни (зсув міг змінюватись)
- Політичні винятки

Україна, наприклад, мала різні правила DST у різні роки. До 1981 року літнього часу не було. З 1981 по 2011 переходи відбувались за європейськими правилами. У 2011 спробували скасувати переведення (залишившись на літньому часі), але у 2014 повернули. У 2022 через війну переведення призупинили.

Це означає, що для коректної роботи з часовими зонами потрібна база даних правил — IANA Time Zone Database (tzdata). Ця база оновлюється кілька разів на рік, коли країни змінюють свої правила.

### Ідентифікатори часових зон

Правильний спосіб ідентифікувати часову зону — використовувати IANA-ідентифікатор: "Europe/Kyiv", "America/New_York", "Asia/Tokyo".

Поширена помилка — використовувати абревіатури: "EST", "PST", "CET". Проблема в тому, що абревіатури неоднозначні: "CST" може означати Central Standard Time (США), China Standard Time, або Cuba Standard Time.

Ще гірше — зберігати лише зсув: "+02:00". Це не зберігає інформацію про DST! Час "14:00 +02:00" у січні та "14:00 +02:00" у липні — це різні UTC-моменти для зони з DST.

### Конвертація між зонами

```rust
use chrono::{DateTime, Utc, TimeZone};
use chrono_tz::Tz;

// Час у Києві
let kyiv: Tz = "Europe/Kyiv".parse().unwrap();
let kyiv_time = kyiv.with_ymd_and_hms(2024, 7, 15, 14, 30, 0).unwrap();

// Той самий момент у Нью-Йорку
let new_york: Tz = "America/New_York".parse().unwrap();
let ny_time = kyiv_time.with_timezone(&new_york);

println!("Київ: {}", kyiv_time);      // 2024-07-15 14:30:00 EEST
println!("Нью-Йорк: {}", ny_time);    // 2024-07-15 07:30:00 EDT
```

Зверніть увагу: різниця 7 годин, а не 8 (як було б без DST), бо обидві зони в літньому часі.

---

## ТЕОРІЯ: ЛІТНІЙ ЧАС (DST) — ДЖЕРЕЛО ПОМИЛОК

### Година, що зникла

При переході на літній час годинники переводяться вперед. У більшості країн це відбувається о 2:00 ночі, коли 2:00 стає 3:00.

Що станеться, якщо спробувати створити час "2:30" у цю дату?

```rust
use chrono::TimeZone;
use chrono_tz::Europe::Kyiv;

// Останні неділі березня о 3:00 годинники переводять вперед
// Припустимо, це 31 березня 2024

let result = Kyiv.with_ymd_and_hms(2024, 3, 31, 2, 30, 0);
// result буде Err або None — цей час не існує!
```

Час 2:30 у цю дату не існує. Бібліотека chrono повертає помилку. Деякі бібліотеки в інших мовах "тихо" коригують час до 3:30, що може бути несподіванкою.

### Година, що повторилась

При переході на зимовий час годинники переводяться назад. О 3:00 стає 2:00.

Час між 2:00 та 3:00 існує двічі: один раз перед переведенням (ще в літньому часі), другий — після (вже в зимовому).

```rust
use chrono::TimeZone;
use chrono_tz::Europe::Kyiv;

// Останні неділі жовтня о 4:00 годинники переводять назад
// Час 2:30 існує двічі!

// Перші 2:30 (ще літній час, EEST, UTC+3)
let first = Kyiv.with_ymd_and_hms(2024, 10, 27, 2, 30, 0)
    .earliest()  // Вибрати раніший варіант
    .unwrap();

// Другі 2:30 (вже зимовий час, EET, UTC+2)  
let second = Kyiv.with_ymd_and_hms(2024, 10, 27, 2, 30, 0)
    .latest()    // Вибрати пізніший варіант
    .unwrap();

// Різниця між ними — 1 година!
println!("{}", second.signed_duration_since(first).num_hours());  // 1
```

### Як це впливає на арифметику

Додавання "1 доби" до часу може дати несподіваний результат:

```rust
use chrono::{Duration, TimeZone};
use chrono_tz::Europe::Kyiv;

// 30 березня 2024, 12:00 (за день до переходу на літній час)
let before = Kyiv.with_ymd_and_hms(2024, 3, 30, 12, 0, 0).unwrap();

// Додаємо 24 години
let after_hours = before + Duration::hours(24);

// Додаємо 1 календарну добу
let after_day = before + Duration::days(1);

println!("До: {}", before);           // 12:00 EET (UTC+2)
println!("+24 год: {}", after_hours); // 13:00 EEST (UTC+3)
println!("+1 день: {}", after_day);   // 13:00 EEST (UTC+3)
```

Зачекайте, 12:00 + 24 години = 13:00? Так, бо в цю добу лише 23 години через перехід DST! Якщо ви хотіли "той самий час наступного дня" (12:00), потрібна інша логіка.

### Правила для роботи з DST

**Для точних інтервалів (таймери, таймаути)**: використовуйте UTC або Instant. Додавайте секунди/мілісекунди, не "години" чи "дні".

**Для календарних подій (зустрічі, рейси)**: зберігайте локальний час + часову зону. "Щоденна зустріч о 10:00 за київським часом" має бути о 10:00 незалежно від DST.

**Для історичних даних**: зберігайте UTC. Конвертуйте в локальний час лише для відображення.

---

## ТЕОРІЯ: АРИФМЕТИКА ДАТ — НЕОЧЕВИДНІ РЕЗУЛЬТАТИ

### Проблема: 31 січня + 1 місяць = ?

Що буде, якщо до 31 січня додати один місяць?

- 31 лютого? Такого дня не існує.
- 28/29 лютого? Останній день місяця.
- 3 березня? 31 день від 31 січня.

Різні бібліотеки роблять по-різному! Це джерело нескінченних багів.

```rust
use chrono::{NaiveDate, Months};

let jan_31 = NaiveDate::from_ymd_opt(2024, 1, 31).unwrap();

// chrono "насичує" до останнього валідного дня
let feb = jan_31.checked_add_months(Months::new(1)).unwrap();
println!("{}", feb);  // 2024-02-29 (бо 2024 високосний)

let mar = feb.checked_add_months(Months::new(1)).unwrap();
println!("{}", mar);  // 2024-03-29 (не 2024-03-31!)
```

Зверніть увагу на другий перехід: 29 лютого + 1 місяць = 29 березня, а не 31 березня. Якщо ви очікували "останній день місяця", результат неправильний.

### Різниця між "додати місяці" та "додати дні"

"Місяць" — це не фіксована кількість днів. Січень має 31 день, лютий — 28 або 29, квітень — 30.

```rust
use chrono::{NaiveDate, Duration, Months};

let date = NaiveDate::from_ymd_opt(2024, 1, 15).unwrap();

// Додати 1 місяць (календарно)
let plus_month = date.checked_add_months(Months::new(1)).unwrap();
println!("{}", plus_month);  // 2024-02-15

// Додати 30 днів
let plus_30 = date + Duration::days(30);
println!("{}", plus_30);     // 2024-02-14

// Додати 31 день (кількість днів у січні)
let plus_31 = date + Duration::days(31);
println!("{}", plus_31);     // 2024-02-15
```

Для 15 січня результати майже збігаються, але для граничних випадків (кінець місяця, лютий) різниця драматична.

### Високосні роки — ще один виняток

Правила високосних років складніші, ніж здається:

1. Рік високосний, якщо ділиться на 4
2. АЛЕ не високосний, якщо ділиться на 100
3. АЛЕ високосний, якщо ділиться на 400

Тому: 2000 — високосний (ділиться на 400), 1900 — не високосний (ділиться на 100, але не на 400), 2024 — високосний (ділиться на 4).

```rust
fn is_leap_year(year: i32) -> bool {
    (year % 4 == 0 && year % 100 != 0) || (year % 400 == 0)
}

assert!(is_leap_year(2000));   // true
assert!(!is_leap_year(1900));  // true (не високосний!)
assert!(is_leap_year(2024));   // true
```

Помилка з 1900 роком була причиною багів у багатьох системах. Excel досі вважає 1900 рік високосним (для сумісності з Lotus 1-2-3).

### 29 лютого + 1 рік = ?

```rust
use chrono::NaiveDate;

let leap_day = NaiveDate::from_ymd_opt(2024, 2, 29).unwrap();

// Що буде через рік?
let next_year = leap_day.with_year(2025);
// next_year = None! 29 лютого 2025 не існує
```

Люди, народжені 29 лютого, стикаються з цією проблемою щороку. Системи мають вирішувати: святкувати 28 лютого чи 1 березня?

---

## ТЕОРІЯ: INSTANT VS SYSTEMTIME

### Два різних види "часу"

Rust має два типи для роботи з часом у стандартній бібліотеці:

**std::time::Instant** — монотонний час. Гарантовано ніколи не йде назад. Ідеальний для вимірювання інтервалів.

**std::time::SystemTime** — системний час (wall clock). Може "стрибати" при синхронізації NTP. Представляє "реальний" час.

### Чому SystemTime може "стрибати"

Комп'ютерний годинник не ідеальний. Він поступово "втікає" від реального часу. Служба NTP (Network Time Protocol) періодично синхронізує годинник з серверами точного часу.

При синхронізації час може:
- Стрибнути вперед (годинник відставав)
- Стрибнути назад (годинник поспішав)
- Поступово коригуватись (slew)

Якщо ви виміряли час до і після операції через SystemTime, різниця може бути від'ємною!

```rust
use std::time::{SystemTime, Duration};
use std::thread;

let start = SystemTime::now();
thread::sleep(Duration::from_secs(1));
let end = SystemTime::now();

// Може бути Err, якщо під час sleep відбулась синхронізація назад!
match end.duration_since(start) {
    Ok(duration) => println!("Минуло: {:?}", duration),
    Err(e) => println!("Час пішов назад на {:?}", e.duration()),
}
```

### Instant — гарантована монотонність

Instant базується на монотонному годиннику операційної системи, який ніколи не коригується назад.

```rust
use std::time::Instant;
use std::thread;

let start = Instant::now();
thread::sleep(Duration::from_secs(1));
let elapsed = start.elapsed();  // Завжди >= 0

println!("Минуло: {:?}", elapsed);
```

Instant ідеальний для:
- Таймаутів
- Вимірювання продуктивності
- Ігрових циклів
- Будь-яких інтервалів

### Instant не можна серіалізувати

Instant — це "відносний" час, прив'язаний до моменту запуску системи. Його не можна зберегти у файл і прочитати на іншому комп'ютері або після перезавантаження.

```rust
use std::time::Instant;

let now = Instant::now();
// Неможливо: Instant не реалізує Serialize
// let json = serde_json::to_string(&now);
```

Для збереження часу використовуйте SystemTime або бібліотеки на кшталт chrono.

### Коли що використовувати

| Задача | Тип |
|--------|-----|
| Вимірювання тривалості операції | Instant |
| Таймаут | Instant |
| Логування події | SystemTime / chrono |
| Планування на майбутнє | chrono з timezone |
| Збереження в базу даних | UTC timestamp |
| Відображення користувачу | Локальний час |

---

## ТЕОРІЯ: ПРОБЛЕМА 2038 РОКУ (Y2K38)

### Що станеться 19 січня 2038?

Unix timestamp традиційно зберігається як 32-бітне знакове ціле число (i32). Максимальне значення i32 — 2147483647, що відповідає:

**19 січня 2038 року, 03:14:07 UTC**

Наступна секунда переповнить i32, і значення стане від'ємним. Системи, що інтерпретують timestamp як i32, "повернуться" в 1901 рік або поведуться непередбачувано.

### Чи актуальна ця проблема?

На жаль, так. Багато систем досі використовують 32-бітні timestamps:

- Вбудовані системи (мікроконтролери, IoT)
- Старі бази даних
- Бінарні протоколи та формати файлів
- Legacy-код

Якщо ваш БПЛА використовує 32-бітний мікроконтролер з 32-бітним часом — це потенційна проблема.

### Рішення

Сучасні системи використовують 64-бітні timestamps. В Rust:

```rust
use std::time::{SystemTime, UNIX_EPOCH};

let now = SystemTime::now();
let timestamp = now.duration_since(UNIX_EPOCH).unwrap().as_secs();

// timestamp — це u64, достатньо до року 292277026596
println!("Timestamp: {}", timestamp);
```

При роботі з зовнішніми системами перевіряйте, який формат часу вони використовують.

---

## ТЕОРІЯ: ВИСОКОСНІ СЕКУНДИ

### Що це таке

Обертання Землі поступово сповільнюється. Астрономічна доба стає трохи довшою. Щоб атомний час (UTC) не розходився з астрономічним, періодично додаються високосні секунди.

Високосна секунда — це 23:59:60. Хвилина має 61 секунду замість 60.

З 1972 року було додано 27 високосних секунд. Останню додали 31 грудня 2016 року.

### Як це впливає на програми

Більшість систем ігнорують високосні секунди. Unix timestamp "повторює" останню секунду або "розмазує" її по сусідніх секундах (leap smear).

Проблеми виникають у системах, що потребують дуже точного часу:
- Наукові вимірювання
- Фінансові транзакції (високочастотний трейдинг)
- GPS та супутникова навігація
- Координація розподілених систем

```rust
// chrono підтримує високосні секунди
use chrono::NaiveTime;

// Звичайний час
let normal = NaiveTime::from_hms_opt(23, 59, 59).unwrap();

// Високосна секунда
let leap = NaiveTime::from_hms_opt(23, 59, 60);
// leap = Some(...) якщо бібліотека підтримує
```

### Практична порада

Для більшості застосувань високосні секунди можна ігнорувати. Системи NTP та операційні системи обробляють їх автоматично. Проблеми виникають лише при роботі з астрономічним часом або при синхронізації систем з різною обробкою високосних секунд.

---

## ТЕОРІЯ: ФОРМАТИ СЕРІАЛІЗАЦІЇ ЧАСУ

### ISO 8601 — стандарт

ISO 8601 — це міжнародний стандарт представлення дати та часу:

```
2024-01-15T14:30:00Z           # UTC
2024-01-15T14:30:00+02:00      # Зі зсувом
2024-01-15T14:30:00.123Z       # З мілісекундами
2024-01-15                      # Лише дата
14:30:00                        # Лише час
```

Переваги:
- Однозначний (на відміну від "01/02/2024" — це 1 лютого чи 2 січня?)
- Сортується лексикографічно
- Широко підтримується

### RFC 3339 — підмножина ISO 8601

RFC 3339 — це профіль ISO 8601 для інтернет-протоколів. Він більш строгий:

```
2024-01-15T14:30:00Z
2024-01-15T14:30:00+02:00
```

Завжди використовуйте "T" як роздільник (не пробіл), завжди вказуйте часову зону.

### Серіалізація в Rust з chrono

```rust
use chrono::{DateTime, Utc, Local};

let utc: DateTime<Utc> = Utc::now();

// ISO 8601 / RFC 3339
println!("{}", utc.to_rfc3339());
// 2024-01-15T12:30:00.123456789+00:00

// Власний формат
println!("{}", utc.format("%Y-%m-%d %H:%M:%S"));
// 2024-01-15 12:30:00

// Timestamp
println!("{}", utc.timestamp());
// 1705322400
```

### Небезпечні формати

Уникайте форматів без часової зони:

```rust
// Погано: незрозуміло яка зона
"2024-01-15 14:30:00"

// Погано: неоднозначний порядок
"01/15/2024"  // США
"15/01/2024"  // Європа

// Добре: явна зона
"2024-01-15T14:30:00+02:00"
"2024-01-15T12:30:00Z"
```

---

## ПРАКТИЧНІ РЕКОМЕНДАЦІЇ

### Правило 1: Зберігайте час у UTC

```rust
use chrono::Utc;

// В базу даних, файл, API — завжди UTC
let event_time = Utc::now();
database.insert("event_time", event_time);
```

### Правило 2: Конвертуйте в локальний лише для відображення

```rust
use chrono::{Local, Utc, TimeZone};
use chrono_tz::Tz;

let utc_time = Utc::now();

// Для UI — локальний час користувача
let user_tz: Tz = "Europe/Kyiv".parse().unwrap();
let display_time = utc_time.with_timezone(&user_tz);
println!("Ваш час: {}", display_time.format("%d.%m.%Y %H:%M"));
```

### Правило 3: Для інтервалів використовуйте Instant

```rust
use std::time::Instant;

let start = Instant::now();
perform_operation();
let elapsed = start.elapsed();

println!("Операція зайняла {:?}", elapsed);
```

### Правило 4: Обробляйте неіснуючий та подвійний час

```rust
use chrono::TimeZone;
use chrono_tz::Europe::Kyiv;

fn safe_local_datetime(year: i32, month: u32, day: u32, hour: u32, min: u32) 
    -> Result<DateTime<Tz>, TimeError> 
{
    match Kyiv.with_ymd_and_hms(year, month, day, hour, min, 0) {
        LocalResult::Single(dt) => Ok(dt),
        LocalResult::Ambiguous(early, late) => {
            // Вибираємо раніший варіант або повертаємо помилку
            log::warn!("Ambiguous time, using earlier");
            Ok(early)
        },
        LocalResult::None => {
            Err(TimeError::NonexistentTime)
        }
    }
}
```

### Правило 5: Не робіть арифметику з "місяцями" без перевірки

```rust
use chrono::{NaiveDate, Months};

fn add_months_safe(date: NaiveDate, months: u32) -> Option<NaiveDate> {
    date.checked_add_months(Months::new(months))
}

// Завжди перевіряйте результат
if let Some(new_date) = add_months_safe(date, 1) {
    // Використовуємо new_date
} else {
    // Обробка помилки (overflow)
}
```

### Правило 6: У логах та API використовуйте ISO 8601

```rust
use chrono::Utc;

let now = Utc::now();
log::info!("Event at {}", now.to_rfc3339());
// Event at 2024-01-15T12:30:00.123456789+00:00
```

---

## Застосування до рою БПЛА

### Синхронізація часу в рої

```rust
use std::time::{Instant, Duration};
use chrono::{DateTime, Utc};

struct DroneTimeSync {
    /// Монотонний час для внутрішніх операцій
    boot_instant: Instant,
    
    /// UTC-час останньої синхронізації
    last_sync_utc: DateTime<Utc>,
    
    /// Instant останньої синхронізації
    last_sync_instant: Instant,
}

impl DroneTimeSync {
    /// Отримати поточний UTC-час без звернення до системного годинника
    fn estimated_utc(&self) -> DateTime<Utc> {
        let elapsed = self.last_sync_instant.elapsed();
        self.last_sync_utc + chrono::Duration::from_std(elapsed).unwrap()
    }
    
    /// Синхронізуватись з командним центром
    fn sync(&mut self, server_utc: DateTime<Utc>) {
        self.last_sync_utc = server_utc;
        self.last_sync_instant = Instant::now();
    }
    
    /// Час для логування (Instant для монотонності)
    fn mission_elapsed(&self) -> Duration {
        self.boot_instant.elapsed()
    }
}
```

### Планування місії з урахуванням часових зон

```rust
use chrono::{DateTime, Utc, Duration};
use chrono_tz::Tz;

struct Mission {
    id: MissionId,
    /// Завжди зберігаємо в UTC!
    start_time_utc: DateTime<Utc>,
    duration: Duration,
    timezone_for_display: Tz,
}

impl Mission {
    fn new(
        id: MissionId,
        local_time: DateTime<Tz>,  // Оператор вводить локальний час
        duration: Duration,
        tz: Tz,
    ) -> Self {
        Self {
            id,
            start_time_utc: local_time.with_timezone(&Utc),  // Зберігаємо UTC
            duration,
            timezone_for_display: tz,
        }
    }
    
    fn display_start_time(&self) -> String {
        let local = self.start_time_utc.with_timezone(&self.timezone_for_display);
        local.format("%d.%m.%Y %H:%M %Z").to_string()
    }
    
    fn is_time_to_start(&self) -> bool {
        Utc::now() >= self.start_time_utc
    }
    
    fn time_until_start(&self) -> Option<Duration> {
        let now = Utc::now();
        if now < self.start_time_utc {
            Some((self.start_time_utc - now).to_std().unwrap())
        } else {
            None
        }
    }
}
```

### Логування з мітками часу

```rust
use chrono::Utc;
use std::time::Instant;

struct FlightLogger {
    mission_start: Instant,
    log_entries: Vec<LogEntry>,
}

struct LogEntry {
    /// UTC для кореляції з зовнішніми системами
    utc_time: chrono::DateTime<Utc>,
    /// Час від початку місії для внутрішнього аналізу
    mission_time_ms: u64,
    event: String,
}

impl FlightLogger {
    fn new() -> Self {
        Self {
            mission_start: Instant::now(),
            log_entries: Vec::new(),
        }
    }
    
    fn log(&mut self, event: impl Into<String>) {
        self.log_entries.push(LogEntry {
            utc_time: Utc::now(),
            mission_time_ms: self.mission_start.elapsed().as_millis() as u64,
            event: event.into(),
        });
    }
    
    fn export_csv(&self) -> String {
        let mut csv = String::from("utc_time,mission_time_ms,event\n");
        for entry in &self.log_entries {
            csv.push_str(&format!(
                "{},{},{}\n",
                entry.utc_time.to_rfc3339(),
                entry.mission_time_ms,
                entry.event
            ));
        }
        csv
    }
}
```

---

## Резюме

У цьому розділі ми розглянули підступні аспекти роботи з часом і датами.

**Час — соціальна конструкція** з часовими зонами, DST та календарними винятками. Зберігайте час у UTC, конвертуйте в локальний лише для відображення.

**DST створює неіснуючі та подвійні години**. При переході на літній час година зникає, при переході на зимовий — повторюється. Завжди обробляйте ці випадки явно.

**Арифметика дат неочевидна**. "31 січня + 1 місяць" не має однозначної відповіді. Перевіряйте результати операцій з датами.

**Instant vs SystemTime**: Instant — монотонний, ніколи не йде назад, ідеальний для вимірювання інтервалів. SystemTime може "стрибати" при синхронізації.

**Проблема 2038**: 32-бітні Unix timestamps переповняться 19 січня 2038 року. Використовуйте 64-бітні типи.

**Формати серіалізації**: завжди використовуйте ISO 8601 / RFC 3339 з явною часовою зоною. Уникайте неоднозначних форматів.

---

## 🔗 Зв'язок з наступним матеріалом

Опанувавши підступності часу та дат, ви готові до вивчення проблем обробки помилок. Там вас чекають свої сюрпризи: unwrap() як бомба уповільненої дії, втрата контексту помилки при конвертації, та паніка у drop().
