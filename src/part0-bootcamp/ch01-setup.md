# ЧАСТИНА 0: ІМПЕРАТИВНИЙ RUST (BOOTCAMP)

---

# Розділ 1: Налаштування середовища та перший проєкт

---

## 📋 Анотація

Цей розділ — ваша відправна точка у світ Rust та мультиагентних систем. Ми почнемо з найголовнішого: встановимо всі необхідні інструменти та переконаємось, що вони працюють. Ви познайомитесь із rustup — менеджером версій Rust, cargo — системою збірки, яка стане вашим найкращим другом на найближчі місяці, та створите свій перший проєкт. Цей проєкт буде скромним — лише кілька рядків коду — але він закладе фундамент для всього, що ми будуватимемо далі.

---

## 🎯 Цілі навчання

Після завершення цього розділу ви зможете:

1. **Встановити Rust** на вашу операційну систему
2. **Пояснити роль** трьох основних інструментів: rustup, rustc, cargo
3. **Створити новий проєкт** за допомогою команди `cargo new`
4. **Розібрати структуру** типового Rust-проєкту
5. **Скомпілювати та запустити** програму
6. **Використовувати макрос** `println!` для виводу
7. **Пояснити концепцію агента** як автономної програми

---

## 📚 Ключові терміни

| Термін | Визначення |
|--------|------------|
| **rustup** | Офіційний інструмент для встановлення та керування версіями Rust |
| **rustc** | Компілятор мови Rust, що перетворює вихідний код на машинний |
| **cargo** | Система збірки та менеджер пакетів для Rust |
| **crate** | Одиниця компіляції в Rust; може бути бібліотекою або виконуваним файлом |
| **Cargo.toml** | Маніфест проєкту, що описує залежності та метадані |
| **макрос** | Код, що генерує інший код під час компіляції |
| **агент** | Автономна сутність, здатна сприймати середовище та діяти в ньому |

---

## 1.1 ТЕОРІЯ

### 1.1.1 Що таке Rust і чому він особливий

Перш ніж встановлювати будь-які інструменти, давайте зрозуміємо, **чому** ми обираємо саме Rust для створення мультиагентних систем.

#### Проблема, яку вирішує Rust

Уявіть, що ви розробляєте систему керування роєм дронів. Кожен дрон — це автономний агент, який приймає рішення в реальному часі. Що станеться, якщо у вашому коді є помилка роботи з пам'яттю?

**У мовах без збирача сміття (C, C++):**
- Дрон може звернутися до вже звільненої пам'яті
- Програма може перезаписати критичні дані
- У найгіршому випадку — дрон втрачає контроль і падає

**У мовах зі збирачем сміття (Java, Python, Go):**
- Збирач сміття може "заморозити" програму на кілька мілісекунд
- Ці мілісекунди можуть бути критичними для маневру уникнення
- Непередбачуваність latency робить систему ненадійною

**Rust пропонує третій шлях:**
- Компілятор **аналізує ваш код** і не дозволяє зробити помилки пам'яті
- Якщо код скомпілювався — він **гарантовано безпечний**
- Немає збирача сміття — **немає непередбачуваних пауз**
- Продуктивність на рівні C/C++

#### Ціна цієї безпеки

Rust не дає нічого безкоштовно. Ціна безпеки — **суворий компілятор**. Він буде відхиляти код, який інші мови пропустили б без питань. Спочатку це дратує. Ви будете бачити помилки на кшталт:

```text
error[E0382]: borrow of moved value: `data`
```

Але з часом ви зрозумієте: кожна така помилка — це баг, який **не потрапить у production**. Компілятор Rust — це не ворог, а найсуворіший code reviewer, який ніколи не втомлюється.

### 1.1.2 Тріада інструментів: rustup, rustc, cargo

Екосистема Rust побудована на трьох основних інструментах. Розуміння їхньої ролі критично важливе.

#### rustup — менеджер версій

**Що це таке?**

rustup — це програма, яка встановлює і керує версіями Rust на вашому комп'ютері. Rust розвивається швидко: нова стабільна версія виходить кожні 6 тижнів. Крім того, існують:

- **stable** — стабільна версія для production
- **beta** — версія для тестування перед стабільним релізом
- **nightly** — щонічні збірки з експериментальними можливостями

**Навіщо це потрібно?**

Уявіть ситуацію: ви працюєте над двома проєктами. Один використовує Rust 1.70, інший потребує Rust 1.75. Без rustup вам довелося б постійно перевстановлювати компілятор. З rustup — одна команда:

```bash
rustup default 1.70.0   # Для першого проєкту
rustup default 1.75.0   # Для другого проєкту
```

**Ключові команди:**

| Команда | Що робить |
|---------|-----------|
| `rustup update` | Оновлює Rust до останньої версії |
| `rustup show` | Показує встановлені версії |
| `rustup default stable` | Встановлює стабільну версію за замовчуванням |

#### rustc — компілятор

**Що це таке?**

rustc (Rust Compiler) — це програма, яка перетворює ваш вихідний код (файли `.rs`) на виконуваний машинний код. Це "серце" мови Rust.

**Як він працює?**

Коли ви пишете код на Rust, rustc виконує кілька етапів:

1. **Лексичний аналіз** — розбиває текст на токени (слова, символи)
2. **Синтаксичний аналіз** — будує дерево структури програми
3. **Семантичний аналіз** — перевіряє типи, ownership, borrowing
4. **Генерація LLVM IR** — перетворює на проміжний код
5. **Оптимізація та генерація машинного коду**

На етапі 3 відбувається "магія" Rust — перевірка безпеки пам'яті.

**Чому ви рідко використовуватимете rustc напряму?**

Теоретично можна компілювати так:

```bash
rustc main.rs
```

Але реальні проєкти мають десятки файлів і зовнішні залежності. Керувати цим вручну — кошмар. Тому існує cargo.

#### cargo — ваш найкращий друг

**Що це таке?**

cargo — це одночасно:
- **Система збірки** — компілює ваш проєкт
- **Менеджер пакетів** — завантажує залежності
- **Інструмент тестування** — запускає тести
- **Генератор документації** — створює HTML-документацію

**Чому cargo такий важливий?**

cargo вирішує проблему, яка мучить розробників C/C++ десятиліттями: **відтворюваність збірки**. Коли ви клонуєте проєкт з GitHub, вам достатньо виконати:

```bash
cargo build
```

І cargo автоматично:
1. Завантажить усі залежності правильних версій
2. Скомпілює їх у правильному порядку
3. Скомпілює ваш проєкт

**Ключові команди cargo:**

| Команда | Що робить | Коли використовувати |
|---------|-----------|---------------------|
| `cargo new назва` | Створює новий проєкт | На початку нового проєкту |
| `cargo build` | Компілює проєкт | Коли потрібен лише бінарник |
| `cargo run` | Компілює та запускає | Для швидкого тестування |
| `cargo check` | Перевіряє код без компіляції | Для швидкої перевірки помилок |
| `cargo test` | Запускає тести | Перед commit'ом |
| `cargo doc --open` | Генерує документацію | Для вивчення API |

**Різниця між `cargo build` та `cargo check`:**

`cargo check` набагато швидший, бо пропускає генерацію машинного коду. Він лише перевіряє, чи код правильний. Використовуйте `cargo check` під час розробки, `cargo build` — коли потрібен готовий бінарник.

### 1.1.3 Анатомія Rust-проєкту

Коли ви створюєте проєкт командою `cargo new`, cargo генерує стандартну структуру каталогів. Розуміння цієї структури критично важливе.

#### Структура каталогів

```text
my_project/
├── Cargo.toml      ← Маніфест проєкту (метадані, залежності)
├── Cargo.lock      ← Зафіксовані версії (з'являється після збірки)
├── src/
│   └── main.rs     ← Вихідний код (точка входу)
└── target/         ← Скомпільовані файли (з'являється після збірки)
```

Давайте розберемо кожен елемент детально.

#### Cargo.toml — маніфест проєкту

Це найважливіший файл у проєкті. Він написаний у форматі TOML (Tom's Obvious Minimal Language) — простому форматі конфігурації.

**Приклад Cargo.toml:**

```toml
[package]
name = "my_agent"
version = "0.1.0"
edition = "2021"

[dependencies]
```

**Розбір кожного рядка:**

| Рядок | Пояснення |
|-------|-----------|
| `[package]` | Секція з метаданими проєкту |
| `name = "my_agent"` | Назва проєкту (використовується для бінарника) |
| `version = "0.1.0"` | Версія у форматі semver (major.minor.patch) |
| `edition = "2021"` | Редакція Rust (визначає синтаксис та можливості) |
| `[dependencies]` | Секція залежностей (поки порожня) |

**Що таке "edition"?**

Edition — це спосіб Rust еволюціонувати без зламу старого коду. Кожні 3 роки виходить нова редакція:
- **2015** — перша стабільна версія
- **2018** — покращення синтаксису модулів
- **2021** — нові можливості форматування, покращення замикань

Важливо: код різних редакцій може працювати разом! Залежність з edition = "2018" працюватиме у проєкті з edition = "2021".

#### src/main.rs — точка входу

Цей файл містить функцію `main()` — точку входу програми. Коли ви запускаєте програму, виконання починається саме тут.

**Мінімальний main.rs:**

```rust
fn main() {
    println!("Hello, world!");
}
```

**Розбір синтаксису:**

```rust
fn main() {
//│  │   │
//│  │   └── Порожні дужки — функція не приймає аргументів
//│  └────── Назва функції (main — спеціальна, точка входу)
//└───────── Ключове слово для оголошення функції

    println!("Hello, world!");
//  │      │ │             │
//  │      │ └─────────────┴── Аргумент макросу — рядок тексту
//  │      └── Знак оклику означає, що це МАКРОС, не функція
//  └── Назва макросу для виводу тексту

}
// Закриваюча дужка — кінець тіла функції
```

**Чому println! — макрос, а не функція?**

Це важливе питання. У Rust звичайна функція не може приймати довільну кількість аргументів різних типів. Але println! може:

```rust
println!("Один аргумент");
println!("Два: {} {}", x, y);
println!("Три: {} {} {}", a, b, c);
```

Макрос — це код, який генерує інший код під час компіляції. println! аналізує рядок форматування і генерує правильний код для кожного випадку.

#### target/ — результати компіляції

Цей каталог з'являється після першої компіляції. Усередині:

```text
target/
├── debug/           ← Збірка для розробки
│   └── my_agent     ← Ваш виконуваний файл
└── release/         ← Оптимізована збірка (cargo build --release)
```

**Чому існують дві збірки?**

| Тип збірки | Компіляція | Швидкість програми | Використання |
|------------|------------|-------------------|--------------|
| **debug** | Швидка (~2 сек) | Повільніша | Розробка |
| **release** | Повільна (~30 сек) | Максимальна | Production |

Під час розробки використовуйте debug — швидша ітерація важливіша. Для production — release.

### 1.1.4 Макрос println! детально

Оскільки println! буде вашим основним інструментом дебагу протягом усього курсу, розберемо його детально.

#### Базовий синтаксис

```rust
println!("Текст");
```

Це просто виводить текст і переходить на новий рядок. Якщо потрібно вивести без переходу на новий рядок — використовуйте `print!`:

```rust
print!("Без переходу...");
print!(" продовження");
println!(" кінець");
// Вивід: Без переходу... продовження кінець
```

#### Плейсхолдери {}

Фігурні дужки `{}` — це плейсхолдер, який заміняється значенням:

```rust
let name = "Агент-007";
println!("Ідентифікатор: {}", name);
// Вивід: Ідентифікатор: Агент-007
```

**Як це працює?**

Макрос println! під час компіляції:
1. Аналізує рядок форматування
2. Рахує кількість плейсхолдерів `{}`
3. Перевіряє, чи вистачає аргументів
4. Генерує код для виводу кожного значення

Якщо кількість плейсхолдерів не збігається з кількістю аргументів — помилка компіляції:

```rust
println!("{} {}", x);  // Помилка! Два плейсхолдери, один аргумент
```

#### Кілька значень

```rust
let x = 10;
let y = 20;
let z = 30;
println!("Координати: x={}, y={}, z={}", x, y, z);
// Вивід: Координати: x=10, y=20, z=30
```

Значення підставляються **по порядку**: перше `{}` отримує x, друге — y, третє — z.

#### Іменовані аргументи

Rust 2021 дозволяє вставляти змінні напряму в рядок:

```rust
let agent = "SCOUT-001";
let status = "активний";
println!("Агент {agent} зараз {status}");
// Вивід: Агент SCOUT-001 зараз активний
```

Це зручніше, ніж нумеровані плейсхолдери, особливо коли багато змінних.

#### Форматування чисел

```rust
let pi = 3.14159265358979;

println!("Без форматування: {}", pi);
// Вивід: Без форматування: 3.14159265358979

println!("Два знаки: {:.2}", pi);
// Вивід: Два знаки: 3.14

println!("Чотири знаки: {:.4}", pi);
// Вивід: Чотири знаки: 3.1416
```

**Синтаксис `{:.N}`:**
- `:` — початок специфікації формату
- `.N` — кількість знаків після коми

#### Вирівнювання

```rust
let name = "Агент";

println!("|{:<10}|", name);  // Ліворуч
// Вивід: |Агент     |

println!("|{:>10}|", name);  // Праворуч
// Вивід: |     Агент|

println!("|{:^10}|", name);  // По центру
// Вивід: |  Агент   |
```

**Синтаксис `{:XN}`:**
- `X` — символ вирівнювання (`<` ліворуч, `>` праворуч, `^` центр)
- `N` — загальна ширина поля

### 1.1.5 Концепція агента: перший погляд

Перш ніж писати код агента, потрібно зрозуміти, що таке агент концептуально.

#### Визначення агента

**Агент** — це автономна програмна сутність, яка:

1. **Сприймає** своє середовище (через сенсори або дані)
2. **Обробляє** інформацію (приймає рішення)
3. **Діє** на середовище (виконує команди)
4. **Працює автономно** (без постійного зовнішнього керування)

#### Цикл роботи агента

```text
    ┌─────────────────────────────────────────┐
    │              СЕРЕДОВИЩЕ                 │
    └───────────────┬─────────────────────────┘
                    │ сприйняття
                    ▼
    ┌─────────────────────────────────────────┐
    │              СЕНСОРИ                    │
    │     (камера, GPS, радар, дані)          │
    └───────────────┬─────────────────────────┘
                    │
                    ▼
    ┌─────────────────────────────────────────┐
    │         ОБРОБКА / РІШЕННЯ               │
    │   (аналіз даних, планування дій)        │
    └───────────────┬─────────────────────────┘
                    │
                    ▼
    ┌─────────────────────────────────────────┐
    │            АКТУАТОРИ                    │
    │    (мотори, команди, повідомлення)      │
    └───────────────┬─────────────────────────┘
                    │ дія
                    ▼
    ┌─────────────────────────────────────────┐
    │              СЕРЕДОВИЩЕ                 │
    └─────────────────────────────────────────┘
```

Цей цикл повторюється безперервно, поки агент працює.

#### Наш перший "агент"

На цьому етапі наш "агент" буде дуже примітивним — просто програма, що виводить своє ім'я та статус. Але концептуально вона вже демонструє ключові елементи:

- **Ідентичність** — агент має унікальний ідентифікатор
- **Стан** — агент знає свій поточний статус
- **Дія** — агент повідомляє про себе (вивід у консоль)

У наступних розділах ми додамо:
- Змінні для зберігання стану (Розділ 2)
- Функції для поведінки (Розділ 3)
- Цикли для безперервної роботи (Розділ 4)
- Структури для організації даних (Розділ 5)

---

## 1.2 ПРАКТИКА

### 1.2.1 Встановлення Rust

#### Крок 1: Встановлення rustup

**На Linux та macOS:**

Відкрийте термінал і виконайте команду:

```bash
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
```

**Що робить ця команда?**

Давайте розберемо по частинах:

| Частина | Пояснення |
|---------|-----------|
| `curl` | Завантажує файл з інтернету |
| `--proto '=https'` | Використовувати тільки HTTPS (безпечно) |
| `--tlsv1.2` | Мінімальна версія TLS (безпека) |
| `-sSf` | s=silent, S=show errors, f=fail on HTTP errors |
| `https://sh.rustup.rs` | Офіційний скрипт встановлення |
| `\| sh` | Передати завантажений скрипт оболонці для виконання |

Скрипт запитає, який тип встановлення ви хочете. Виберіть опцію 1 (default).

**На Windows:**

1. Завантажте rustup-init.exe з https://rustup.rs
2. Запустіть завантажений файл
3. Дотримуйтесь інструкцій на екрані

**Примітка для Windows:** Вам може знадобитися встановити Visual Studio Build Tools. Інсталятор підкаже, якщо це потрібно.

#### Крок 2: Перезавантаження терміналу

Після встановлення **закрийте і відкрийте термінал заново**. Це потрібно, щоб нові змінні середовища (PATH) набули чинності.

#### Крок 3: Перевірка встановлення

Виконайте наступні команди:

```bash
rustc --version
```

Очікуваний результат (версія може відрізнятися):
```bash
rustc 1.75.0 (82e1608df 2023-12-21)
```

```bash
cargo --version
```

Очікуваний результат:
```bash
cargo 1.75.0 (1d8b05cdd 2023-11-20)
```

```bash
rustup --version
```

Очікуваний результат:
```rust
rustup 1.26.0 (5af9b9484 2023-04-05)
```

**Якщо команди не знайдено:**

Переконайтесь, що ви перезапустили термінал. Якщо проблема залишається:

```bash
# На Linux/macOS додайте до ~/.bashrc або ~/.zshrc:
source "$HOME/.cargo/env"
```

### 1.2.2 Створення першого проєкту

#### Крок 1: Створення проєкту

Виберіть каталог для ваших проєктів і виконайте:

```bash
cargo new hello_agent
```

**Що відбувається при виконанні цієї команди?**

cargo виконує кілька дій:

1. **Створює каталог** `hello_agent/`
2. **Ініціалізує Git-репозиторій** (якщо Git встановлено)
3. **Створює Cargo.toml** з метаданими проєкту
4. **Створює src/main.rs** з шаблонним кодом
5. **Створює .gitignore** (ігнорує target/)

Очікуваний вивід:
```rust
     Created binary (application) `hello_agent` package
```

#### Крок 2: Перехід у каталог проєкту

```bash
cd hello_agent
```

#### Крок 3: Огляд структури

```bash
ls -la
```

Очікуваний результат:
```text
total 16
drwxr-xr-x  4 user user  128 Dec 29 10:00 .
drwxr-xr-x 10 user user  320 Dec 29 10:00 ..
-rw-r--r--  1 user user    8 Dec 29 10:00 .gitignore
drwxr-xr-x  7 user user  224 Dec 29 10:00 .git
-rw-r--r--  1 user user  186 Dec 29 10:00 Cargo.toml
drwxr-xr-x  3 user user   96 Dec 29 10:00 src
```

### 1.2.3 Дослідження створених файлів

#### Файл Cargo.toml

Відкрийте Cargo.toml у вашому редакторі:

```toml
[package]
name = "hello_agent"
version = "0.1.0"
edition = "2021"

[dependencies]
```

**Детальний розбір:**

**Секція [package]:**

```toml
name = "hello_agent"
```
Назва вашого проєкту. Вона буде використовуватися:
- Як назва виконуваного файлу (hello_agent або hello_agent.exe)
- Як назва пакету при публікації на crates.io

```toml
version = "0.1.0"
```
Версія у форматі Semantic Versioning:
- **0** — major версія (несумісні зміни API)
- **1** — minor версія (нова функціональність, сумісна)
- **0** — patch версія (виправлення багів)

Версія 0.x.x означає "pre-release" — API може змінюватись.

```toml
edition = "2021"
```
Редакція Rust. Це визначає, які мовні можливості доступні. Завжди використовуйте найновішу редакцію для нових проєктів.

**Секція [dependencies]:**

Поки порожня. Сюди додаються зовнішні бібліотеки:

```toml
[dependencies]
serde = "1.0"        # Серіалізація
tokio = "1.0"        # Асинхронність
```

#### Файл src/main.rs

Відкрийте src/main.rs:

```rust
fn main() {
    println!("Hello, world!");
}
```

Це мінімальна працююча програма на Rust. Давайте розберемо кожен елемент.

**Рядок 1: `fn main() {`**

- `fn` — ключове слово для оголошення функції (скорочення від "function")
- `main` — назва функції. `main` — спеціальна назва: це точка входу програми
- `()` — параметри функції. Порожні дужки означають "без параметрів"
- `{` — початок тіла функції

**Рядок 2: `println!("Hello, world!");`**

- `println!` — макрос для виводу тексту з переходом на новий рядок
- `!` — знак оклику вказує, що це макрос, не звичайна функція
- `("Hello, world!")` — аргумент макросу: рядок для виводу
- `;` — крапка з комою завершує statement (інструкцію)

**Рядок 3: `}`**

- Закриваюча дужка — кінець тіла функції

### 1.2.4 Компіляція та запуск

#### Спосіб 1: cargo run (рекомендовано)

```bash
cargo run
```

Ця команда робить дві речі:
1. Компілює проєкт (якщо потрібно)
2. Запускає скомпільований бінарник

Очікуваний вивід:
```text
   Compiling hello_agent v0.1.0 (/path/to/hello_agent)
    Finished dev [unoptimized + debuginfo] target(s) in 0.50s
     Running `target/debug/hello_agent`
Hello, world!
```

**Розбір виводу:**

| Рядок | Пояснення |
|-------|-----------|
| `Compiling hello_agent v0.1.0` | cargo компілює проєкт |
| `Finished dev [unoptimized + debuginfo]` | Завершено debug-збірку |
| `Running target/debug/hello_agent` | Запускається бінарник |
| `Hello, world!` | Вивід вашої програми |

#### Спосіб 2: cargo build + ручний запуск

```bash
cargo build
```

Вивід:
```text
   Compiling hello_agent v0.1.0 (/path/to/hello_agent)
    Finished dev [unoptimized + debuginfo] target(s) in 0.50s
```

Тепер запустіть бінарник вручну:

```bash
./target/debug/hello_agent
```

Вивід:
```text
Hello, world!
```

**Коли використовувати який спосіб?**

- `cargo run` — для швидкого тестування під час розробки
- `cargo build` — коли потрібен лише бінарник (для передачі іншим)

### 1.2.5 Експерименти з println!

Тепер давайте поекспериментуємо з макросом println!. Замініть вміст src/main.rs:

```rust
fn main() {
    // Експеримент 1: Простий текст
    // Просто виводимо рядок. Це найпростіше використання println!
    println!("Привіт, світе!");
    
    // Експеримент 2: Порожній рядок
    // println! без аргументів виводить лише перехід на новий рядок
    // Це корисно для візуального розділення виводу
    println!();
    
    // Експеримент 3: Змінна та плейсхолдер
    // Створюємо змінну (let) і виводимо її значення через {}
    let language = "Rust";
    println!("Ми вивчаємо: {}", language);
    
    // Експеримент 4: Кілька змінних
    // Можна використовувати кілька плейсхолдерів
    // Значення підставляються по порядку
    let year = 2015;
    let creator = "Mozilla Research";
    println!("{} створено у {} році компанією {}", language, year, creator);
    
    // Експеримент 5: Вбудовані змінні (Rust 2021+)
    // Можна вставляти змінні прямо в рядок без {}
    let version = "1.75";
    println!("Поточна версія: {version}");
    
    // Експеримент 6: Обчислення
    // Всередині {} можна виконувати прості обчислення
    println!("2 + 2 = {}", 2 + 2);
    println!("10 * 5 = {}", 10 * 5);
    
    // Експеримент 7: Форматування чисел
    // {:.N} — виводить N знаків після коми
    let pi = 3.14159265358979;
    println!("π = {:.2}", pi);  // 3.14
    println!("π = {:.5}", pi);  // 3.14159
}
```

Запустіть і подивіться результат:

```bash
cargo run
```

Очікуваний вивід:
```rust
Привіт, світе!

Ми вивчаємо: Rust
Rust створено у 2015 році компанією Mozilla Research
Поточна версія: 1.75
2 + 2 = 4
10 * 5 = 50
π = 3.14
π = 3.14159
```

**Що ви дізналися з цих експериментів:**

1. `println!` виводить текст і переходить на новий рядок
2. `{}` — плейсхолдер для значень
3. Змінні оголошуються через `let`
4. У Rust 2021+ можна вставляти змінні як `{variable_name}`
5. `{:.N}` форматує числа з N знаками після коми

### 1.2.6 Застосування до наскрізного проєкту: Hello, Agent!

Тепер створимо нашого першого "агента". Це ще не справжній агент, але він закладає основу для всього, що ми будуватимемо далі.

Замініть вміст src/main.rs:

```rust
// =============================================================================
// НАСКРІЗНИЙ ПРОЄКТ: Інтелектуальний автономний рій БПЛА
// Розділ 1: Перший агент
// =============================================================================
//
// Цей код демонструє найпростішу форму агента — програму, яка:
// 1. Має ідентичність (унікальний ID)
// 2. Має стан (тип, статус)
// 3. Виконує дію (виводить інформацію про себе)
//
// У наступних розділах ми додамо:
// - Змінні для зберігання позиції та батареї (Розділ 2)
// - Функції для поведінки (Розділ 3)
// - Цикли для безперервної роботи (Розділ 4)
// - Структури даних (Розділ 5)
// =============================================================================

fn main() {
    // =========================================================================
    // ІДЕНТИЧНІСТЬ АГЕНТА
    // =========================================================================
    // Кожен агент у рої має унікальний ідентифікатор.
    // Формат: ТИП-НОМЕР (наприклад, SCOUT-001 = розвідник номер 001)
    // У реальній системі ID генерується автоматично.
    
    let agent_id = "SCOUT-001";
    
    // =========================================================================
    // ТИП АГЕНТА
    // =========================================================================
    // Наш рій складається з різних типів агентів:
    // - Розвідник (Scout) — пошук та виявлення
    // - Бойовий (Combat) — ураження цілей
    // - Зв'язківець (Comm) — ретрансляція даних
    // - Транспорт (Transport) — доставка вантажів
    
    let agent_type = "Розвідник";
    
    // =========================================================================
    // СТАТУС АГЕНТА
    // =========================================================================
    // Агент може бути в різних станах:
    // - Ініціалізація — запуск систем
    // - Готовий — очікує команди
    // - На місії — виконує завдання
    // - Повертається — летить на базу
    // - Критичний — потребує допомоги
    
    let status = "Ініціалізація";
    
    // =========================================================================
    // ВИВІД ІНФОРМАЦІЇ ПРО АГЕНТА
    // =========================================================================
    // Це простий "інтерфейс" нашого агента.
    // У майбутньому замість println! буде мережева комунікація.
    
    // Виводимо заголовок системи
    // Символи ╔ ╗ ╚ ╝ ║ ═ — це box-drawing characters для гарного оформлення
    println!("╔══════════════════════════════════════════╗");
    println!("║       СИСТЕМА КЕРУВАННЯ РОЄМ БПЛА        ║");
    println!("╠══════════════════════════════════════════╣");
    
    // Виводимо дані агента
    // {:<30} — вирівнювання ліворуч, ширина 30 символів
    // Це забезпечує рівні колонки
    println!("║ Агент:  {:<30} ║", agent_id);
    println!("║ Тип:    {:<30} ║", agent_type);
    println!("║ Статус: {:<30} ║", status);
    println!("╚══════════════════════════════════════════╝");
    
    // =========================================================================
    // СИМУЛЯЦІЯ ЦИКЛУ ІНІЦІАЛІЗАЦІЇ
    // =========================================================================
    // Справжній агент при запуску перевіряє всі свої системи.
    // Тут ми лише симулюємо цей процес текстовим виводом.
    
    println!();  // Порожній рядок для візуального розділення
    
    // Формат виводу: [ID] повідомлення
    // Це стандартний формат для логів у розподілених системах
    println!("[{}] Запуск системи...", agent_id);
    println!("[{}] Перевірка сенсорів... ОК", agent_id);
    println!("[{}] Перевірка зв'язку... ОК", agent_id);
    println!("[{}] Перевірка навігації... ОК", agent_id);
    println!("[{}] Перевірка батареї... ОК", agent_id);
    
    println!();
    println!("[{}] ✓ Агент готовий до роботи!", agent_id);
}
```

Запустіть програму:

```bash
cargo run
```

Очікуваний вивід:
```text
╔══════════════════════════════════════════╗
║       СИСТЕМА КЕРУВАННЯ РОЄМ БПЛА        ║
╠══════════════════════════════════════════╣
║ Агент:  SCOUT-001                        ║
║ Тип:    Розвідник                        ║
║ Статус: Ініціалізація                    ║
╚══════════════════════════════════════════╝

[SCOUT-001] Запуск системи...
[SCOUT-001] Перевірка сенсорів... ОК
[SCOUT-001] Перевірка зв'язку... ОК
[SCOUT-001] Перевірка навігації... ОК
[SCOUT-001] Перевірка батареї... ОК

[SCOUT-001] ✓ Агент готовий до роботи!
```

**Що демонструє ця програма:**

1. **Ідентичність агента** — кожен агент має унікальний ID
2. **Типізація** — агенти мають різні ролі в рої
3. **Стан** — агент знає свій поточний статус
4. **Послідовність ініціалізації** — перед роботою агент перевіряє системи
5. **Формат логування** — стандартний формат `[ID] повідомлення`

**Обмеження поточної реалізації:**

На цьому етапі наш "агент" має серйозні обмеження:

| Обмеження | Причина | Коли виправимо |
|-----------|---------|----------------|
| Статичні дані | Ще не знаємо про типи даних | Розділ 2 |
| Немає логіки рішень | Ще не знаємо про if/else | Розділ 4 |
| Одноразове виконання | Ще не знаємо про цикли | Розділ 4 |
| Немає структури | Дані розкидані по змінних | Розділ 11 |

---

## 1.3 ЛАБОРАТОРНА РОБОТА

### Мета роботи

Закріпити навички створення проєктів, використання println! та розуміння структури Rust-програми.

### Завдання

Створіть програму, яка симулює запуск **кількох агентів різних типів**.

### Вимоги

1. Створіть новий проєкт `multi_agent_demo`
2. Програма має виводити інформацію про **3 різних агенти**:
   - Розвідник (SCOUT-001)
   - Бойовий (COMBAT-001)
   - Зв'язківець (COMM-001)
3. Для кожного агента виведіть:
   - ID
   - Тип
   - Роль у місії (текстовий опис)
4. Використайте форматування для вирівнювання виводу

### Приклад виконання

```text
╔═══════════════════════════════════════════════════════════╗
║              ІНІЦІАЛІЗАЦІЯ РОЮ БПЛА                       ║
╠═══════════════════════════════════════════════════════════╣
║                                                           ║
║  Агент #1: SCOUT-001                                      ║
║  Тип:      Розвідник                                      ║
║  Роль:     Пошук та виявлення ворожих позицій            ║
║                                                           ║
║  Агент #2: COMBAT-001                                     ║
║  Тип:      Бойовий                                        ║
║  Роль:     Ураження виявлених цілей                      ║
║                                                           ║
║  Агент #3: COMM-001                                       ║
║  Тип:      Зв'язківець                                    ║
║  Роль:     Ретрансляція даних між агентами               ║
║                                                           ║
╚═══════════════════════════════════════════════════════════╝

[SYSTEM] Ініціалізація завершена
[SYSTEM] Агентів у рої: 3
[SYSTEM] Рій готовий до місії!
```

### Критерії оцінювання

| Критерій | Бали |
|----------|------|
| Проєкт створено та компілюється | 2 |
| Виводиться інформація про 3 агенти | 3 |
| Використано форматування (вирівнювання) | 2 |
| Код має коментарі | 2 |
| Власне оформлення виводу | 1 |
| **Максимум** | **10** |

---

## 1.4 TROUBLESHOOTING

### Проблема 1: Команда cargo не знайдена

**Симптоми:**
```text
bash: cargo: command not found
```

**Причина:** Змінні середовища не оновились після встановлення.

**Рішення:**

Крок 1: Перезапустіть термінал (закрийте і відкрийте заново)

Крок 2: Якщо не допомогло, виконайте:
```bash
source "$HOME/.cargo/env"
```

Крок 3: Якщо все ще не працює, перевірте, чи існує файл:
```bash
ls ~/.cargo/bin/cargo
```

Якщо файлу немає — перевстановіть Rust.

### Проблема 2: Помилка "expected `;`"

**Симптоми:**
```rust
error: expected `;`, found `}`
```

**Причина:** Ви забули крапку з комою в кінці рядка.

**Приклад помилкового коду:**
```rust
fn main() {
    println!("Hello")   // ← Немає ;
}
```

**Виправлення:**
```rust
fn main() {
    println!("Hello");  // ← Додали ;
}
```

**Пояснення:** У Rust кожен statement (інструкція) має закінчуватись крапкою з комою. Це відрізняється від Python, де достатньо нового рядка.

### Проблема 3: Неправильні лапки

**Симптоми:**
```rust
error: unknown start of token: \u{201c}
```

**Причина:** Ви використали "розумні лапки" (curly quotes) замість прямих.

**Помилково:**
```rust
println!("Hello");  // Неправильні лапки "
```

**Правильно:**
```rust
println!("Hello");  // Правильні лапки "
```

**Як це трапляється:** Деякі текстові редактори (Word, Google Docs) автоматично замінюють лапки. Використовуйте спеціалізований редактор коду (VS Code, Sublime Text).

### Проблема 4: Unicode символи не відображаються

**Симптоми:**
```text
╔═══════╗ виглядає як ????????
```

**Причина:** Термінал не підтримує UTF-8.

**Рішення для Windows:**
```cmd
chcp 65001
```

**Рішення для Linux/macOS:**
Додайте до ~/.bashrc:
```bash
export LANG=uk_UA.UTF-8
```

---

## ✅ ТЕСТИ ДЛЯ САМОПЕРЕВІРКИ

### Рівень 1: Базове розуміння

**1.1** Яка команда створює новий Rust-проєкт?

<details>
<summary>Відповідь</summary>

```bash
cargo new назва_проєкту
```

</details>

**1.2** Чим відрізняється `cargo run` від `cargo build`?

<details>
<summary>Відповідь</summary>

- `cargo build` — лише компілює проєкт
- `cargo run` — компілює І запускає

</details>

**1.3** Як називається файл з метаданими проєкту?

<details>
<summary>Відповідь</summary>

Cargo.toml

</details>

### Рівень 2: Розуміння синтаксису

**2.1** Що виведе цей код?

```rust
fn main() {
    let x = 5;
    let y = 10;
    println!("{} + {} = {}", x, y, x + y);
}
```

<details>
<summary>Відповідь</summary>

```rust
5 + 10 = 15
```

Плейсхолдери `{}` замінюються значеннями x, y, та результатом x + y по порядку.

</details>

**2.2** Чому `println!` має знак оклику?

<details>
<summary>Відповідь</summary>

Знак оклику вказує, що це **макрос**, а не функція. Макроси можуть приймати довільну кількість аргументів різних типів, що неможливо для звичайних функцій у Rust.

</details>

### Рівень 3: Практичне застосування

**3.1** Виправте помилку:

```rust
fn main() {
    let name = "Rust"
    println!("Hello, {}", name);
}
```

<details>
<summary>Відповідь</summary>

Пропущена крапка з комою:

```rust
fn main() {
    let name = "Rust";  // ← Додали ;
    println!("Hello, {}", name);
}
```

</details>

---

## 📝 РЕЗЮМЕ

### Що ви вивчили

1. **Інструменти Rust:**
   - rustup — керує версіями
   - rustc — компілює код
   - cargo — організовує проєкт

2. **Структура проєкту:**
   - Cargo.toml — метадані та залежності
   - src/main.rs — точка входу
   - target/ — скомпільовані файли

3. **Базовий синтаксис:**
   - `fn main()` — точка входу
   - `let` — оголошення змінної
   - `println!` — вивід тексту
   - `{}` — плейсхолдер для значень

4. **Концепція агента:**
   - Агент має ідентичність
   - Агент має стан
   - Агент виконує дії

### Ключові команди

```bash
cargo new назва    # Створити проєкт
cargo build        # Скомпілювати
cargo run          # Скомпілювати та запустити
cargo check        # Швидка перевірка без компіляції
```

---

## 🔗 ЗВ'ЯЗОК З НАСТУПНИМ РОЗДІЛОМ

Ви створили першого "агента", але він дуже обмежений:
- Усі дані жорстко закодовані
- Немає можливості змінювати значення
- Немає типів даних

У **Розділі 2: Змінні та базові типи даних** ви дізнаєтесь:
- Як створювати змінні різних типів
- Чому Rust розрізняє `let` та `let mut`
- Як моделювати стан агента через примітивні типи

---

> **Наступний розділ:** [Розділ 2: Змінні та базові типи даних](./02_Zminni_ta_bazovi_typy.md)
