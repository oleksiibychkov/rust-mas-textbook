# Розділ 3: Функції та параметри

---

## 📋 Анотація

Функції — це основний інструмент організації коду в будь-якій мові програмування. Вони дозволяють розбити складну програму на менші, зрозумілі частини, кожна з яких виконує одну конкретну задачу. У цьому розділі ви опануєте мистецтво написання функцій у Rust: від найпростіших до функцій з параметрами та значеннями, що повертаються. Особливу увагу ми приділимо унікальній концепції Rust — різниці між **statements** та **expressions**, яка визначає, як функції повертають значення. У контексті нашого агента БПЛА функції стануть "поведінками": обчислення відстані, перевірка батареї, прийняття рішень про напрямок.

---

## 🎯 Цілі навчання

Після завершення цього розділу ви зможете:

1. **Оголошувати функції** з параметрами та типами повернення
2. **Пояснити різницю** між statements та expressions
3. **Використовувати** неявне та явне повернення значень
4. **Застосовувати** ранній return для спрощення логіки
5. **Організовувати код агента** як набір спеціалізованих функцій
6. **Документувати функції** для зрозумілості коду

---

## 📚 Ключові терміни

| Термін | Визначення |
|--------|------------|
| **функція (function)** | Іменований блок коду, що виконує певну задачу |
| **параметр (parameter)** | Змінна в оголошенні функції, що приймає значення |
| **аргумент (argument)** | Конкретне значення, передане у функцію при виклику |
| **тип повернення** | Тип значення, яке функція повертає |
| **statement** | Інструкція, що виконує дію, але не має значення |
| **expression** | Вираз, що обчислюється до певного значення |
| **unit type `()`** | Порожній тип, означає "нічого" |
| **сигнатура функції** | Ім'я, параметри та тип повернення функції |

---

## 💡 Мотиваційний кейс: Чому функції критичні

Уявіть, що ви пишете програму керування дроном. Ваш початковий код у `main()`:

```rust
fn main() {
    // Обчислюємо відстань до цілі
    let dx = target_x - agent_x;
    let dy = target_y - agent_y;
    let distance = (dx * dx + dy * dy).sqrt();
    
    // Перевіряємо батарею
    let battery_ok = battery > 20;
    
    // Перевіряємо, чи можемо долетіти
    let fuel_needed = distance * 0.1;
    let can_reach = battery as f64 > fuel_needed;
    
    // ... ще 50 рядків логіки ...
}
```

**Через місяць** ваш `main()` — це 500 рядків. Проблеми:

1. **Неможливо знайти потрібний код** — де обчислюється відстань? Прокручуйте 10 екранів
2. **Дублювання** — обчислення відстані потрібне в 5 місцях, ви копіюєте код
3. **Важко тестувати** — як перевірити логіку батареї окремо?
4. **Страшно змінювати** — зміна в одному місці ламає щось в іншому

**З функціями:**

```rust
fn main() {
    let distance = calculate_distance(agent_x, agent_y, target_x, target_y);
    let can_reach = can_reach_target(battery, distance);
    
    if can_reach {
        navigate_to_target();
    } else {
        return_to_base();
    }
}
```

**6 рядків**, і ви бачите всю логіку. Кожна функція — окрема відповідальність. Потрібно змінити обчислення відстані? Йдете в `calculate_distance()`. Це не просто зручність — це необхідність.

---

## 3.1 ТЕОРІЯ: АНАТОМІЯ ФУНКЦІЇ

### 3.1.1 Структура функції в Rust

Кожна функція в Rust має чітку структуру. Розберемо її на прикладі:

```rust
fn calculate_distance(x1: f64, y1: f64, x2: f64, y2: f64) -> f64 {
    let dx = x2 - x1;
    let dy = y2 - y1;
    (dx * dx + dy * dy).sqrt()
}
```

**Схема структури:**

```text
fn calculate_distance(x1: f64, y1: f64, x2: f64, y2: f64) -> f64 {
│  │                  │                                    │    │
│  │                  └── параметри (ім'я: тип)            │    │
│  │                                                       │    │
│  └── ім'я функції (snake_case)                          │    │
│                                                          │    │
└── ключове слово fn                                      │    │
                                                          │    │
                        тип повернення ───────────────────┘    │
                                                               │
                        тіло функції ──────────────────────────┘
```

**Розбір кожного елементу:**

| Елемент | Приклад | Призначення |
|---------|---------|-------------|
| `fn` | `fn` | Ключове слово, що позначає початок функції |
| Ім'я | `calculate_distance` | Унікальний ідентифікатор функції |
| Параметри | `(x1: f64, ...)` | Вхідні дані функції |
| `->` | `-> f64` | Стрілка перед типом повернення |
| Тип повернення | `f64` | Тип значення, що повертається |
| Тіло | `{ ... }` | Код, що виконується |

### 3.1.2 Найпростіша функція

Почнемо з функції без параметрів і без повернення значення:

```rust
fn say_hello() {
    println!("Привіт від агента!");
}

fn main() {
    say_hello();  // Виклик функції
    say_hello();  // Можна викликати багато разів
    say_hello();
}
```

**Що відбувається при виконанні:**

1. Програма починає виконання з `main()`
2. Зустрічає `say_hello();` — передає керування у функцію `say_hello`
3. Функція виводить текст
4. Керування повертається в `main()` на наступний рядок
5. Процес повторюється для кожного виклику

**Результат:**
```text
Привіт від агента!
Привіт від агента!
Привіт від агента!
```

**Навіщо це потрібно:**

Навіть проста функція без параметрів корисна:
- **Іменування дії** — `initialize_sensors()` зрозуміліше ніж 20 рядків коду
- **Уникнення дублювання** — один раз написали, викликаємо скрізь
- **Легкість зміни** — змінюєте в одному місці, працює всюди

### 3.1.3 Функція з одним параметром

Додамо вхідні дані:

```rust
fn greet_agent(agent_id: u8) {
    println!("Привіт, агент №{}!", agent_id);
}

fn main() {
    greet_agent(1);
    greet_agent(7);
    greet_agent(42);
}
```

**Розбір синтаксису параметра:**

```text
agent_id: u8
│         │
│         └── тип параметра
│
└── ім'я параметра
```

**Що відбувається при виклику `greet_agent(7)`:**

1. Значення `7` копіюється в параметр `agent_id`
2. Всередині функції `agent_id` має значення `7`
3. Функція використовує це значення
4. Після завершення функції параметр `agent_id` зникає

**Результат:**
```text
Привіт, агент №1!
Привіт, агент №7!
Привіт, агент №42!
```

**Важливо:** Типи параметрів ОБОВ'ЯЗКОВІ. Rust не виводить типи параметрів автоматично:

```rust
// ПОМИЛКА: не вказано тип
fn greet(name) {  // error: expected one of `:`, `@`, or `|`
    println!("Hi, {}", name);
}

// ПРАВИЛЬНО: тип вказано явно
fn greet(name: &str) {
    println!("Hi, {}", name);
}
```

### 3.1.4 Функція з кількома параметрами

Функція може приймати будь-яку кількість параметрів:

```rust
fn print_position(agent_id: u8, x: i32, y: i32, altitude: u16) {
    println!("Агент {}: позиція ({}, {}), висота {} м", 
             agent_id, x, y, altitude);
}

fn main() {
    print_position(1, 100, 200, 50);
    print_position(2, -50, 300, 75);
    print_position(3, 0, 0, 100);
}
```

**Правила для параметрів:**

1. **Розділяються комами** — `(a: i32, b: i32, c: i32)`
2. **Кожен має свій тип** — навіть якщо типи однакові
3. **Порядок має значення** — аргументи присвоюються по порядку

**Що відбувається при виклику `print_position(1, 100, 200, 50)`:**

| Аргумент | Присвоюється параметру | Тип |
|----------|------------------------|-----|
| `1` | `agent_id` | `u8` |
| `100` | `x` | `i32` |
| `200` | `y` | `i32` |
| `50` | `altitude` | `u16` |

**Результат:**
```rust
Агент 1: позиція (100, 200), висота 50 м
Агент 2: позиція (-50, 300), висота 75 м
Агент 3: позиція (0, 0), висота 100 м
```

### 3.1.5 Функція з поверненням значення

Тепер функція, що не просто виконує дію, а **обчислює і повертає** результат:

```rust
fn square(x: i32) -> i32 {
    x * x
}

fn main() {
    let result = square(5);
    println!("5² = {}", result);
    
    // Можна використовувати результат напряму
    println!("7² = {}", square(7));
    
    // Або в виразах
    let sum_of_squares = square(3) + square(4);
    println!("3² + 4² = {}", sum_of_squares);
}
```

**Розбір синтаксису:**

```rust
fn square(x: i32) -> i32 {
//                 ^^^^^^
//                 тип повернення
    x * x
//  ^^^^^
//  значення, що повертається (без крапки з комою!)
}
```

**Що відбувається при виклику `square(5)`:**

1. Значення `5` копіюється в параметр `x`
2. Обчислюється `x * x` = `5 * 5` = `25`
3. Значення `25` **повертається** з функції
4. У місці виклику `square(5)` "підставляється" значення `25`

**Результат:**
```rust
5² = 25
7² = 49
3² + 4² = 25
```

---

## 3.2 ТЕОРІЯ: STATEMENTS VS EXPRESSIONS

### 3.2.1 Що таке statement

**Statement** (інструкція) — це код, що **виконує дію**, але **не має значення**.

Приклади statements:

```rust
let x = 5;              // Оголошення змінної — statement
println!("Hello");      // Виклик макросу — statement
fn foo() { }            // Визначення функції — statement
```

**Ключова характеристика:** Statement не можна присвоїти змінній.

```rust
// ПОМИЛКА: let — це statement, він не має значення
let x = (let y = 5);  // error: expected expression, found `let` statement
```

### 3.2.2 Що таке expression

**Expression** (вираз) — це код, що **обчислюється до певного значення**.

Приклади expressions:

```rust
5                   // Літерал — expression (значення: 5)
3 + 4               // Арифметика — expression (значення: 7)
x * y               // Операція — expression (значення: результат множення)
square(5)           // Виклик функції — expression (значення: те, що повертає функція)
```

**Ключова характеристика:** Expression можна присвоїти змінній:

```rust
let a = 5;           // 5 — expression
let b = 3 + 4;       // 3 + 4 — expression
let c = square(5);   // square(5) — expression
```

### 3.2.3 Блок як expression

**Унікальна особливість Rust:** Блок коду `{ }` — це теж expression!

```rust
fn main() {
    let result = {
        let x = 10;
        let y = 20;
        x + y       // Останній рядок БЕЗ крапки з комою — значення блоку
    };
    
    println!("result = {}", result);  // 30
}
```

**Що тут відбувається:**

1. Створюється блок `{ ... }`
2. Всередині блоку створюються змінні `x` та `y`
3. Останній рядок `x + y` **без крапки з комою** — це значення всього блоку
4. Це значення (30) присвоюється змінній `result`

**Візуалізація:**

```rust
let result = {           ← Блок починається
    let x = 10;          ← Statement (є крапка з комою)
    let y = 20;          ← Statement (є крапка з комою)
    x + y                ← Expression (НЕМАЄ крапки з комою)
};                       ← Блок закінчується, повертає 30
```

### 3.2.4 Критична роль крапки з комою

**Крапка з комою перетворює expression на statement!**

Це найважливіше правило, яке часто викликає помилки:

```rust
fn main() {
    // БЕЗ крапки з комою — expression, блок повертає 6
    let value1 = {
        let x = 5;
        x + 1        // ← expression, значення 6
    };
    println!("value1 = {}", value1);  // 6
    
    // З крапкою з комою — statement, блок повертає ()
    let value2 = {
        let x = 5;
        x + 1;       // ← statement (крапка з комою!), значення ()
    };
    println!("value2 = {:?}", value2);  // ()
}
```

**Порівняння:**

| Код | Тип | Значення |
|-----|-----|----------|
| `x + 1` | expression | `6` |
| `x + 1;` | statement | `()` |

### 3.2.5 if/else як expression

В Rust `if/else` — це **expression**, що повертає значення:

```rust
fn main() {
    let battery = 75;
    
    // if/else як expression
    let status = if battery > 50 {
        "нормальний"
    } else {
        "низький"
    };
    
    println!("Статус батареї: {}", status);
}
```

**Що відбувається:**

1. Перевіряється умова `battery > 50` (75 > 50 = true)
2. Виконується гілка `true`, яка має значення `"нормальний"`
3. Це значення присвоюється змінній `status`

**Важливо:** Обидві гілки `if` та `else` повинні повертати **однаковий тип**:

```rust
// ПОМИЛКА: типи не збігаються
let value = if condition {
    5       // i32
} else {
    "text"  // &str — ПОМИЛКА!
};

// ПРАВИЛЬНО: обидві гілки повертають i32
let value = if condition {
    5
} else {
    10
};
```

### 3.2.6 Як функції повертають значення

Тепер ви розумієте, чому функції в Rust можуть повертати значення **без явного `return`**:

```rust
fn add(a: i32, b: i32) -> i32 {
    a + b  // Expression! Це значення функції
}
```

**Тіло функції — це блок.** Останній expression у блоці стає значенням, що повертається.

**Типова помилка:**

```rust
fn add(a: i32, b: i32) -> i32 {
    a + b;  // Помилка! Крапка з комою зробила це statement
            // Функція повертає (), а очікується i32
}
```

Компілятор допоможе:

```text
error[E0308]: mismatched types
 --> src/main.rs:1:31
  |
1 | fn add(a: i32, b: i32) -> i32 {
  |                           ^^^ expected `i32`, found `()`
2 |     a + b;
  |          - help: remove this semicolon to return this value
```

---

## 3.3 ТЕОРІЯ: ПОВЕРНЕННЯ ЗНАЧЕНЬ

### 3.3.1 Неявне повернення (ідіоматичний спосіб)

**Рекомендований спосіб** у Rust — останній expression без крапки з комою:

```rust
fn calculate_area(width: f64, height: f64) -> f64 {
    width * height  // Повертається автоматично
}

fn get_battery_status(level: u8) -> &'static str {
    if level > 80 {
        "відмінний"
    } else if level > 50 {
        "нормальний"
    } else if level > 20 {
        "низький"
    } else {
        "критичний"
    }
}
```

**Чому це ідіоматично:**

1. **Менше коду** — не треба писати `return`
2. **Явний потік** — значення "випливає" з кінця функції
3. **Функціональний стиль** — функція як обчислення значення

### 3.3.2 Явне повернення через return

Ключове слово `return` **негайно** виходить з функції з вказаним значенням:

```rust
fn divide(a: f64, b: f64) -> f64 {
    if b == 0.0 {
        return 0.0;  // Негайний вихід!
    }
    
    a / b  // Неявне повернення
}
```

**Коли використовувати `return`:**

1. **Ранній вихід** — вийти з функції до кінця
2. **Guard clauses** — перевірки на початку
3. **Вихід з циклів у функції**

### 3.3.3 Guard clauses: патерн раннього виходу

**Guard clause** — це перевірка на початку функції, що виходить рано при невалідних умовах:

```rust
fn calculate_flight_time(distance: f64, speed: f64) -> f64 {
    // Guard clause: перевіряємо некоректні вхідні дані
    if distance < 0.0 {
        return 0.0;  // Від'ємна відстань неможлива
    }
    if speed <= 0.0 {
        return 0.0;  // Нульова або від'ємна швидкість неможлива
    }
    
    // Основна логіка — тільки для валідних даних
    distance / speed
}
```

**Переваги guard clauses:**

1. **Валідація на початку** — одразу видно, що перевіряється
2. **Основна логіка не вкладена** — менше рівнів вкладеності
3. **Легко додавати перевірки** — просто ще один `if`

**Порівняння без guard clauses:**

```rust
// БЕЗ guard clauses — глибока вкладеність
fn calculate_flight_time_nested(distance: f64, speed: f64) -> f64 {
    if distance >= 0.0 {
        if speed > 0.0 {
            distance / speed
        } else {
            0.0
        }
    } else {
        0.0
    }
}

// З guard clauses — плоска структура
fn calculate_flight_time_flat(distance: f64, speed: f64) -> f64 {
    if distance < 0.0 { return 0.0; }
    if speed <= 0.0 { return 0.0; }
    
    distance / speed
}
```

### 3.3.4 Функції, що нічого не повертають

Деякі функції просто виконують дії без результату:

```rust
fn print_agent_status(id: u8, battery: u8) {
    println!("Агент #{}: батарея {}%", id, battery);
}

fn log_event(message: &str) {
    println!("[LOG] {}", message);
}
```

**Технічно** такі функції повертають **unit type** `()`:

```rust
// Ці оголошення еквівалентні:
fn do_something() { }
fn do_something() -> () { }
```

`()` — це "порожній" тип, що означає "нічого корисного не повертається".

### 3.3.5 Повернення кількох значень через кортеж

Функція може повернути **тільки одне значення**, але це може бути **кортеж**:

```rust
fn get_position() -> (f64, f64) {
    let x = 100.5;
    let y = 200.3;
    (x, y)  // Повертаємо кортеж
}

fn main() {
    // Спосіб 1: отримати кортеж цілком
    let position = get_position();
    println!("Позиція: ({}, {})", position.0, position.1);
    
    // Спосіб 2: деструктуризація
    let (x, y) = get_position();
    println!("x = {}, y = {}", x, y);
}
```

**Приклад для агента:**

```rust
fn calculate_vector(x1: f64, y1: f64, x2: f64, y2: f64) -> (f64, f64, f64) {
    let dx = x2 - x1;
    let dy = y2 - y1;
    let distance = (dx * dx + dy * dy).sqrt();
    
    (dx, dy, distance)  // Повертаємо три значення
}

fn main() {
    let (dx, dy, dist) = calculate_vector(0.0, 0.0, 3.0, 4.0);
    println!("Вектор: ({}, {}), відстань: {}", dx, dy, dist);
    // Вектор: (3, 4), відстань: 5
}
```

---

## 3.4 ПРАКТИКА: ПРОСТІ ФУНКЦІЇ

### 3.4.1 Експеримент: перша функція

Створіть проєкт і напишіть найпростішу функцію:

```rust
// Функція, що виводить привітання
// Не приймає параметрів, не повертає значення
fn greet() {
    println!("Привіт! Система агентів БПЛА активована.");
}

fn main() {
    // Виклик функції — просто ім'я та круглі дужки
    greet();
    
    // Можна викликати багато разів
    greet();
}
```

**Що демонструє цей код:**

1. **Синтаксис оголошення** — `fn ім'я() { тіло }`
2. **Синтаксис виклику** — `ім'я()`
3. **Багаторазовий виклик** — функцію можна викликати скільки завгодно

**Результат:**
```text
Привіт! Система агентів БПЛА активована.
Привіт! Система агентів БПЛА активована.
```

### 3.4.2 Експеримент: функція з параметром

```rust
// Функція приймає один параметр — ID агента
// Параметр agent_id має тип u8 (число 0-255)
fn report_agent(agent_id: u8) {
    println!("Агент #{:03} доповідає: готовий до роботи!", agent_id);
}

fn main() {
    // При виклику передаємо конкретне значення (аргумент)
    report_agent(1);    // agent_id = 1
    report_agent(7);    // agent_id = 7
    report_agent(42);   // agent_id = 42
    
    // Аргументом може бути змінна
    let my_id: u8 = 100;
    report_agent(my_id);  // agent_id = 100
}
```

**Що демонструє цей код:**

1. **Параметр з типом** — `agent_id: u8`
2. **Передача літерала** — `report_agent(1)`
3. **Передача змінної** — `report_agent(my_id)`
4. **Форматування** — `{:03}` виводить число з ведучими нулями

**Результат:**
```text
Агент #001 доповідає: готовий до роботи!
Агент #007 доповідає: готовий до роботи!
Агент #042 доповідає: готовий до роботи!
Агент #100 доповідає: готовий до роботи!
```

### 3.4.3 Експеримент: функція з поверненням

```rust
// Функція приймає число і повертає його квадрат
// -> i32 означає: "повертає значення типу i32"
fn square(x: i32) -> i32 {
    x * x  // Це expression — автоматично повертається
}

fn main() {
    // Результат функції можна зберегти в змінну
    let result = square(5);
    println!("5² = {}", result);
    
    // Або використати напряму
    println!("7² = {}", square(7));
    
    // Або в обчисленнях
    let hypotenuse_squared = square(3) + square(4);
    println!("3² + 4² = {} (гіпотенуза² трикутника 3-4-5)", hypotenuse_squared);
}
```

**Що демонструє цей код:**

1. **Тип повернення** — `-> i32`
2. **Неявне повернення** — останній expression без `;`
3. **Збереження результату** — `let result = square(5)`
4. **Комбінування викликів** — `square(3) + square(4)`

**Результат:**
```rust
5² = 25
7² = 49
3² + 4² = 25 (гіпотенуза² трикутника 3-4-5)
```

### 3.4.4 Експеримент: statements vs expressions

```rust
fn main() {
    // ===== БЛОК ЯК EXPRESSION =====
    
    // Блок без крапки з комою в кінці — повертає значення
    let value1 = {
        let a = 10;
        let b = 20;
        a + b        // ← Expression! Значення блоку = 30
    };
    println!("value1 = {}", value1);  // 30
    
    // Блок з крапкою з комою в кінці — повертає ()
    let value2 = {
        let a = 10;
        let b = 20;
        a + b;       // ← Statement! Значення блоку = ()
    };
    println!("value2 = {:?}", value2);  // ()
    
    // ===== IF/ELSE ЯК EXPRESSION =====
    
    let battery = 75;
    
    // if/else повертає значення
    let status = if battery > 50 {
        "OK"         // ← значення гілки true
    } else {
        "LOW"        // ← значення гілки false
    };
    
    println!("Battery: {} → {}", battery, status);
}
```

**Що демонструє цей код:**

1. **Блок як expression** — `{ ... }` може мати значення
2. **Роль крапки з комою** — перетворює expression на statement
3. **Unit type** — `()` означає "нічого"
4. **if/else як expression** — можна присвоювати результат

**Результат:**
```rust
value1 = 30
value2 = ()
Battery: 75 → OK
```

---

## 3.5 ПРАКТИКА: ФУНКЦІЇ ДЛЯ АГЕНТА БПЛА

### 3.5.1 Обчислення відстані

Одна з найважливіших функцій для агента — обчислення відстані між двома точками:

```rust
/// Обчислює евклідову відстань між двома точками на площині.
/// 
/// # Формула
/// distance = √((x2-x1)² + (y2-y1)²)
///
/// # Параметри
/// - x1, y1: координати першої точки
/// - x2, y2: координати другої точки
///
/// # Повертає
/// Відстань як число з плаваючою точкою
fn calculate_distance(x1: f64, y1: f64, x2: f64, y2: f64) -> f64 {
    // Крок 1: Обчислюємо різницю координат
    let dx = x2 - x1;  // Різниця по X
    let dy = y2 - y1;  // Різниця по Y
    
    // Крок 2: Обчислюємо суму квадратів
    let sum_of_squares = dx * dx + dy * dy;
    
    // Крок 3: Обчислюємо корінь (метод .sqrt())
    sum_of_squares.sqrt()  // Повертаємо результат
}

fn main() {
    // Тест 1: відомий прямокутний трикутник 3-4-5
    let d1 = calculate_distance(0.0, 0.0, 3.0, 4.0);
    println!("Відстань (0,0) → (3,4) = {}", d1);  // 5.0
    
    // Тест 2: агент і ціль
    let agent_x = 100.0;
    let agent_y = 200.0;
    let target_x = 400.0;
    let target_y = 600.0;
    
    let distance_to_target = calculate_distance(agent_x, agent_y, target_x, target_y);
    println!("Відстань до цілі: {:.2} м", distance_to_target);  // 500.00 м
}
```

**Розбір логіки:**

1. **Різниця координат** — наскільки треба пройти по X та Y
2. **Сума квадратів** — теорема Піфагора: c² = a² + b²
3. **Корінь** — отримуємо фінальну відстань

**Результат:**
```rust
Відстань (0,0) → (3,4) = 5
Відстань до цілі: 500.00 м
```

### 3.5.2 Перевірка рівня батареї

```rust
/// Повертає текстовий статус батареї на основі рівня заряду.
///
/// # Рівні
/// - > 80%: "відмінний"
/// - > 50%: "нормальний"  
/// - > 20%: "низький"
/// - ≤ 20%: "критичний"
fn get_battery_status(level: u8) -> &'static str {
    if level > 80 {
        "відмінний"
    } else if level > 50 {
        "нормальний"
    } else if level > 20 {
        "низький"
    } else {
        "критичний"
    }
}

/// Перевіряє, чи достатньо батареї для польоту заданої відстані.
///
/// # Припущення
/// Витрата: 1% батареї на 50 метрів польоту
fn can_complete_mission(battery: u8, distance: f64) -> bool {
    // Розраховуємо необхідний заряд
    let required_percent = (distance / 50.0) as u8;
    
    // Додаємо 20% резерву для повернення
    let total_required = required_percent.saturating_add(20);
    
    // Порівнюємо з наявним зарядом
    battery >= total_required
}

fn main() {
    // Тестуємо статуси
    println!("=== Статуси батареї ===");
    for level in [95, 75, 45, 15] {
        println!("{}%: {}", level, get_battery_status(level));
    }
    
    // Тестуємо можливість місії
    println!("\n=== Перевірка місій ===");
    let battery = 60;
    
    for distance in [100.0, 500.0, 1000.0, 2000.0] {
        let can_do = can_complete_mission(battery, distance);
        let status = if can_do { "✓ можливо" } else { "✗ неможливо" };
        println!("Батарея {}%, відстань {} м: {}", battery, distance, status);
    }
}
```

**Результат:**
```text
=== Статуси батареї ===
95%: відмінний
75%: нормальний
45%: низький
15%: критичний

=== Перевірка місій ===
Батарея 60%, відстань 100 м: ✓ можливо
Батарея 60%, відстань 500 м: ✓ можливо
Батарея 60%, відстань 1000 м: ✗ неможливо
Батарея 60%, відстань 2000 м: ✗ неможливо
```

### 3.5.3 Повний приклад: система агента

```rust
// ============================================================
// НАСКРІЗНИЙ ПРОЄКТ: Система функцій агента БПЛА
// ============================================================

fn main() {
    println!("╔════════════════════════════════════════════════╗");
    println!("║     СИСТЕМА КЕРУВАННЯ АГЕНТОМ БПЛА             ║");
    println!("╚════════════════════════════════════════════════╝\n");
    
    // Ініціалізація агента
    let agent_id: u8 = 7;
    let mut position_x: f64 = 0.0;
    let mut position_y: f64 = 0.0;
    let mut battery: u8 = 85;
    
    // Ціль місії
    let target_x: f64 = 300.0;
    let target_y: f64 = 400.0;
    
    // Вивід початкового стану
    print_agent_status(agent_id, position_x, position_y, battery);
    
    // Обчислення відстані до цілі
    let distance = calculate_distance(position_x, position_y, target_x, target_y);
    println!("Відстань до цілі: {:.1} м", distance);
    
    // Перевірка можливості місії
    if can_complete_mission(battery, distance) {
        println!("✓ Місія можлива. Починаємо рух...\n");
        
        // Симуляція руху
        let (new_x, new_y, new_battery) = simulate_movement(
            position_x, position_y, 
            target_x, target_y, 
            battery
        );
        
        position_x = new_x;
        position_y = new_y;
        battery = new_battery;
        
        println!("--- Після руху ---");
        print_agent_status(agent_id, position_x, position_y, battery);
    } else {
        println!("✗ Недостатньо батареї для місії!");
    }
}

/// Виводить інформацію про стан агента
fn print_agent_status(id: u8, x: f64, y: f64, battery: u8) {
    println!("Агент #{:03}", id);
    println!("  Позиція: ({:.1}, {:.1})", x, y);
    println!("  Батарея: {}% [{}]", battery, get_battery_status(battery));
    println!();
}

/// Обчислює відстань між двома точками
fn calculate_distance(x1: f64, y1: f64, x2: f64, y2: f64) -> f64 {
    let dx = x2 - x1;
    let dy = y2 - y1;
    (dx * dx + dy * dy).sqrt()
}

/// Повертає текстовий статус батареї
fn get_battery_status(level: u8) -> &'static str {
    if level > 80 { "відмінний" }
    else if level > 50 { "нормальний" }
    else if level > 20 { "низький" }
    else { "критичний" }
}

/// Перевіряє можливість виконання місії
fn can_complete_mission(battery: u8, distance: f64) -> bool {
    let required = ((distance / 50.0) as u8).saturating_add(20);
    battery >= required
}

/// Симулює рух агента до цілі
/// Повертає: (нова_x, нова_y, залишок_батареї)
fn simulate_movement(
    from_x: f64, from_y: f64,
    to_x: f64, to_y: f64,
    battery: u8
) -> (f64, f64, u8) {
    let distance = calculate_distance(from_x, from_y, to_x, to_y);
    let battery_used = (distance / 50.0) as u8;
    let new_battery = battery.saturating_sub(battery_used);
    
    (to_x, to_y, new_battery)
}
```

**Результат:**
```text
╔════════════════════════════════════════════════╗
║     СИСТЕМА КЕРУВАННЯ АГЕНТОМ БПЛА             ║
╚════════════════════════════════════════════════╝

Агент #007
  Позиція: (0.0, 0.0)
  Батарея: 85% [відмінний]

Відстань до цілі: 500.0 м
✓ Місія можлива. Починаємо рух...

--- Після руху ---
Агент #007
  Позиція: (300.0, 400.0)
  Батарея: 75% [нормальний]
```

---

## 3.6 ЛАБОРАТОРНА РОБОТА

### Мета роботи

Навчитися декомпозувати задачі на функції та організовувати код агента.

### Завдання

Створіть систему функцій для агента БПЛА, що включає:

1. **Функцію обчислення напрямку** `calculate_heading(x1, y1, x2, y2) -> f64`
2. **Функцію нормалізації кута** `normalize_angle(angle) -> f64` (до 0-360°)
3. **Функцію оцінки пріоритету** `get_priority(agent_type: char, has_emergency: bool) -> u8`
4. **Головну функцію**, що демонструє роботу всіх функцій

### Критерії оцінювання

| Критерій | Бали |
|----------|------|
| Правильна реалізація `calculate_heading` | 3 |
| Правильна реалізація `normalize_angle` | 2 |
| Правильна реалізація `get_priority` | 2 |
| Демонстрація в main() | 2 |
| Документаційні коментарі | 1 |
| **Максимум** | **10** |

---

## 3.7 TROUBLESHOOTING

### Помилка: "mismatched types: expected `i32`, found `()`"

**Причина:** Ви додали крапку з комою до останнього expression.

```rust
fn add(a: i32, b: i32) -> i32 {
    a + b;  // ← Крапка з комою!
}
```

**Рішення:** Видаліть крапку з комою:

```rust
fn add(a: i32, b: i32) -> i32 {
    a + b  // ← Без крапки з комою
}
```

### Помилка: "expected `:`, found `)`"

**Причина:** Ви не вказали тип параметра.

```rust
fn greet(name) { }  // ← Немає типу
```

**Рішення:** Додайте тип:

```rust
fn greet(name: &str) { }
```

### Помилка: "function should have a snake case name"

**Причина:** Ім'я функції не в snake_case.

```rust
fn CalculateDistance() { }  // ← CamelCase
```

**Рішення:** Використовуйте snake_case:

```rust
fn calculate_distance() { }
```

---

## 📝 РЕЗЮМЕ

### Що ви вивчили

1. **Оголошення функцій** — `fn name(param: Type) -> ReturnType { body }`
2. **Statements vs Expressions** — statements виконують дії, expressions мають значення
3. **Крапка з комою** — перетворює expression на statement
4. **Неявне повернення** — останній expression без `;`
5. **Явний return** — для раннього виходу
6. **Guard clauses** — перевірки на початку функції
7. **Кортежі** — для повернення кількох значень

### Ключові правила

- Типи параметрів ОБОВ'ЯЗКОВІ
- Останній expression без `;` — значення функції
- if/else — це expression, можна присвоювати
- Блок `{ }` — теж expression

---

## 🔗 ЗВ'ЯЗОК З НАСТУПНИМ РОЗДІЛОМ

Ми навчились створювати функції, але логіка поки лінійна. У реальному агенті потрібні:
- Умовні рішення (якщо батарея низька — повертатись)
- Цикли (патрулювати, поки є заряд)

У **Розділі 4: Керування потоком виконання** ви дізнаєтесь:
- Як працює if/else для складних умов
- Як організувати цикли loop, while, for
- Як реалізувати цикл сприйняття-рішення-дії агента

---

> **Наступний розділ:** [Розділ 4: Керування потоком виконання](./04_Keruvannya_potokom.md)
