# Підступні задачі з цілими числами

---

## 📋 Анотація

Цей розділ розкриває неочевидні аспекти роботи з цілими числами, які можуть призвести до катастрофічних наслідків. Ви дізнаєтесь, чому програма, що бездоганно працює в режимі розробки, може видавати абсурдні результати в продакшені, як просте віднімання може перетворити нуль на чотири мільярди, та чому навіть така тривіальна операція як обчислення середнього може переповнити змінну. У контексті рою БПЛА ці знання життєво важливі: помилка переповнення в обчисленні координат або висоти може миттєво перетворити безпечний політ на катастрофу.

---

## 🎯 Цілі навчання

Після завершення цього розділу ви зможете:

1. **Пояснити** різницю поведінки overflow у debug та release режимах
2. **Розпізнавати** небезпечні операції з unsigned типами
3. **Уникати** переповнення при арифметичних операціях
4. **Правильно конвертувати** між числовими типами різної розрядності
5. **Розуміти** особливості оператора залишку від ділення для від'ємних чисел
6. **Використовувати** checked, saturating та wrapping арифметику
7. **Писати** безпечний код для критичних обчислень

---

## 📚 Ключові терміни

| Термін | Визначення |
|--------|------------|
| **переповнення (overflow)** | Ситуація, коли результат операції виходить за межі типу |
| **обертання (wrapping)** | Поведінка, при якій значення "обертається" через межу типу |
| **насичення (saturation)** | Поведінка, при якій значення обмежується максимумом/мінімумом типу |
| **checked arithmetic** | Арифметика, що повертає None при переповненні |
| **signed/unsigned** | Типи зі знаком (можуть бути від'ємними) та без знаку |
| **two's complement** | Спосіб представлення від'ємних чисел у двійковій системі |

---

## 💡 Мотиваційний кейс: Ракета Ariane 5 та 64-бітне число в 16-бітній змінній

4 червня 1996 року перша ракета Ariane 5 Європейського космічного агентства вибухнула через 37 секунд після старту. Збитки склали понад 370 мільйонів доларів. Причина — переповнення цілого числа.

Інерційна навігаційна система (IRS) обчислювала горизонтальну швидкість ракети як 64-бітне число з плаваючою точкою. Потім це значення конвертувалось у 16-бітне ціле зі знаком для передачі в інші системи. Проблема: Ariane 5 була значно потужнішою за Ariane 4, і її горизонтальна швидкість перевищувала максимальне значення для 16-бітного числа (32767).

При конвертації виникло переповнення. Система захисту інтерпретувала це як критичну помилку і вимкнула основну IRS. Резервна система мала той самий код і теж вимкнулась. Без навігаційних даних бортовий комп'ютер отримав некоректні значення, спробував скорегувати траєкторію, і ракета розвалилась від аеродинамічних навантажень.

Код, що бездоганно працював на Ariane 4 роками, став смертельним на новій ракеті через одну невраховану конвертацію типів.

---

## ТЕОРІЯ: ПРЕДСТАВЛЕННЯ ЦІЛИХ ЧИСЕЛ У ПАМ'ЯТІ

### Unsigned типи: просто двійкове число

Беззнакові типи (u8, u16, u32, u64, u128, usize) представляють невід'ємні цілі числа. Кожен біт має свою "вагу" — степінь двійки.

Для u8 (8 біт) значення обчислюється як: b₇×2⁷ + b₆×2⁶ + b₅×2⁵ + b₄×2⁴ + b₃×2³ + b₂×2² + b₁×2¹ + b₀×2⁰

Мінімальне значення: 00000000₂ = 0
Максимальне значення: 11111111₂ = 128 + 64 + 32 + 16 + 8 + 4 + 2 + 1 = 255

Діапазони unsigned типів:
- u8: 0 до 255 (2⁸ - 1)
- u16: 0 до 65535 (2¹⁶ - 1)
- u32: 0 до 4294967295 (≈4.3 мільярди)
- u64: 0 до 18446744073709551615 (≈18 квінтильйонів)

### Signed типи: two's complement

Знакові типи (i8, i16, i32, i64, i128, isize) використовують представлення "доповнення до двох" (two's complement). Старший біт має від'ємну вагу.

Для i8: -b₇×2⁷ + b₆×2⁶ + ... + b₀×2⁰

Це означає:
- 01111111₂ = 127 (максимальне додатне)
- 00000000₂ = 0
- 11111111₂ = -128 + 64 + 32 + 16 + 8 + 4 + 2 + 1 = -1
- 10000000₂ = -128 (мінімальне від'ємне)

Діапазони signed типів:
- i8: -128 до 127
- i16: -32768 до 32767
- i32: -2147483648 до 2147483647 (≈±2.1 мільярди)
- i64: ≈±9.2 квінтильйони

### Чому це важливо розуміти

Розуміння представлення допомагає передбачити поведінку при переповненні. Коли u8 зі значенням 255 збільшується на 1, всі біти стають нулями (з "переносом", який втрачається): 11111111 + 1 = 100000000, але зберігається лише 8 молодших біт = 00000000 = 0.

Аналогічно, коли u8 зі значенням 0 зменшується на 1: 00000000 - 1 = 11111111 = 255. Це називається "обертання" (wrapping).

---

## ТЕОРІЯ: ПЕРЕПОВНЕННЯ — НАЙПІДСТУПНІША ПАСТКА

### Дві поведінки Rust: debug vs release

Rust має унікальний підхід до переповнення: поведінка відрізняється залежно від режиму компіляції.

У **debug режимі** (за замовчуванням при `cargo build` або `cargo run`) переповнення викликає паніку. Програма аварійно завершується з повідомленням про помилку. Це допомагає виявити баги під час розробки.

У **release режимі** (при `cargo build --release`) переповнення виконує "тихе обертання" (wrapping). Жодної паніки, жодного попередження — просто неправильний результат.

Розглянемо приклад:

```rust
fn calculate_score(base: u8, bonus: u8) -> u8 {
    base + bonus
}

fn main() {
    let score = calculate_score(250, 10);
    println!("Score: {}", score);
}
```

У debug режимі: паніка з повідомленням "attempt to add with overflow".
У release режимі: виводить "Score: 4" (250 + 10 = 260, що обертається до 260 - 256 = 4).

Це надзвичайно підступно. Програма проходить всі тести в debug режимі (бо тести зазвичай не досягають граничних значень). Потім деплоїться в продакшен у release режимі. І тільки там, при реальних даних, починає видавати абсурдні результати — без жодних повідомлень про помилку.

### Чому Rust обрав таку поведінку

Перевірка на переповнення при кожній операції має вартість — додаткові інструкції процесора. У debug режимі це прийнятно заради безпеки. У release режимі пріоритет — продуктивність, тому перевірки вимкнені за замовчуванням.

Це компроміс. Деякі інші мови (Python) мають цілі числа довільної точності без переповнення, але платять за це продуктивністю. C та C++ мають undefined behavior при переповненні signed типів — ще гірше, бо компілятор може генерувати непередбачуваний код.

Rust дозволяє змінити поведінку release режиму через налаштування в Cargo.toml:

```toml
[profile.release]
overflow-checks = true
```

Але це рідко роблять через вплив на продуктивність.

### Коли переповнення найбільш небезпечне

Переповнення особливо небезпечне, коли результат використовується для:

**Індексації масивів**: переповнений індекс може вказувати на випадкову пам'ять або викликати паніку.

**Виділення пам'яті**: якщо розмір буфера обчислюється з переповненням, буде виділено менше пам'яті, ніж потрібно. Подальший запис вийде за межі.

**Умов циклів**: переповнення може перетворити скінченний цикл на нескінченний або навпаки.

**Критичних обчислень**: координати, висота, швидкість, відстань — все це може стати катастрофічно неправильним.

---

## ТЕОРІЯ: ВІДНІМАННЯ ВІД UNSIGNED — МИТТЄВА КАТАСТРОФА

### Проблема: від'ємних значень не існує

Unsigned типи не можуть представити від'ємні числа. Що станеться, якщо результат віднімання має бути від'ємним?

```rust
let a: u32 = 5;
let b: u32 = 10;
let result = a - b;  // 5 - 10 = ?
```

У debug режимі: паніка "attempt to subtract with overflow".
У release режимі: result = 4294967291 (обертання: 5 - 10 = -5, що в unsigned стає 2³² - 5).

Це не просто "велике число" — це максимально можливе значення мінус 4. У контексті БПЛА, якщо це відстань або координата, дрон може "телепортуватися" на тисячі кілометрів.

### Типові сценарії помилки

**Обчислення різниці**: порівняння двох значень, коли невідомо, яке більше.

```rust
fn distance(a: u32, b: u32) -> u32 {
    a - b  // БАГ: якщо b > a, отримаємо сміття
}
```

**Декремент лічильника**: зменшення, коли значення вже 0.

```rust
let mut remaining: u32 = 0;
remaining -= 1;  // Хотіли -1, отримали 4294967295
```

**Обчислення індексу**: віднімання від позиції.

```rust
let position: usize = 3;
let offset: usize = 5;
let new_position = position - offset;  // БАГ!
```

### Рішення: перевірка або зміна типу

**Перевірка перед відніманням**:

```rust
fn safe_subtract(a: u32, b: u32) -> Option<u32> {
    if a >= b { Some(a - b) } else { None }
}
```

**Використання checked_sub**:

```rust
let result = a.checked_sub(b);  // Повертає None якщо b > a
```

**Використання saturating_sub**:

```rust
let result = a.saturating_sub(b);  // Повертає 0 якщо b > a
```

**Зміна на signed тип**, якщо від'ємний результат можливий і має сенс:

```rust
let a: i32 = 5;
let b: i32 = 10;
let result = a - b;  // -5, коректно
```

---

## ТЕОРІЯ: ОБЧИСЛЕННЯ СЕРЕДНЬОГО — НЕОЧІКУВАНЕ ПЕРЕПОВНЕННЯ

### Класична формула і її проблема

Обчислення середнього двох чисел здається тривіальним:

```rust
fn average(a: u32, b: u32) -> u32 {
    (a + b) / 2
}
```

Проблема: `a + b` може переповнитись, навіть якщо результат (середнє) цілком поміщається в тип.

```rust
let x: u32 = 3_000_000_000;
let y: u32 = 2_000_000_000;
let avg = average(x, y);  // Очікуємо 2_500_000_000
```

Сума 3000000000 + 2000000000 = 5000000000, що перевищує максимум u32 (4294967295). У release режимі отримаємо (5000000000 - 4294967296) / 2 = 352516352 — абсолютно неправильно.

### Рішення: переупорядкування операцій

Математично еквівалентна формула, що не переповнюється:

```rust
fn average_safe(a: u32, b: u32) -> u32 {
    a / 2 + b / 2 + (a % 2 + b % 2) / 2
}
```

Тут кожна проміжна операція гарантовано не переповниться. Але формула складніша.

**Простіший варіант** для випадку, коли a ≤ b:

```rust
fn average_safe(a: u32, b: u32) -> u32 {
    a + (b - a) / 2  // b - a не переповниться, якщо b >= a
}
```

Або використати більший тип для проміжних обчислень:

```rust
fn average_safe(a: u32, b: u32) -> u32 {
    ((a as u64 + b as u64) / 2) as u32
}
```

Цей підхід безпечний, бо u64 гарантовано вміщує суму двох u32.

### Де це критично

Алгоритм бінарного пошуку традиційно використовує `mid = (low + high) / 2`. У масиві з мільярдами елементів (якщо індекси — u32 або навіть usize на 32-бітній системі) це може переповнитись.

Правильна формула: `mid = low + (high - low) / 2`.

---

## ТЕОРІЯ: КОНВЕРТАЦІЯ ТИПІВ — ВТРАТА ДАНИХ

### Звуження типу (narrowing)

При конвертації з більшого типу в менший зайві біти відкидаються:

```rust
let big: u32 = 256;
let small: u8 = big as u8;  // small = 0!
```

Число 256 у двійковій системі: 100000000 (9 біт). При конвертації в u8 залишається лише 8 молодших біт: 00000000 = 0.

Це не помилка компіляції, не паніка — просто втрата даних. Rust дозволяє це, бо `as` — явна операція, і програміст нібито знає, що робить.

```rust
let value: u16 = 1000;
let byte: u8 = value as u8;  // byte = 232 (1000 % 256)
```

### Конвертація signed ↔ unsigned

Ця конвертація зберігає бітове представлення, але інтерпретація змінюється:

```rust
let negative: i8 = -1;
let unsigned: u8 = negative as u8;  // unsigned = 255
```

Число -1 в i8 має представлення 11111111. Те саме представлення в u8 — це 255.

У зворотному напрямку:

```rust
let big: u8 = 200;
let signed: i8 = big as i8;  // signed = -56
```

Число 200 = 11001000₂. У i8 старший біт має від'ємну вагу: -128 + 64 + 8 = -56.

### Безпечні альтернативи

**TryFrom/TryInto** повертають Result замість тихої конвертації:

```rust
use std::convert::TryFrom;

let big: u32 = 256;
let result = u8::try_from(big);  // Err(TryFromIntError)

let small: u32 = 100;
let result = u8::try_from(small);  // Ok(100)
```

**Перевірка перед конвертацією**:

```rust
fn safe_convert(value: u32) -> Option<u8> {
    if value <= u8::MAX as u32 {
        Some(value as u8)
    } else {
        None
    }
}
```

### Особливий випадок: usize та архітектура

Тип `usize` має розмір, що залежить від архітектури: 32 біти на 32-бітних системах, 64 біти на 64-бітних. Це створює проблеми переносимості.

```rust
let value: u64 = 5_000_000_000;
let index: usize = value as usize;
```

На 64-бітній системі: index = 5000000000, коректно.
На 32-бітній системі: index = 705032704 (обертання), некоректно.

Код, що працює на вашій машині розробки, може ламатися на вбудованих системах (як мікроконтролери дронів).

---

## ТЕОРІЯ: ДІЛЕННЯ ТА ЗАЛИШОК ВІД ДІЛЕННЯ

### Ділення на нуль

На відміну від чисел з плаваючою точкою (де ділення на нуль дає нескінченність або NaN), ділення цілих чисел на нуль завжди викликає паніку:

```rust
let a: i32 = 10;
let b: i32 = 0;
let result = a / b;  // panic: attempt to divide by zero
```

Це паніка і в debug, і в release режимах. Rust не дозволяє "тихе" ділення на нуль для цілих.

Рішення — перевірка або checked_div:

```rust
let result = if b != 0 { Some(a / b) } else { None };
// або
let result = a.checked_div(b);  // None якщо b == 0
```

### Переповнення при діленні — так, воно можливе!

Існує рівно один випадок, коли ділення цілих може переповнитись:

```rust
let min: i32 = i32::MIN;  // -2147483648
let result = min / -1;     // panic в debug, 2147483648 в release... але це не поміщається!
```

Математично -2147483648 / -1 = 2147483648. Але максимальне значення i32 — лише 2147483647. Результат на одиницю більший.

У debug режимі це паніка. У release режимі поведінка залежить від платформи — зазвичай результат "обертається" до i32::MIN.

Цей випадок рідкісний, але існує. Якщо ви ділите на значення, що може бути -1, і ділене може бути MIN — перевіряйте.

### Оператор залишку (%) з від'ємними числами

Поведінка оператора `%` для від'ємних чисел відрізняється в різних мовах програмування. У Rust результат має знак діленого:

```rust
println!("{}", 7 % 3);    //  1
println!("{}", -7 % 3);   // -1
println!("{}", 7 % -3);   //  1
println!("{}", -7 % -3);  // -1
```

Інваріант: `a == (a / b) * b + (a % b)` завжди виконується.

Для -7 % 3: -7 / 3 = -2 (округлення до нуля), -2 * 3 = -6, -7 - (-6) = -1.

Це може бути несподіванкою, якщо ви очікуєте математичний модуль (завжди невід'ємний). Для отримання завжди невід'ємного залишку:

```rust
fn modulo(a: i32, b: i32) -> i32 {
    ((a % b) + b) % b
}

println!("{}", modulo(-7, 3));  // 2
```

Або використати метод `rem_euclid`:

```rust
println!("{}", (-7_i32).rem_euclid(3));  // 2
```

---

## ТЕОРІЯ: ПОРІВНЯННЯ SIGNED ТА UNSIGNED

### Rust забороняє пряме порівняння

На відміну від C/C++, Rust не дозволяє безпосередньо порівнювати signed та unsigned:

```rust
let a: i32 = -1;
let b: u32 = 1;
// if a < b { ... }  // ПОМИЛКА КОМПІЛЯЦІЇ!
```

Компілятор вимагає явної конвертації. Це захист від тонких помилок, що існують в C:

```c
// C код — небезпечний!
int a = -1;
unsigned int b = 1;
if (a < b) {
    printf("a < b\n");  // НЕ виконається!
}
```

У C -1 неявно конвертується в unsigned перед порівнянням, стаючи величезним числом (4294967295). Порівняння 4294967295 < 1 хибне.

### Як правильно порівнювати

**Конвертувати в signed** (якщо unsigned гарантовано поміщається):

```rust
let a: i32 = -1;
let b: u32 = 1;
if a < b as i32 {  // Безпечно, якщо b <= i32::MAX
    println!("a < b");
}
```

**Конвертувати в більший тип**:

```rust
let a: i32 = -1;
let b: u32 = 3_000_000_000;
if (a as i64) < (b as i64) {
    println!("a < b");
}
```

**Логічна перевірка**:

```rust
let a: i32 = -1;
let b: u32 = 1;
// Якщо a від'ємне, воно завжди менше за будь-яке unsigned
let a_less = a < 0 || (a as u32) < b;
```

---

## ТЕОРІЯ: ЯВНА АРИФМЕТИКА — CHECKED, SATURATING, WRAPPING

### Checked: повертає None при переповненні

```rust
let a: u8 = 250;
let b: u8 = 10;

let result = a.checked_add(b);  // None
let result = a.checked_sub(b);  // Some(240)
```

Методи: `checked_add`, `checked_sub`, `checked_mul`, `checked_div`, `checked_rem`, `checked_neg`, `checked_pow`.

Це найбезпечніший підхід для критичного коду — ви явно обробляєте випадок переповнення.

### Saturating: обмежує до максимуму/мінімуму

```rust
let a: u8 = 250;
let b: u8 = 10;

let result = a.saturating_add(b);  // 255 (не 260, не паніка, а максимум)
let result = 0u8.saturating_sub(10);  // 0 (не обертання, а мінімум)
```

Методи: `saturating_add`, `saturating_sub`, `saturating_mul`, `saturating_pow`.

Це корисно, коли "обмежене" значення краще за неправильне. Наприклад, якщо яскравість пікселя виходить за 255, логічно обмежити її до 255, а не обернути до 4.

### Wrapping: явне обертання

```rust
let a: u8 = 250;
let b: u8 = 10;

let result = a.wrapping_add(b);  // 4 (обертання)
```

Методи: `wrapping_add`, `wrapping_sub`, `wrapping_mul`, `wrapping_div`, `wrapping_rem`, `wrapping_neg`, `wrapping_pow`, `wrapping_shl`, `wrapping_shr`.

Це та сама поведінка, що й у release режимі, але явна. Корисно для криптографії, хеш-функцій, роботи з протоколами, де обертання — бажана поведінка.

### Overflowing: повертає результат і прапорець

```rust
let a: u8 = 250;
let b: u8 = 10;

let (result, overflowed) = a.overflowing_add(b);  // (4, true)
```

Повертає кортеж: обернений результат та булевий прапорець, чи відбулось переповнення. Корисно, коли потрібно і результат, і інформація про переповнення.

---

## ПРАКТИЧНІ РЕКОМЕНДАЦІЇ

### Правило 1: Для критичних обчислень використовуйте checked-арифметику

```rust
// Погано: тихе переповнення в release
fn calculate_altitude_change(current: u32, delta: u32) -> u32 {
    current + delta
}

// Добре: явна обробка переповнення
fn calculate_altitude_change(current: u32, delta: u32) -> Option<u32> {
    current.checked_add(delta)
}
```

### Правило 2: Уникайте unsigned для значень, що можуть бути від'ємними

```rust
// Погано: температура може бути від'ємною
fn process_temperature(temp: u32) { ... }

// Добре: використовуємо signed
fn process_temperature(temp: i32) { ... }
```

### Правило 3: При конвертації типів використовуйте TryFrom

```rust
// Погано: тиха втрата даних
let value: u32 = some_calculation();
let byte: u8 = value as u8;

// Добре: явна обробка помилки
let byte = u8::try_from(value).unwrap_or_else(|_| {
    log::warn!("Value {} doesn't fit in u8, clamping", value);
    u8::MAX
});
```

### Правило 4: Для індексів і розмірів перевіряйте межі

```rust
// Погано: потенційне переповнення
let index = base_index + offset;
let value = array[index];

// Добре: перевірка
let index = base_index.checked_add(offset)
    .filter(|&i| i < array.len())
    .expect("Index out of bounds");
let value = array[index];
```

### Правило 5: Документуйте припущення про діапазони

```rust
/// Обчислює відстань між точками.
/// 
/// # Panics
/// Панікує, якщо координати виходять за межі i32::MAX / 2,
/// що може призвести до переповнення при обчисленні квадратів.
fn distance(x1: i32, y1: i32, x2: i32, y2: i32) -> f64 {
    let dx = (x2 - x1) as i64;  // Розширюємо до i64 для безпеки
    let dy = (y2 - y1) as i64;
    ((dx * dx + dy * dy) as f64).sqrt()
}
```

### Правило 6: Тестуйте граничні значення

```rust
#[cfg(test)]
mod tests {
    use super::*;
    
    #[test]
    fn test_with_max_values() {
        let result = calculate(u32::MAX, 1);
        // Перевіряємо, що функція коректно обробляє граничні випадки
    }
    
    #[test]
    fn test_subtraction_near_zero() {
        let result = safe_subtract(1, 2);
        assert_eq!(result, None);
    }
}
```

---

## Застосування до рою БПЛА

### Безпечне обчислення координат

```rust
#[derive(Clone, Copy)]
struct Position {
    x: i32,  // Метри від бази, signed — може бути від'ємним
    y: i32,
    altitude: u32,  // Висота завжди невід'ємна
}

impl Position {
    /// Переміщує позицію на заданий вектор.
    /// Повертає None, якщо нова позиція виходить за допустимі межі.
    fn translate(&self, dx: i32, dy: i32, dz: i32) -> Option<Position> {
        let new_x = self.x.checked_add(dx)?;
        let new_y = self.y.checked_add(dy)?;
        
        // Висота потребує особливої обробки — вона unsigned
        let new_altitude = if dz >= 0 {
            self.altitude.checked_add(dz as u32)?
        } else {
            // dz від'ємний — перевіряємо, чи не опустимось нижче нуля
            let decrease = (-dz) as u32;
            self.altitude.checked_sub(decrease)?
        };
        
        Some(Position { x: new_x, y: new_y, altitude: new_altitude })
    }
    
    /// Обчислює відстань до іншої позиції.
    fn distance_to(&self, other: &Position) -> f64 {
        // Використовуємо i64 для проміжних обчислень
        let dx = (other.x as i64) - (self.x as i64);
        let dy = (other.y as i64) - (self.y as i64);
        let dz = (other.altitude as i64) - (self.altitude as i64);
        
        ((dx * dx + dy * dy + dz * dz) as f64).sqrt()
    }
}
```

### Безпечний лічильник палива

```rust
struct FuelTank {
    capacity: u32,     // Максимальна ємність у мілілітрах
    current: u32,      // Поточний рівень
}

impl FuelTank {
    fn consume(&mut self, amount: u32) -> Result<(), FuelError> {
        self.current = self.current
            .checked_sub(amount)
            .ok_or(FuelError::InsufficientFuel {
                requested: amount,
                available: self.current,
            })?;
        Ok(())
    }
    
    fn refuel(&mut self, amount: u32) -> u32 {
        let space_available = self.capacity.saturating_sub(self.current);
        let actual_amount = amount.min(space_available);
        
        // saturating_add гарантує, що не перевищимо capacity
        self.current = self.current.saturating_add(actual_amount);
        
        actual_amount  // Повертаємо скільки реально заправили
    }
    
    fn percentage(&self) -> u8 {
        // Обчислюємо відсоток без переповнення
        // current * 100 може переповнитись для великих значень
        let percentage = (self.current as u64 * 100 / self.capacity as u64) as u8;
        percentage.min(100)  // Гарантуємо не більше 100%
    }
}
```

### Безпечна конвертація телеметрії

```rust
/// Конвертує внутрішні координати (i32) у формат протоколу (i16).
/// Координати в протоколі мають менший діапазон через обмеження bandwidth.
fn encode_position_for_transmission(pos: Position) -> Result<EncodedPosition, TelemetryError> {
    let x = i16::try_from(pos.x / 10)  // Зменшуємо точність для компресії
        .map_err(|_| TelemetryError::CoordinateOutOfRange { 
            coord: "x", 
            value: pos.x 
        })?;
    
    let y = i16::try_from(pos.y / 10)
        .map_err(|_| TelemetryError::CoordinateOutOfRange { 
            coord: "y", 
            value: pos.y 
        })?;
    
    let altitude = u16::try_from(pos.altitude)
        .map_err(|_| TelemetryError::AltitudeOutOfRange { 
            value: pos.altitude 
        })?;
    
    Ok(EncodedPosition { x, y, altitude })
}
```

---

## Резюме

У цьому розділі ми розглянули підступні проблеми з цілими числами.

**Переповнення** — найнебезпечніша пастка. У debug режимі Rust викликає паніку, у release — тихе обертання. Код може працювати в тестах і ламатися в продакшені. Рішення: checked-арифметика для критичних обчислень.

**Віднімання від unsigned** перетворює невеликий результат на величезне число при "переході через нуль". Рішення: перевірка, saturating_sub, або signed типи.

**Обчислення середнього** (a + b) / 2 може переповнитись, навіть якщо результат поміщається. Рішення: a + (b - a) / 2 або проміжні обчислення в більшому типі.

**Конвертація типів** через `as` може втратити дані без попередження. Рішення: TryFrom/TryInto.

**Оператор %** для від'ємних чисел повертає результат зі знаком діленого, не завжди невід'ємний залишок. Рішення: rem_euclid для математичного модуля.

**Порівняння signed та unsigned** заборонене в Rust (на відміну від C), що захищає від тонких помилок.

**Явна арифметика** (checked, saturating, wrapping) дозволяє контролювати поведінку при переповненні замість покладання на режим компіляції.

---

## 🔗 Зв'язок з наступним матеріалом

Опанувавши підступності цілих чисел, ви готові до вивчення роботи з рядками та текстом у Rust. Там вас чекають свої сюрпризи: UTF-8 кодування, різниця між байтами та символами, та чому `string[0]` — це не те, що ви очікуєте.
